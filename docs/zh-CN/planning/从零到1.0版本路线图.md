# 从零到 1.0 版本路线图

Eva-CLI 从架构设计走向 1.0 发布，应按阶段推进，而不是直接从文档跳到大规模
Runtime 实现。每个阶段都要留下可评审、可测试、可版本化的交付物，并成为下一阶段
的基础。

截至 2026-07-03，仓库已经从纯文档阶段推进到 V0.5 可观测和加固阶段：

- 中文架构详案、英文公开入口和官网结构已经基本齐备。
- 根目录 `Cargo.toml` 已建立 workspace，`crates/` 下已有 `eva-core`、`eva-config`、
  `eva-policy`、`eva-eventbus`、`eva-scheduler`、`eva-agent`、`eva-lua-host`、
  `eva-capability`、`eva-adapter`、`eva-mcp`、`eva-discovery`、`eva-memory`、
  `eva-hardware`、`eva-backup`、`eva-lifecycle`、`eva-runtime`、`eva-cli` 等边界 crate。
- `eva-core` 已落地 Topic、Event、Invoke、Capability、ID 和结构化错误等基础契约，
  并带有聚焦单元测试。
- `config/schemas/` 已放置 Agent、Adapter、Capability、Eva、Policy、Routes 等
  JSON Schema 文件。
- `eva-cli` 已实现 `doctor`、`config validate`、`inspect`、`run --example basic` 和 `task status/logs/cancel`。
- `eva-runtime` 已提供 V0.3 no-op summary、V0.4 in-memory basic loop 与 V0.5 task diagnostics，`examples/basic/`
  可以端到端跑通 CLI -> EventBus -> Scheduler -> Agent -> Lua host -> builtin capability -> task report。

因此，当前定位不再是“准备创建模块结构”，而是：

1. 保持已拆分 crate 的契约边界，不让下层模块反向依赖组合根；
2. 以 V0.5 basic loop + task diagnostics 作为后续发布加固的回归基线；
3. V1.0 core 发布面已完成，后续进入 V1.1 外部能力生态。

## 阶段总览

| 阶段 | 状态 | 当前结论 |
| --- | --- | --- |
| 1. 架构和方案文档 | 基本完成 | 中文详案、英文入口和官网已能支撑第一轮实现。 |
| 2. 模块划分 | 基本完成 | Rust workspace 和主要 crate 边界已落地，后续只应按验证需要微调。 |
| 3. 契约定义 | 基本完成 | `eva-core`、`eva-config`、`eva-policy`、Lua host/capability/storage/eventbus/scheduler/agent 的 V0.4 契约已可编译、可测试。 |
| 4. 最小可运行骨架 | 完成 | `eva doctor`、`eva config validate`、`eva inspect` 和 `eva run --example basic` 已形成真实命令面。 |
| 5. 最小端到端 Runtime 闭环 | 完成 | `examples/basic/` 与 runtime/CLI 测试已经证明最小闭环可运行。 |
| 6. 逐模块实现 | 进行中 | V0.5 已补任务状态、日志、取消、超时、重试、dead-letter replay 和 generation marker。 |
| 7. 集成、加固和发布准备 | 完成 | V1.0 quickstart、CI、release notes、已知限制和安装验证已落地。 |

## 进度阶段

### 1. 架构和方案文档

目的：

- 明确产品目标、非目标、系统边界和核心假设。
- 描述运行时模型、扩展模型、记忆模型、发现模型、硬件接入、配置系统和恢复策略。
- 在实现前记录设计风险。

已落地：

- 总体架构、模块划分、Runtime/EventBus/Scheduler/Agent/Adapter/Memory/Discovery/Config/
  Hardware、热更新、备份迁移、进程级升级、CLI 命令面等中文详案。
- 英文默认公开入口和摘要型英文文档。
- 官网静态站点、文档索引、博客内容和 i18n manifest。
- `docs/zh-CN/中文文档入口.md` 中的推荐阅读顺序和内容权威说明。

仍需维护：

- 当实现偏离方案时，先更新对应中文详案，再同步英文摘要或全文。
- 风险评审需要随着最小闭环实现继续更新，不能停留在设计期假设。

### 2. 模块划分

目的：

- 把架构转换成具体源码结构。
- 用目录、crate 和模块名称表达职责边界。
- 在不急着写完整逻辑的前提下，让系统形态先落地。

已落地：

- 根目录 `Cargo.toml` 已定义 Rust workspace。
- 主要 crate 边界已经存在：
  - `eva-core`
  - `eva-config`
  - `eva-policy`
  - `eva-observability`
  - `eva-storage`
  - `eva-eventbus`
  - `eva-scheduler`
  - `eva-agent`
  - `eva-lua-host`
  - `eva-capability`
  - `eva-adapter`
  - `eva-mcp`
  - `eva-discovery`
  - `eva-memory`
  - `eva-hardware`
  - `eva-backup`
  - `eva-lifecycle`
  - `eva-runtime`
  - `eva-cli`
- 根入口 `src/main.rs` 已通过 `eva_cli::run()` 连接到 CLI crate。

仍需收敛：

- `examples/basic/` 尚未成为第一条可运行链路。
- 部分 crate 目前只有职责常量或占位模块，不能被视为行为完成。
- 后续不应继续扩张 crate 数量，除非有清晰的编译边界、依赖方向或测试收益。

### 3. 契约定义

目的：

- 在填充业务逻辑前，先定义接口和数据契约。
- 让模块边界变得可测试、可评审。
- 避免运行时代码硬编码尚未稳定的文档假设。

已落地：

- `eva-core` 已定义并导出基础契约：
  - Topic 与 TopicPattern。
  - Event、EventPayload、EventTarget、TraceContext、EventMetadata。
  - AgentId、AdapterId、CapabilityId、EventId、GenerationId、RequestId 等 ID newtype。
  - CapabilityName、CapabilityRef、ProviderHint。
  - InvokeRequest、InvokeResponse、InvokeInput、InvokeOutput、InvokeStatus。
  - EvaError、ErrorKind、ProviderCode、ErrorContext。
- `config/schemas/` 已有第一批 JSON Schema 文件：
  - `agent.schema.json`
  - `adapter.schema.json`
  - `capability.schema.json`
  - `eva.schema.json`
  - `policy.schema.json`
  - `routes.schema.json`
- `crates/eva-core` 已有针对 Topic、Event、Invoke、ID 和 Error 的单元测试。

下一步必须补齐：

- `AgentManifest`、`AdapterManifest`、`CapabilityManifest` 的 Rust 类型、反序列化和 schema 对齐测试。
- MCP、hardware、sandbox、adapter、workspace policy 的有效配置类型和合并规则。
- EventBus trait、Subscription、DeadLetter、RecoverableEventLog 的最小可测试契约。
- Scheduler route rule、match result、queue target 和冲突处理契约。
- Agent lifecycle、mailbox、runtime state 和隔离执行边界。
- Lua host API 契约：
  - `ctx.tools`
  - `ctx.host`
  - `ctx.memory`
  - `ctx.global_memory`
  - `ctx.knowledge`
- Capability 命名、provider 选择、冲突处理和审计字段。

完成标准：

- 每个公开契约都有编译期测试、单元测试或 schema 校验测试。
- 文档中的字段名、Rust 类型和 JSON Schema 不出现三套命名。
- 占位 `RESPONSIBILITY` 常量不再被误认为已实现行为。

### 4. 最小可运行骨架

目的：

- 证明仓库可以构建和启动。
- 在深入实现 Runtime 之前，先建立开发闭环。

已落地：

- 根二进制入口已存在。
- `eva-cli` crate 已承接用户入口职责。
- `eva-runtime` crate 已作为组合根边界存在。
- `eva doctor`、`eva config validate`、`eva inspect` 已形成稳定文本/JSON 输出和 exit code。
- `eva run --example basic` 已进入 V0.5 in-memory runtime 闭环，并输出 task report 与 generation marker。
- Runtime builder 同时保留 V0.3 no-op summary、V0.4 in-memory basic mode 和 V0.5 task diagnostics mode。
- trace、audit、结构化错误和 JSON envelope 已在 CLI/runtime 路径中可观察。
- `.github/workflows/pages.yml` 已覆盖网站发布链路。

后续补齐：

- CI 中的 Rust format、lint、test 和 schema validation 要纳入发布门禁。
- V0.5 需要在现有闭环上加入 task status、logs、cancel、timeout、retry 和 hot reload 雏形。
- V1.0 前需要把 quickstart、安装说明、release notes 和已知限制与真实命令保持一致。

完成标准：

- 从干净 checkout 可以构建 CLI。
- `eva doctor` 可以报告本地环境、配置路径、schema 状态和已发现能力数量。
- `eva config validate` 可以校验最小项目配置、manifest 和 policy。
- `eva run --example basic` 可以启动最小 runtime loop。
- Rust 测试和文档/网站校验在 CI 中运行。

### 5. 最小端到端 Runtime 闭环

目的：

- 把骨架变成最小真实 Eva-CLI 行为。
- 在扩展平台能力前，先用一条很窄的可运行路径验证核心架构。

目标闭环：

1. 用户输入进入 CLI/Ingress。
2. Ingress 发布 typed Topic event。
3. EventBus 接收并持有事件。
4. Scheduler 按 TopicPattern 把事件路由到一个 Agent queue。
5. AgentRuntime 从 queue 中取出事件。
6. Lua Agent 在隔离状态中处理事件。
7. Lua Agent 调用一个受控 Rust capability/tool。
8. Rust 在执行前校验 schema 和 policy。
9. Tool 结果以结构化值返回 Lua。
10. Runtime 输出 trace 和 audit 数据。
11. 失败时返回结构化、带 retry 语义的错误。

完成标准：

- `examples/basic/` 可以运行。
- 一个 Lua Agent 可以处理一个事件。
- 一个受控 tool 调用可以通过 Rust 校验并执行。
- trace、audit 和结构化错误可观察。
- 回归测试覆盖完整闭环。
- 文档中的 quickstart 与真实命令一致。

### 6. 逐个模块补齐具体实现

目的：

- 在最小闭环验证架构后，再按模块补齐真实行为。
- 每个模块实现都应基于契约和测试。

推荐顺序：

1. Config 和 schema 校验。
2. EventBus。
3. Scheduler。
4. Agent mailbox 与 lifecycle。
5. Lua runtime 和 host bindings。
6. Capability/tool layer 和 AdapterRegistry。
7. Error model、trace 和 audit。
8. MemoryService 和 ContextBuilder。
9. Knowledge 访问。
10. Discovery。
11. Hot reload 和 generation switching。
12. MCP 和 Skill adapter。
13. HardwareAdapter。
14. Backup、restore、migration package 和 ReleaseSnapshot 支持。
15. Process lifecycle、draining、rollback 和 supervisor 集成。

建议：

- 一次只实现一个模块切片。
- 优先删除和简化契约，而不是继续增加层级。
- 每个模块需要有聚焦测试，才能视为完成。
- 跨模块行为应通过 examples 和集成测试验证，不能只靠单元测试。
- 共享类型必须优先沉到 `eva-core` 或对应边界 crate，避免 runtime 反向定义底层契约。

### 7. 集成、加固和发布准备

目的：

- 从内部可运行推进到可发布 CLI。

预期交付物：

- 已支持场景的可运行 examples。
- 面向用户的 quickstart。
- 安装说明。
- 覆盖 format、lint、unit test、integration test、schema validation 和 website build 的 CI。
- macOS、Linux、Windows 跨平台检查。
- 针对 sandbox、policy、secret、文件写入、MCP 和硬件访问的安全评审。
- 版本化 release notes。
- 破坏性变更迁移说明。

完成标准：

- 新用户可以安装 Eva-CLI，跑通 quickstart，并诊断常见环境问题。
- 文档示例和真实行为一致。
- 已知限制被明确写出。
- 不支持的高级能力在 CLI 输出、文档和 release notes 中都明确标记。

## 1.0 发布标准

Eva-CLI 1.0 不代表所有设想都已经实现，而是代表核心承诺已经稳定。

1.0 必须满足：

- CLI 安装和启动可靠。
- `eva doctor`、`eva config validate` 和最小 `eva run` 链路稳定。
- 最小 Agent runtime 闭环稳定。
- Manifest、policy 和 Lua host API 契约已版本化。
- Lua Agent 可以通过 Rust 校验调用受控 tool/capability。
- 结构化错误、trace 和 audit 可用。
- Config validation 能尽早拦截不支持或不安全的配置。
- EventBus、Scheduler、AgentRuntime、AdapterRegistry 和 Runtime builder 的最小行为有集成测试。
- 文档、示例和 release artifact 与实现一致。
- 破坏性变更有迁移说明。
- 尚不支持的高级能力被明确标记。

1.0 不强制包含：

- 完整 MCP 双向生态。
- 全量硬件接入。
- 完整备份/迁移/ReleaseSnapshot 自动化。
- 复杂多 Agent 策略优化。
- IDE 插件和移动端入口。

这些能力可以进入 1.x 路线，但不能阻塞核心 runtime 闭环的稳定发布。

## 当前最近里程碑

V0.5 最近里程碑已经完成：

1. manifest、policy 和 routes schema 的 Rust 类型与校验测试已落地。
2. `eva config validate` 已实现最小配置加载、schema 路径检查和跨文件校验。
3. `eva doctor` 已输出本地环境、配置路径、schema、runtime boundary 和 Lua host 状态。
4. EventBus、Scheduler、Storage、AgentRuntime、Lua host 和 Capability router 已有可测试 in-memory 实现。
5. `examples/basic/` 已覆盖“事件进入、路由、Agent 处理、受控 capability 返回结果”的闭环。
6. `eva run --example basic --output json` 已输出 V0.5 结构化运行报告。
7. `eva task status`、`eva task logs`、`eva task cancel` 已可读取 `.eva/tasks` 本地 task report。
8. AgentRuntime 已记录 timeout、cancel、retry attempts，EventBus 已提供 dead-letter replay 诊断。
9. Lua generation marker 已进入 runtime report。
10. 该闭环已加入 runtime 和 CLI 单元测试，并通过 workspace 测试。

V1.0 最近里程碑已经完成：

1. 固定 quickstart，覆盖 version、doctor、config validate、inspect、run、task status/logs、timeout replay 和 cancel 示例。
2. 补安装说明、CI/release 门禁、release notes 和已知限制。
3. 把 controlled Lua contract、in-memory task report、非 durable recovery 等限制写入用户文档。
4. 在不扩展 V1.1 外部 provider 的前提下，完成核心 CLI 发布验收。

下一最近里程碑进入 V1.1：Adapter、MCP、Skill 和 Discovery 的受控外部能力生态。

这样的节奏能把项目从“失败路径可诊断、可取消、可审计”推进到“核心 CLI 可发布”，同时避免在 V1.1 之前提前扩展外部 provider 能力。
