> Language: 简体中文
> Content authority: V1.x real runtime implementation plan
> English default entry: [../../en/planning/v1.x-real-runtime-implementation-plan.md](../../en/planning/v1.x-real-runtime-implementation-plan.md)
> Translation status: current

# V1.x 真实运行时能力补齐实施计划

更新时间：2026-07-10

本文基于 [V1.x 未完整实现功能清单](V1.x未完整实现功能清单.md)，把 V1.5.1 之后仍停留在 contract、diagnostic、plan-first、in-memory、simulated 或 envelope 路径的能力拆成可执行的小版本计划。目标不是重新定义架构，而是把“未完整实现”逐步升级为可审计、可回滚、可测试的真实执行路径。

当前基线：

- V1.11.5-alpha 是当前实现基线；V1.6-V1.11 已完成 durable runtime、Lua VM、Adapter/MCP/Skill 受控执行、policy/discovery/memory/observability 基线、hardware/apply gate、release evidence gate 和 CLI runtime command 补齐。
- `eva-cli` 命令面稳定，JSON envelope 和 exit code 是兼容性承诺；后续真实副作用只能通过新增字段、显式确认和 release gate 扩展。
- 剩余缺口集中在常驻 daemon、provider supervision、完整 MCP、destructive restore file mutation、生产 OS service manager、真实硬件 I/O、后台 memory/knowledge 服务、生产观测后端、生产发布上传和 CLI operator UX。
- 本计划中的版本号是实施里程碑建议；真正发布时仍需遵守 [版本管理方案](../release/版本管理方案.md)。

## 实施约束

| 约束 | 要求 |
| --- | --- |
| 先测试后扩大副作用 | 每次把 plan/envelope/dry-run 升级为真实执行前，先补失败用例、拒绝用例和回归用例。 |
| 保持 plan-first | restore、upgrade、hardware bind、release pointer、权限扩大等高风险路径必须先输出计划和风险。 |
| 不绕过边界 | Lua、Agent、Discovery 不能直接拿 provider、设备、文件、socket 或进程句柄。 |
| 保持兼容 | V1.0-V1.5 已公开命令、JSON 字段、exit code 默认不破坏；新增字段必须可选。 |
| 小步交付 | 每个小版本只包含 3-5 个小功能迭代；每个迭代必须有前置项、实现逻辑和验收口径。 |
| 文档同步 | 任一真实路径落地后，同步本计划、未完整实现清单、使用手册、release notes 和 i18n manifest。 |

## 版本总览

| 版本 | 主题 | 小功能迭代数 | 目标结果 | 前置版本 |
| --- | --- | ---: | --- | --- |
| V1.6 | Durable runtime/storage | 5 | EventBus、task、audit、artifact 从 in-memory/local diagnostic 升级为可恢复持久路径 | V1.5.1 |
| V1.7 | 真实 Lua VM 与热更新 | 4 | Lua Agent 能执行真实脚本逻辑，并具备资源限制、host API 和 generation swap/rollback | V1.6 |
| V1.8 | 外部 provider、MCP 与 Skill runner | 5 | Adapter/MCP/Skill 从 envelope/probe 升级为受控真实进程或协议执行 | V1.6、V1.7 |
| V1.9 | Policy、Discovery、Memory、Observability 深化 | 5 | 配置、权限、发现、记忆和观测进入可持久、可审计、可门禁状态 | V1.6-V1.8 |
| V1.10 | 硬件真实接入与高风险 apply | 5 | 硬件、restore、upgrade 从计划路径进入可确认、可回滚真实执行 | V1.6、V1.9 |
| V1.11 | 分发链路与 CLI 收敛 | 5 | 签名制品、安装器、外部扫描和 CLI 模块拆分形成发布闭环 | V1.10 |
| V1.12 | 常驻 runtime daemon 与 scheduler apply | 6 | 把 basic runtime、task recovery、agent drain/reload evidence 升级为可启动、可控制、可恢复的常驻 daemon | V1.11.5 |
| V1.13 | Provider supervision 与完整 MCP | 8 | 外部 provider、MCP 和 Skill runner 进入受监督进程、会话隔离、限流、streaming 和恢复路径 | V1.12 |
| V1.14 | Destructive restore 与生产 service-manager handoff | 8 | restore 从 gated report 进入真实文件 mutation；upgrade 从本地 state-store 进入 OS service manager blue-green handoff | V1.12、V1.13 |
| V1.15 | 真实硬件 I/O 与后台 Memory/Knowledge 服务 | 8 | 接入真实 driver/OS 权限/hotplug subscriber，并把 memory/knowledge 做成后台服务 | V1.12、V1.13 |
| V1.16 | 生产观测后端与发布上传链路 | 7 | 完整 runtime/provider/task 观测 wiring、OpenTelemetry/metrics exporter、生产签名和包仓库发布 | V1.12-V1.15 |
| V1.17 | CLI 剩余收敛与 V1.x release closure | 6 | 收敛剩余 dispatcher、强化 operator UX、固定 JSON contract diff 和最终文档/release gate | V1.16 |

## V1.12+ 剩余能力实施计划

本节是当前未完整实现清单的直接执行版本。V1.6-V1.11 的完成记录保留在后文，V1.12 起只覆盖仍未进入完整真实执行路径的功能。

状态口径：

| 状态 | 含义 |
| --- | --- |
| Planned | 已拆成可执行逻辑修改，但尚未开始。 |
| Blocked | 不能安全开工，必须先满足前置项或外部条件。 |
| In Progress | 已开始实现，尚未满足退出标准。 |
| Done | 已实现、已验证，并已同步清单/文档。 |

### V1.12 常驻 Runtime Daemon 与 Scheduler Apply

目标：把 `run --example basic`、durable recovery、agent lifecycle evidence 和 generation drain/reload 连接成可控制的本机 daemon。V1.12 不扩大 provider/hardware/restore 的真实副作用，只解决 runtime 进程、控制面、长任务和 scheduler apply。

| 节点 | 逻辑修改 | 前置项 | 具体实施内容 | 主要模块 | 验证/退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.12.1 | Daemon 配置和进程边界 | V1.11.5 release baseline；durable backend 可打开 | 新增 `eva daemon start/status/stop` 的本机 daemon mode 边界、pid/lock/state 目录、foreground/dev smoke 和 shutdown contract；启动时验证 durable backend、policy、observability sink，并保持 provider 进程不启动。 | `eva-runtime`、`eva-cli` | `cargo test -p eva-runtime daemon`；`cargo test -p eva-cli daemon_`；`cargo test -p eva-cli`；`cargo run -- daemon start --foreground --dev ... --output json`；重复启动返回 lock conflict；坏 backend 不创建 daemon state。 | Done |
| V1.12.2 | Runtime control API/IPC | V1.12.1 | 新增 V1.12.2 本机 filesystem mailbox 控制面；显式 `--no-shutdown-after-smoke` 的前台 daemon 处理 status、shutdown、submit task、cancel task、drain、reload plan；CLI control 请求不暴露远程网络监听。 | `eva-runtime`、`eva-cli`、`eva-agent` | `cargo test -p eva-runtime daemon -- --nocapture`；`cargo test -p eva-cli daemon_ -- --nocapture`；`eva daemon status` 通过 mailbox 查询 running daemon；无 daemon 时稳定返回 `unavailable`；IPC request/response 含 `trace_id`、`request_file`、`response_file`。 | Done |
| V1.12.3 | 长任务状态机 | V1.12.1；V1.6 task store | `TaskStateSnapshot` 增加 durable task lifecycle 字段和迁移 API，覆盖 queued/running/cancelling/timed_out/completed/failed/interrupted/recovering、heartbeat、deadline、cancel token 和 task log append；daemon submit/cancel 接入该状态机；AgentRunControl 透传 cancel token/deadline evidence。 | `eva-runtime`、`eva-agent`、`eva-storage` | `cargo test -p eva-storage task_state -- --nocapture`；`cargo test -p eva-agent cancel -- --nocapture`；`cargo test -p eva-runtime recovery -- --nocapture`；`cargo test -p eva-cli daemon_control -- --nocapture`；长任务 cancel 进入 `cancelling`；重启后 non-terminal task 恢复为 recovering/interrupted；task logs 可追加读取。 | Done |
| V1.12.4 | Scheduler backoff 和 retry dispatch | V1.12.3；durable EventBus backoff 字段已存在 | 新增 daemon live scheduler retry tick；按 `next_attempt_after_ms` 读取 due dead-letter，生成 replay event，投递 scheduler mailbox，并用 `scheduler-retry` consumer 对 replay event 写 durable log ack/fail；recovery/diagnostics 复用 replay 状态查询，避免 checkpoint 与 live tick 重复 redrive。 | `eva-scheduler`、`eva-eventbus`、`eva-runtime` | `cargo test -p eva-eventbus due_dead_letters -- --nocapture`；`cargo test -p eva-scheduler retry_dispatch -- --nocapture`；`cargo test -p eva-runtime scheduler_retry -- --nocapture`；daemon foreground retry tick smoke；backoff 未到期不投递；到期只投递一次；重启前后 retry 语义一致。 | Done |
| V1.12.5 | Agent drain/reload 真实 mutation | V1.12.2、V1.12.4；V1.7.4 generation route gate | `agent drain/reload` 在连接 daemon 时执行 scheduler generation gate 和 drain state mutation；无 daemon 时保留当前 `mutation_executed:false` evidence path。 | `eva-cli`、`eva-runtime`、`eva-agent`、`eva-scheduler`、`eva-lifecycle` | 有 daemon 时 `mutation_executed:true` 且新任务不进入旧 generation；无 daemon 时 JSON contract 不变。 | Done |
| V1.12.6 | Daemon recovery/release gate | V1.12.1-V1.12.5 | `release check` 增加 daemon readiness gate；CI 增加 foreground daemon smoke、crash/restart recovery、drain/reload apply smoke；文档同步 daemon 边界和非目标。 | `eva-release`、`eva-cli`、docs | `cargo test --workspace`；daemon smoke pass；release check 输出 `REL-DAEMON-RUNTIME-001`。 | Done |

### V1.13 Provider Supervision 与完整 MCP

目标：让 Adapter/MCP/Skill 能在常驻 runtime 下以受监督、可限流、可恢复的方式执行，同时补齐 MCP HTTP/auth/streaming。V1.13 仍不默认扩大 allowlist；所有 provider 执行必须保留 manifest、policy 和 audit gate。

| 节点 | 逻辑修改 | 前置项 | 具体实施内容 | 主要模块 | 验证/退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.13.1 | Provider supervisor trait 和进程表 | V1.12.1、V1.12.2 | 新增 supervisor 抽象，记录 provider process/session id、manifest digest、start command、health、last error、restart policy；stdio/http/skill runner 统一通过 supervisor 申请执行槽位。 | `eva-adapter`、`eva-runtime`、`eva-storage` | unauthorized provider 不启动；process table 可查询；provider crash 写 audit 且释放执行槽。 | Done |
| V1.13.2 | Credential 与 session 隔离 | V1.13.1；policy domain parser 已完成 | 将 credential env/header/session token 绑定到 provider session scope；日志、artifact、error context 继续脱敏；禁止跨 provider/session 复用 secret。 | `eva-adapter`、`eva-policy`、`eva-observability` | credential redaction 回归；session 结束后 secret 不落盘；跨 provider 访问被拒绝。 | Done |
| V1.13.3 | 并发、限流和熔断 | V1.13.1 | 从 adapter manifest/policy 读取 concurrency、rate limit、circuit breaker；non-retryable error 不 fallback，retryable error 按 backoff 入队；熔断状态进入 provider health。 | `eva-adapter`、`eva-capability`、`eva-scheduler` | 并发超限返回可重试门禁；熔断期间不启动新进程；恢复窗口后可半开探测。 | Done |
| V1.13.4 | Provider streaming 数据面 | V1.13.1、V1.13.2 | 为 stdout/http/MCP stream 定义 bounded chunk、truncation、cancel 和 artifact sink；CLI JSON 只输出摘要，完整 stream 写受控 artifact。 | `eva-adapter`、`eva-storage`、`eva-cli` | 大输出不会撑爆内存；cancel 后 stream 停止；artifact path 不越权。 | Done |
| V1.13.5 | Provider 执行态恢复 | V1.12.3、V1.13.1 | daemon 重启时扫描 provider session/task/process table；不可恢复的进程标记 interrupted；可重试任务进入 scheduler backoff；保留最后安全错误和 audit chain。 | `eva-runtime`、`eva-adapter`、`eva-storage` | provider 调用中崩溃后不丢 task；不可重复副作用不自动重放；恢复报告可查。 | Done |
| V1.13.6 | MCP HTTP transport 与 auth | V1.13.1、V1.13.2；MCP stdio JSON-RPC 已完成 | 增加 HTTP transport client/server boundary、auth header/token policy、timeout/output limit/error object 映射；保持 tools allowlist 先于 RPC。 | `eva-mcp`、`eva-adapter`、`eva-policy` | fake HTTP MCP server e2e；未授权 tool 不发请求；auth 缺失返回 policy error。 | Done |
| V1.13.7 | MCP streaming 与兼容矩阵 | V1.13.4、V1.13.6 | 增加 stream lifecycle、abort/cleanup、server capability compatibility fixture；记录协议版本、transport、tool schema 支持矩阵。 | `eva-mcp`、`eva-release` | stream abort 无悬挂 session；兼容矩阵缺失阻断 release gate；server 不暴露无限代理。 | Done |
| V1.13.8 | Provider supervision release gate | V1.13.1-V1.13.7 | `release check` 增加 provider supervision gate，并复核 V1.13.7 MCP compatibility gate；README、未完整实现清单和使用手册同步 provider 执行边界。 | `eva-release`、docs | `REL-PROVIDER-SUPERVISION-001` pass；`REL-MCP-COMPAT-001` 仍 pass；doc stale scan 无旧描述。 | Done |

### V1.14 Destructive Restore 与生产 Service-Manager Handoff

目标：在 V1.10 的 gate 基线上接入真实文件 mutation 和生产服务管理器。该版本是高风险副作用版本，所有逻辑修改必须先补失败测试、锁测试和 rollback 测试。

| 节点 | 逻辑修改 | 前置项 | 具体实施内容 | 主要模块 | 验证/退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.14.1 | Restore staged mutation planner | V1.10.4；V1.12 durable daemon state | 将 restore target 拆成 staged copy/delete/replace steps，生成 mutation preview、affected paths、preflight hash 和 rollback manifest；默认只 plan。 | `eva-backup`、`eva-storage`、`eva-cli` | path traversal/symlink 越权被拒绝；preview 与 plan digest 可复算。 | Done |
| V1.14.2 | Destructive file mutation engine | V1.14.1；filesystem lock/policy/health gate 通过 | 在 explicit confirmation 后执行 staged mutation；每一步写 transaction log；失败时停止并进入 rollback-required；输出 `mutation_executed:true` 只在实际写入后出现。 | `eva-backup`、`eva-lifecycle`、`eva-cli`、`eva-release` | 临时目录集成测试；中途失败不会留下未知状态；默认 policy 仍拒绝。 | Done |
| V1.14.3 | Restore rollback apply | V1.14.2 | 基于 rollback-required transaction log 与 pre-restore archive entry 反向恢复已提交 copy/delete/replace；rollback 自身受 confirmation、policy、rollback lock、health、preflight hash 和 digest gate 控制；写 `{plan_id}.restore.rollback.txn`，成功输出 `rollback_executed:true`，失败保留二级 evidence。 | `eva-backup`、`eva-cli`、`eva-release` | mutation 失败后 rollback 可恢复；rollback 失败有二级 evidence；digest 校验通过。 | Done |
| V1.14.4 | Restore operator UX 强确认 | V1.14.2 | `restore apply --dry-run`、`restore apply` 和 `restore rollback` 的 text/JSON 输出新增 `operator_confirmation`，显式区分 `apply_allowed`、`mutation_planned`、`mutation_executed`、`rollback_required`、`rollback_executed`，并包含 confirm token、plan id、target root、affected count、不可逆风险和 next action。 | `eva-cli`、docs | JSON contract 兼容；text 输出不会让 gated report 被误读为已恢复。 | Done |
| V1.14.5 | OS service-manager abstraction | V1.10.5；V1.12 daemon lifecycle | 定义 Windows Service、systemd、launchd 的最小 adapter trait；本地测试用 fake adapter；`eva-config` 解析显式 `service_manager` 配置；`release check` 输出抽象层 readiness gate。 | `eva-lifecycle`、`eva-config`、`eva-release` | fake adapter handoff/rollback 测试；平台 adapter 未配置时继续 warning gate。 | Done |
| V1.14.6 | 平台 service-manager adapter | V1.14.5；平台 CI 或受控测试环境 | 分别实现 Windows/Linux/macOS adapter 的 install/status/start/stop/restart/handoff 操作；所有命令带 dry-run 和 audit。 | `eva-lifecycle`、CI | 三平台 smoke；权限不足返回稳定错误；不会误停非 Eva service。 | Blocked：需要平台级服务测试环境 |
| V1.14.7 | 生产 blue-green handoff | V1.14.5、V1.14.6；V1.13 provider supervisor | 将 `upgrade apply --state-store` 升级为 service-manager handoff：启动 candidate daemon、健康检查、drain old daemon、切 release pointer、失败 rollback。 | `eva-lifecycle`、`eva-runtime`、`eva-cli` | candidate health failed 不切 pointer；old daemon drain 超时 rollback；state 可恢复。 | Blocked：依赖 V1.14.6 |
| V1.14.8 | Apply/service-manager release gate | V1.14.2-V1.14.7 | `release check` 将 destructive restore 和 service-manager handoff 从 warning gate 升级为 required gate；同步 release notes、手册和未完整清单。 | `eva-release`、docs | `SEC-LIFE-001` 不再是 warning；新增 restore/service-manager required gates。 | Blocked：依赖 V1.14.7 |

### V1.15 真实硬件 I/O 与后台 Memory/Knowledge 服务

目标：在 daemon、provider supervision 和 observability 基线上开放真实硬件与后台上下文服务。硬件路径不得向 Lua/Agent/Discovery 泄漏 raw handle。

| 节点 | 逻辑修改 | 前置项 | 具体实施内容 | 主要模块 | 验证/退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.15.1 | 真实 OS permission provider | V1.10.2；V1.12 daemon config | 为 Windows/Linux/macOS 定义硬件权限探测 provider，报告用户、安全逻辑 device locator、权限来源和 remediation；权限失败先于 driver start；`hardware bind` 输出 permission evidence。 | `eva-hardware`、`eva-policy`、`eva-cli` | 权限缺失不 claim lease；错误包含 remediation；`raw_device_path_exposed:false`；CLI 不暴露 raw device path 给 Agent。 | Done |
| V1.15.2 | USB/Serial driver baseline | V1.15.1；simulator contract suite | 实现第一个真实 USB/serial line-json driver；沿用 `HardwareDriver` trait、lease、timeout、audit、output limit；真实设备默认 disabled。 | `eva-hardware`、`eva-adapter` | 有 fixture 时 e2e；无 fixture 时 simulator regression pass；driver 不返回 raw handle。 | Blocked：需要真实或虚拟串口 fixture |
| V1.15.3 | Socket/BLE/vendor SDK driver 扩展 | V1.15.2；provider/session policy | 增加 socket/BLE/vendor SDK driver kind 的最小实现或 adapter shim；每类 driver 都必须有 simulator parity test。 | `eva-hardware`、`eva-adapter` | 每类 driver 有 contract suite；SDK 错误映射稳定；未授权 driver 不启动。 | Blocked：需要设备/SDK 选择 |
| V1.15.4 | 常驻 hotplug subscriber | V1.12 daemon；V1.15.1 | daemon 启动 manifest snapshot hotplug watcher，把逻辑插拔/重连/失败状态转为 typed EventBus event；订阅方只能看到逻辑设备状态和 `raw_handles_exposed:false`，不拿 OS handle。 | `eva-hardware`、`eva-runtime`、`eva-eventbus` | 插拔/重连/失败事件可订阅；watcher 崩溃释放 lease；重启后 `hardware-hotplug.state` 与 durable EventBus 一致。 | Done |
| V1.15.5 | Hardware safety release gate | V1.15.1-V1.15.4 | `release check` 增加 required gate `REL-HARDWARE-SAFETY-001`，汇总 simulator parity、permission denial、lease cleanup、daemon hotplug smoke 证据，并明确 alpha 只接受 simulator-only evidence。 | `eva-release`、docs | 无硬件环境时 gate 可接受 simulator-only alpha；生产 release 必须有真实或虚拟硬件 fixture evidence。 | Done |
| V1.15.6 | Memory/Knowledge cross-process locks 与 compaction | V1.9.4；V1.12 daemon | 为 durable memory/knowledge store 增加 filesystem `index.lock`、TTL GC、knowledge rebuild checkpoint、stale started checkpoint recovery 和 `memory.maintenance` audit；daemon start 执行一次维护 smoke 并输出 `memory_maintenance` evidence。 | `eva-memory`、`eva-storage`、`eva-runtime`、`eva-cli` | 并发写/读被 `index.lock` 保护；过期数据被 GC 删除且不进入 context；stale started checkpoint 可恢复；daemon JSON 输出 memory/knowledge checkpoint evidence。 | Done |
| V1.15.7 | 检索 provider 执行 | V1.13 provider supervision；V1.15.6 | 将 external retrieval 从 gate 决策升级为受监督 provider invoke；结果进入 knowledge context 前执行 schema、redaction 和 source audit。 | `eva-memory`、`eva-capability`、`eva-adapter` | provider 超时/失败不污染 index；敏感结果被脱敏；source trace 可查。 | Done |
| V1.15.8 | Redaction policy 和 memory audit wiring | V1.15.6、V1.16.1 | 从 policy/config 读取 redaction rules；memory read/write/search/context 注入 audit/metrics；Lua context 继续只读且脱敏。 | `eva-memory`、`eva-policy`、`eva-observability` | redaction rule fixture；audit 关联 request/agent；Lua snapshot 无敏感字段。 | Done |

### V1.16 生产观测后端与发布上传链路

目标：把 JSONL 基线升级为生产可接入的 trace/metrics 后端，并把 release evidence gate 连接到真实签名、attestation 和包仓库上传。

| 节点 | 逻辑修改 | 前置项 | 具体实施内容 | 主要模块 | 验证/退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.16.1 | Runtime/provider/task audit sink 注入 | V1.12、V1.13；V1.9.5 file backend | 把 daemon recovery/control/task lifecycle、scheduler retry、provider supervisor 和 restore apply/rollback 写入 `BestEffortObservabilityPipeline`；沿用既有 hardware lifecycle audit action，并保留 best-effort 降级。 | `eva-runtime`、`eva-adapter`、`eva-cli`、`eva-observability` | trace continuity 覆盖 CLI->daemon->provider/task；sink 失败不破坏主流程；JSONL audit/metrics/span 可查。 | Done |
| V1.16.2 | tracing subscriber bridge | V1.16.1 | 接入 `tracing` subscriber，将 span/event 映射到现有 TraceFields/AuditEvent；避免重复 span id；支持 JSONL/dev console 两种 sink。 | `eva-observability`、`eva-cli` | span 层级稳定；dev console 不泄漏 secret；JSONL schema 兼容 V1.9.5。 | Done |
| V1.16.3 | OpenTelemetry SDK exporter 与 metrics exporter | V1.16.2 | 增加 OTel exporter config、endpoint/auth、batch/timeout/drop policy；metrics exporter 输出 runtime/provider/task labels。 | `eva-observability`、`eva-config` | fake collector e2e；collector 不可用时 degraded；metrics label cardinality 有上限。 | Done |
| V1.16.4 | Retention/rotation/db sink policy | V1.16.1 | 为 JSONL/file/db sink 增加 retention、rotation、max size、tamper/corrupt handling；删除只影响过期观测数据。 | `eva-observability`、`eva-storage` | rotation 后可继续写；corrupt record 可跳过并报警；retention 不删未过期数据。 | Planned |
| V1.16.5 | 生产 signing/attestation credential provider | V1.11.1 artifact evidence；release workflow | 定义 signing service/attestation provider 接口，支持本地 dev key、CI OIDC、KMS/HSM placeholder；release evidence 记录 credential issuer 和 attestation digest。 | `eva-release`、CI | dev key 只能用于 alpha；生产 release 缺 attestation 阻断；secret 不进入日志。 | Blocked：需要生产签名/attestation 凭据策略 |
| V1.16.6 | Homebrew/Winget/Apt 仓库发布 dry-run | V1.11.2 distribution evidence；V1.16.5 credential provider | 为三类包仓库生成 manifest/formula/spec dry-run，校验版本、digest、签名、安装 smoke；不上传时也能生成 evidence。 | `eva-release`、CI、docs | dry-run evidence 可被 `release check` 读取；digest mismatch 阻断。 | Blocked：依赖 V1.16.5 凭据策略 |
| V1.16.7 | 生产上传 pipeline | V1.16.5、V1.16.6；仓库权限 | release workflow 执行 GitHub Release、GHCR、Homebrew/Winget/Apt 上传，上传后复查下载 digest 和安装 smoke；失败自动标记 release incomplete。 | CI/release scripts、`eva-release` | 上传后 evidence 完整；失败不会宣称 release complete；可重试不重复污染仓库。 | Blocked：需要仓库发布凭据 |

### V1.17 CLI 剩余收敛与 V1.x Release Closure

目标：清理剩余 CLI 维护风险，修正 operator 语义风险，并把 V1.x 真实执行路径收束为可发布、可验证、可维护的 release closure。

| 节点 | 逻辑修改 | 前置项 | 具体实施内容 | 主要模块 | 验证/退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.17.1 | `run --example basic` dispatcher split | V1.11.4；V1.12 daemon command shape 固定 | 将剩余 run example/basic parser、writer、task store glue 从 root dispatcher 拆出；保留 public JSON/text 输出。 | `eva-cli` | `run_basic_example_json_succeeds` 通过；root dispatcher 只保留路由。 | Planned |
| V1.17.2 | `invocation_executed` / `mutation_executed` operator 字段 | V1.13 provider call；V1.14 restore mutation | capability/provider 调用输出 `invocation_executed`；restore/upgrade/hardware 输出 `mutation_executed`；旧字段保留兼容但文档解释清楚。 | `eva-cli`、docs | capability confirmed invoke 不再被误读；JSON contract diff 允许新增字段。 | Planned |
| V1.17.3 | 高风险 apply 交互文案 | V1.14.2、V1.14.7 | restore/upgrade/hardware/release upload 的 text 输出统一显示 plan id、confirm token、target、risk、rollback path 和 final state。 | `eva-cli`、docs | 无确认时不执行；确认不匹配返回 usage/policy error；文案不遮蔽关键字段。 | Planned |
| V1.17.4 | Public JSON contract diff suite | V1.17.1-V1.17.3 | 固定关键命令 golden JSON 样本，增加兼容性 diff 工具；新增字段必须可选，删除/重命名字段阻断。 | `eva-cli`、`eva-release`、scripts | CI 中 contract diff pass；破坏性 JSON 改动失败。 | Planned |
| V1.17.5 | 文档/i18n/release notes 同步闭环 | V1.12-V1.16 对应功能完成 | 更新未完整实现清单、实施计划、使用手册、release notes、README、网站卡片和 i18n manifest；英文继续作为摘要入口。 | docs、scripts | `scripts/validate-i18n.ps1`；`scripts/build-site-i18n.ps1`；无 stale status 文案。 | Planned |
| V1.17.6 | V1.x 完整真实执行 release gate | V1.17.1-V1.17.5 | `release check` 汇总 daemon/provider/MCP/restore/service-manager/hardware/observability/signing/distribution gates；生成 V1.x closure report。 | `eva-release`、CI | `cargo clippy --workspace --all-targets -- -D warnings`；`cargo test --workspace`；closure report 无 blocking gate。 | Planned |

### 当前剩余功能到版本映射

| 未完整实现/风险项 | 对应版本 | 解除条件 | 当前状态 |
| --- | --- | --- | --- |
| 常驻 runtime daemon、长任务和 scheduler drain mutation | V1.12 | daemon 可启动/控制/恢复，agent drain/reload 可真实修改 live scheduler | Planned |
| Provider supervision、capability call 生产监督 | V1.13 | provider process/session/concurrency/streaming/recovery 的受控 release gate 已完成；OS process supervisor、OS credential vault 和真实用户隔离仍待后续补 | Done（受控基线；OS 级生产监督仍待补） |
| MCP streaming/compatibility/TLS | V1.13 | HTTP/auth、fixture compatibility matrix、stream lifecycle abort/cleanup evidence 和 `REL-MCP-COMPAT-001` release gate 已有受限基线；后续补 TLS client、真实外部 server matrix 和生产 streaming 数据面 | Planned（TLS/外部兼容仍待补） |
| destructive restore file mutation 和 rollback apply | V1.14 | V1.14.1 已完成 staged mutation planner；V1.14.2 已在现有 gate 后执行 staged file mutation 并写 transaction log；V1.14.3 已基于 rollback-required transaction log 与 pre-restore archive entry 执行反向文件恢复；V1.14.4 已补 operator confirmation 输出 | Partially Done（V1.14.1-V1.14.4 Done；生产 service-manager 仍待后续） |
| 生产 OS service manager handoff | V1.14 | V1.14.5 已完成 service-manager 抽象、fake adapter 和显式 config；三平台 service-manager adapter 与 blue-green handoff 验证仍待平台环境 | Blocked：需要平台服务测试环境 |
| 真实硬件 raw I/O | V1.15 | V1.15.1 已完成 OS permission provider 诊断和 pre-claim 阻断；V1.15.4 已完成 daemon manifest snapshot hotplug subscriber、typed durable EventBus event 和重启一致性 state；V1.15.5 已完成 alpha simulator-only hardware safety release gate；driver fixture 和真实 OS hotplug watcher 仍待补齐 | Blocked：需要硬件/虚拟设备 fixture |
| 后台 Memory/Knowledge 服务 | V1.15 | V1.15.6 已完成 filesystem index lock、TTL GC、knowledge rebuild checkpoint 和 daemon maintenance smoke；V1.15.7 已完成受监督 retrieval provider invoke、schema、redaction 和 source audit；V1.15.8 已完成 policy-driven redaction rules、memory read/write/search/context audit/metrics wiring 和 CLI `memory context` JSONL evidence；V1.16.1 已解除 runtime/provider/restore 观测 pipeline 前置，长驻 scheduler 和生产检索调度仍待后续 | Partially Done（V1.15.6/V1.15.7/V1.15.8 Done；长驻 scheduler 待后续） |
| 生产 OpenTelemetry/metrics 后端 | V1.16 | V1.16.1 已完成 runtime/provider/task/restore audit sink wiring；V1.16.2 已完成 tracing subscriber bridge、span/event 到 TraceFields/AuditEvent 映射、JSONL/dev-console sink 和脱敏；V1.16.3 已完成 OpenTelemetry SDK OTLP HTTP/protobuf exporter、metrics exporter smoke、endpoint/auth/batch/timeout/drop policy 和 metrics label 上限；retention/rotation/db sink 仍待 V1.16.4 | Partially Done（V1.16.1/V1.16.2/V1.16.3 Done；V1.16.4 Planned） |
| 生产 signing/attestation 和包仓库上传 | V1.16 | 生产凭据、仓库发布 dry-run、上传后验证和 provenance evidence 完成 | Blocked：需要生产凭据/仓库权限 |
| CLI operator 语义和剩余 dispatcher | V1.17 | `invocation_executed`/`mutation_executed` 区分清楚，root dispatcher 收敛，contract diff 固定 | Planned |

## V1.6 Durable Runtime/Storage

目标：先补完整 runtime 的地基，让事件、任务、审计和 artifact 在进程重启后可查询、可恢复、可 redrive。

| 迭代 | 小功能 | 前置项 | 具体逻辑实现 | 主要模块 | 验收与退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.6.1 | Durable backend 契约和迁移基线 | `EventLog`、`StateStore`、filesystem artifact trait 已存在；确认不破坏 in-memory 测试 | 新增 durable backend trait extension；定义 task/event/audit/artifact 表或文件布局；实现 schema version、migration lock、open/create/verify 流程；保留 in-memory backend 作为测试实现 | `eva-storage`、`eva-config` | backend 打开、迁移、版本不匹配、只读模式测试通过；`cargo test -p eva-storage` 通过 | Done |
| V1.6.2 | Durable EventBus 与 dead-letter redrive | V1.6.1 完成；现有 publish/ack/fail/dead-letter 语义固定 | 将 publish 写入 durable log；ack/fail 更新 delivery 状态；dead-letter 写入可查询队列；redrive 读取 dead-letter 并生成新 delivery attempt；实现 retry delay/backoff 字段但先保持默认策略 | `eva-eventbus`、`eva-storage` | durable publish/ack/fail/redrive round trip；重启后 dead-letter 仍可查询；backoff 字段序列化兼容 | Done |
| V1.6.3 | Durable task/audit/artifact store | V1.6.1 完成；`TaskReport` 和 `AuditEvent` 字段冻结 | 将 `.eva/tasks` 本地诊断升级为 task store adapter；task status/logs/cancel 读取 durable store；audit sink 写入 durable audit；artifact store 增加 checksum、size、content type 和 retention metadata | `eva-storage`、`eva-cli`、`eva-observability` | `task status/logs/cancel` 在重启后可读；audit 查询包含 trace id；损坏 artifact 返回稳定错误 | Done |
| V1.6.4 | Runtime crash recovery coordinator | V1.6.2、V1.6.3 完成；basic runtime 任务状态语义固定 | Runtime 启动时扫描 pending/running task；把未完成任务标记为 `recovering` 或 `interrupted`；恢复可 redrive 的 event；生成 recovery audit；避免重复执行已经 ack 的 event | `eva-runtime`、`eva-agent`、`eva-eventbus`、`eva-storage` | 模拟进程中断后重启；已 ack 不重复执行；可恢复任务进入 expected 状态；recovery audit 可查 | Done |
| V1.6.5 | Durable 诊断命令和 smoke gate | V1.6.1-V1.6.4 完成；JSON envelope 保持兼容 | 在现有 `doctor`/`inspect` 或新增受控子命令中报告 durable backend path、schema version、migration status、pending redrive count；CI 增加 durable smoke | `eva-cli`、`eva-runtime` | JSON 输出字段可选且稳定；durable smoke 覆盖 clean start、restart、corrupt store；`cargo test --workspace` 通过 | Done |

### V1.6.3 细分进度表

| 节点 | 日期 | 功能修改 | 具体变更 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.6.3.1 | 2026-07-07 | Durable task store adapter | `FileSystemTaskStateStore` 新增 `from_durable_layout`，可直接使用 V1.6.1 backend manifest 中的 `tasks/` 目录；`eva run --example basic`、`eva task status/logs/cancel` 新增 `--durable-backend <path>`，未传参时继续使用兼容的 `.eva/tasks`。 | `cargo fmt --check`; `cargo test -p eva-storage task_state`; `cargo test -p eva-cli task_commands_can_use_durable_backend_task_store` | Done |
| V1.6.3.2 | 2026-07-07 | Durable audit sink | 新增 `FileSystemAuditSink` 和 `AuditRecord`，实现 `AuditSink`；审计记录写入 durable backend 的 `audit/` 目录，保存 action、outcome、trace entries、message、fields 和毫秒时间戳；提供 `query_by_trace_id`，可按 span/request/event/correlation/causation id 查询。 | `cargo fmt`; `cargo test -p eva-storage audit` | Done |
| V1.6.3.3 | 2026-07-07 | Artifact metadata hardening | `ArtifactRecord` 新增 `size_bytes`、`content_type`、`retention_policy` 和 `retain_until_ms`；`FileSystemArtifactStore` 写入 v2 metadata，同时兼容无 `version`/`version=1` 的 legacy metadata；读取时继续校验 key、checksum 和 size，content type/retention metadata 损坏时返回稳定 `Conflict`。 | `cargo fmt --check`; `cargo test -p eva-storage artifact_store`; `cargo test -p eva-backup`; tamper/legacy/corrupt metadata 测试 | Done |

### V1.6.4 细分进度表
| 节点 | 日期 | 功能修改 | 具体变更 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.6.4.1 | 2026-07-07 | Task recovery scanner | `FileSystemTaskStateStore` 新增 `list_snapshots()`，启动恢复逻辑可枚举 durable task snapshots 且跳过 `latest-basic.task` 重复记录；`eva-runtime` 新增 `RuntimeRecoveryCoordinator`，把重启后残留的 `queued`/`running` task 标记为 `interrupted`，带 dead-letter 证据的 task 标记为 `recovering`，并把状态写回 task store。 | `cargo test -p eva-storage task_state`; `cargo test -p eva-runtime recovery` | Done |
| V1.6.4.2 | 2026-07-07 | Event redrive recovery checkpoint | `DurableEventBus` 新增单事件 `redrive_dead_letter()`，`FileSystemDeadLetterStore` 支持按 event id redrive 和更新 `RedrivePolicy`；`RuntimeRecoveryCoordinator::recover_task_store_with_redrive()` 读取 durable dead-letter 与 event log 证据，只 redrive 未 ack 且 `next_attempt_after_ms` 已到期的 event，已 ack、未到期、缺失证据或非法 event id 会进入 skipped 证据；redrive 成功后写回 task snapshot 的 `replayed_events` 和恢复日志。 | `cargo test -p eva-eventbus durable`; `cargo test -p eva-runtime recovery` | Done |
| V1.6.4.3 | 2026-07-07 | Recovery audit and smoke gate | `AuditAction` 新增 `runtime.recovered`；`RuntimeRecoveryCoordinator` 新增 task-only/redrive recovery audit 入口，把 scanned/recovered/redriven/skipped 计数写入 durable `FileSystemAuditSink`；runtime smoke 覆盖 clean start、restart redrive 和 corrupt task store；`release check` 新增 `REL-DURABLE-RECOVERY-001` 门禁，CLI JSON 暴露 recovery evidence。 | `cargo test -p eva-observability audit`; `cargo test -p eva-runtime recovery`; `cargo test -p eva-release`; `cargo test -p eva-cli recovery` | Done |

### V1.6.5 细分进度表
| 节点 | 日期 | 功能修改 | 具体变更 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.6.5.1 | 2026-07-07 | Durable diagnostics data surface | `FileSystemEventLog`、`FileSystemDeadLetterStore` 和 `DurableEventBus` 新增 read-only 打开路径，诊断读取空 log/dead-letter 时不创建目录；`eva-runtime` 新增 `inspect_durable_backend()` 和 `DurableDiagnosticsReport`，报告 backend path、schema/layout version、migration status、event/dead-letter 计数和只统计已到期且未 ack 的 `pending_redrive_count`。 | `cargo fmt --check`; `cargo test -p eva-storage event_log`; `cargo test -p eva-eventbus durable`; `cargo test -p eva-runtime diagnostics` | Done |
| V1.6.5.2 | 2026-07-07 | CLI durable inspect command | `eva inspect durable --durable-backend <path>` 接入 `inspect_durable_backend()`，输出 `inspect.durable` JSON envelope 和 text 摘要；保留旧 `eva inspect` / `inspect runtime` 项目诊断路径；新增 CLI 回归测试确认诊断读取不会创建 `events/log` 或 `events/dead_letters`。 | `cargo fmt --check`; `cargo test -p eva-cli inspect_durable`; `cargo clippy -p eva-cli --all-targets -- -D warnings` | Done |
| V1.6.5.3 | 2026-07-07 | Durable diagnostics smoke gate | CI 和 release workflow 增加 `--durable-backend` basic run 与 `inspect durable` smoke；release evidence 捕获 `inspect-durable.json`；`release check` 新增 `REL-DURABLE-DIAGNOSTICS-001` 门禁和 `durable_diagnostics_smoke_ready` audit marker，要求后续发布继续保留 durable 诊断字段。 | `cargo fmt --check`; `cargo test -p eva-release`; `cargo test -p eva-cli release_check_json_reports_recovery_gate`; local durable smoke (`run --durable-backend`, `inspect durable`, `release check`) | Done |

## V1.7 真实 Lua VM 与热更新

目标：把静态 `on_event` contract 升级为真实 Lua 执行，同时保持沙箱、资源限制和 generation 生命周期。

| 迭代 | 小功能 | 前置项 | 具体逻辑实现 | 主要模块 | 验收与退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.7.1 | Lua VM execution boundary | V1.6 durable task/audit 可记录执行证据；Lua binding 选型 ADR 完成 | 在 `eva-lua-host` 内增加 VM adapter trait；加载 Lua script 后编译 chunk；执行 `on_event(ctx)`；把返回值转换为现有 Invoke/Agent result；静态 parser 作为 compatibility fallback | `eva-lua-host`、`eva-agent` | 成功、语法错误、runtime error、fallback 测试通过；错误映射不泄漏宿主路径 | Done |
| V1.7.2 | `ctx.tools` / `ctx.host` / memory binding | V1.7.1 完成；CapabilityHostApi 和 LuaContextSnapshot 字段固定 | 通过只读 `ctx` 注入 request、trace、memory snapshot；`ctx.tools.call` 只接受 capability ref 和 JSON value；`ctx.host.audit/log` 走 observability sink；禁止 Lua 拿 raw file/socket/process handle | `eva-lua-host`、`eva-capability`、`eva-memory`、`eva-observability` | 未授权 tool 拒绝；memory snapshot 不可写；Lua log/audit 带 trace id；敏感字段不进入脚本上下文 | Done |
| V1.7.3 | Lua 资源限制和取消 | V1.7.1、V1.7.2 完成；task cancel/timeout 已接 durable store | 为 VM 执行增加 wall-clock timeout、instruction budget hook、memory budget 和 cancellation token；`BasicRunOptions` 将 timeout/cancel 边界传入 Lua VM；超时、指令预算、内存超限和取消都映射为稳定错误与 TaskReport evidence。 | `eva-lua-host`、`eva-agent`、`eva-runtime` | 无限循环被终止；内存超限被拒绝；取消后不会继续调用 capability；task report 状态正确；`release check` 包含 `REL-LUA-RESOURCE-LIMITS-001`。 | Done |
| V1.7.4 | Shadow load、generation swap 和 rollback | V1.7.1-V1.7.3 完成；lifecycle generation 状态机已可持久记录 | 新 generation 先 shadow load 和 dry-run health event；通过后切换 scheduler route generation；旧 generation drain；失败时恢复旧 generation；所有切换写 audit | `eva-lua-host`、`eva-lifecycle`、`eva-scheduler`、`eva-runtime` | shadow 失败不切换；swap 后新任务进入新 generation；旧任务 drain；rollback 恢复旧 generation；`release check` 包含 Lua hot reload lifecycle gate | Done |

### V1.7.1 细分进度表

| 节点 | 日期 | 功能修改 | 具体修改 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.7.1.1 | 2026-07-07 | Lua VM adapter trait | `eva-lua-host` 新增 `vm.rs`，定义 `LuaVmAdapter` trait 和 `MluaVmAdapter`；仅在 `eva-lua-host` 引入 `mlua`，使用 vendored Lua 5.4；VM 显式只加载 table/string/utf8/math 标准库，避免打开 `os`、`io`、`package` 和 `debug`。 | `cargo test -p eva-lua-host` | Done |
| V1.7.1.2 | 2026-07-07 | `on_event` real execution boundary | `LuaHost` 通过 adapter 编译并执行 Lua chunk，支持 `return root`、global `on_event` 和 `root.on_event`；向脚本传入受控 event table 与 V1.2 `LuaContextSnapshot` 计数快照；将返回 table 转换成既有 `LuaEventResult`。 | `cargo test -p eva-lua-host`; `cargo test -p eva-runtime basic_example_runs_event_to_lua_and_capability` | Done |
| V1.7.1.3 | 2026-07-07 | Stable error mapping and fallback | 语法错误映射为 `lua_syntax_error`，runtime error 映射为 `lua_runtime_error`，错误消息不泄漏宿主路径；legacy 静态字段脚本在 VM load 失败且符合旧 contract 时走 compatibility fallback。 | `cargo test -p eva-lua-host` | Done |

### V1.7.2 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.7.2.1 | 2026-07-07 | Read-only request/trace/memory ctx | `eva-lua-host` 在受限 VM 中注入只读 `ctx.request`、`ctx.trace` 和 `ctx.memory` 表；保留旧 `ctx.private_memory_count`、`ctx.global_memory_count`、`ctx.knowledge_count` 和 `ctx.audit` 顶层字段以兼容 V1.7.1 脚本；移除 Lua `rawset` 全局入口，避免绕过只读快照。 | `cargo fmt --check`; `cargo test -p eva-lua-host`; `cargo test -p eva-runtime basic_example_runs_event_to_lua_and_capability` | Done |
| V1.7.2.2 | 2026-07-07 | `ctx.host.audit/log` observability sink | `eva-observability::AuditAction` 新增 `lua.host.log` 和 `lua.host.audit`；`eva-lua-host` 暴露受控 `ctx.host.log(level, message)` 与 `ctx.host.audit(message)`，生成带 request/trace/agent 字段的 `LuaHostObservation`；`eva-runtime` 将事件写入 `AuditSink`、task log 和 `BasicRunReport.lua_observability`；CLI JSON 输出结构化 `lua_observability`。 | `cargo fmt --check`; `cargo test -p eva-lua-host`; `cargo test -p eva-runtime basic_example_runs_event_to_lua_and_capability`; `cargo test -p eva-cli run_basic_example_json_succeeds`; `cargo test -p eva-observability audit_action_spelling_is_stable` | Done |
| V1.7.2.3 | 2026-07-07 | `ctx.tools.call` capability binding | `eva-lua-host` 通过 `LuaHost::run_on_event_with_tools` 注入 `CapabilityHostApi`；Lua 仅能调用 `ctx.tools.call(capability, value)`，其中 capability 必须是已注册且启用的 capability ref，value 会转换为 JSON-compatible text payload；返回 table 只包含 `request_id`、`status`、`ok`、`output`、`error` 和 `error_kind`，不暴露 raw provider/file/socket/process handle；basic 示例改为直接调用 `config.lint`，runtime 不再依赖旧 `capability` 返回字段触发二次调用。 | `cargo fmt --check`; `cargo test -p eva-lua-host`; `cargo test -p eva-runtime basic_example_runs_event_to_lua_and_capability`; `cargo test -p eva-cli run_basic_example_json_succeeds`; `cargo clippy -p eva-lua-host -p eva-runtime -p eva-cli -p eva-capability --all-targets -- -D warnings` | Done |

### V1.7.3 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.7.3.1 | 2026-07-07 | Lua wall-clock timeout hook | `eva-lua-host` 新增 `LuaExecutionLimits`，在 `MluaVmAdapter` 内通过 `mlua::Lua::set_hook` 每 N 条 VM 指令检查 wall-clock deadline；hook 超时映射为稳定 `ErrorKind::Timeout` / `lua_timeout`，并保留旧 `run_on_event` 默认无限制兼容路径；`eva-runtime` 将 `BasicRunOptions.timeout_ms` 传入 `run_on_event_with_tools_and_limits`，让 `run --example basic --timeout-ms <ms>` 的预算进入 Lua VM 内部而不是只在 handler 返回后判断。 | `cargo fmt --check`; `cargo test -p eva-lua-host on_event_infinite_loop_is_interrupted_by_timeout_limit`; `cargo test -p eva-runtime timeout_basic_run_records_dead_letter_and_replay`; `cargo test -p eva-runtime basic_example_runs_event_to_lua_and_capability`; `cargo clippy -p eva-lua-host -p eva-runtime --all-targets -- -D warnings` | Done |
| V1.7.3.2 | 2026-07-07 | Instruction budget limit | `LuaExecutionLimits` 新增 `instruction_budget` 与 builder；`MluaVmAdapter` 复用同一个 mlua hook 同时检查 wall-clock deadline 和 instruction counter，超过预算时抛出受控 marker，并映射为稳定 `ErrorKind::Timeout` / `lua_instruction_budget_exceeded`，上下文记录 `instruction_budget`；保留 V1.7.3.1 timeout hook 行为和默认无限制兼容路径。 | `cargo fmt --check`; `cargo test -p eva-lua-host on_event_infinite_loop_is_interrupted_by_instruction_budget`; `cargo test -p eva-lua-host on_event_infinite_loop_is_interrupted_by_timeout_limit`; `cargo clippy -p eva-lua-host --all-targets -- -D warnings` | Done |
| V1.7.3.3 | 2026-07-07 | Lua cancellation token | `eva-lua-host` 新增可共享 `LuaCancellationToken`，`LuaExecutionLimits` 可携带取消 token；mlua hook 优先检查 token，取消后抛出受控 marker 并映射为稳定 `ErrorKind::Conflict` / `lua_cancelled`，同时保持不可重试；`eva-runtime` 将 `BasicRunOptions.cancel_requested` 编码进 Lua limits，host 层测试覆盖第一次 capability 调用后触发取消、循环中断且第二次 capability 不会发生。 | `cargo fmt --check`; `cargo test -p eva-lua-host on_event_cancellation_token_stops_before_second_tool_call`; `cargo test -p eva-runtime cancelled_basic_run_returns_task_record`; `cargo clippy -p eva-lua-host -p eva-runtime --all-targets -- -D warnings` | Done |
| V1.7.3.4 | 2026-07-07 | Lua memory budget | `LuaExecutionLimits` 新增 `memory_limit_bytes` 与 builder；`MluaVmAdapter` 在创建受控 Lua VM 后调用 `Lua::set_memory_limit`，内存分配超限时将 `mlua::Error::MemoryError` 映射为稳定 `ErrorKind::Timeout` / `lua_memory_limit_exceeded`，上下文记录 `memory_limit_bytes`；memory limit 不可用时返回 `lua_memory_limit_unavailable`，避免静默失效。 | `cargo fmt --check`; `cargo test -p eva-lua-host on_event_memory_growth_is_rejected_by_memory_budget`; `cargo test -p eva-lua-host`; `cargo clippy -p eva-lua-host --all-targets -- -D warnings` | Done |

### V1.7.4 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.7.4.1 | 2026-07-07 | Lua shadow load health gate | `eva-lua-host` 新增 `LuaShadowLoader`、`LuaShadowCandidate`、`LuaShadowLoadReport` 和脚本级 report；候选 generation 先使用 dry-run health event 执行脚本，默认带 timeout、instruction budget 和 memory budget，并通过 no-op shadow capability host 支撑 `ctx.tools.call`，只生成健康/拒绝证据，不执行 scheduler route 切换、不 promote generation。 | `cargo fmt --check`; `cargo test -p eva-lua-host shadow_load`; `cargo clippy -p eva-lua-host --all-targets -- -D warnings` | Done |
| V1.7.4.2 | 2026-07-07 | Scheduler generation route gate | `eva-scheduler` 新增 `GenerationRouteGate` 和 `GenerationRouteCandidate`，记录 active generation 与 shadowing candidate；candidate shadow 未通过时 `stamp_new_work()` 继续给新事件写入 active generation，shadow healthy 后才写入 candidate generation，shadow rejected 会清理 candidate 并保留 active generation。 | `cargo fmt --check`; `cargo test -p eva-scheduler generation`; `cargo clippy -p eva-scheduler --all-targets -- -D warnings` | Done |
| V1.7.4.3 | 2026-07-07 | Generation drain evidence | `eva-lifecycle::DrainCoordinator` 新增 `plan_generation_swap_drain()` 和 `GenerationDrainEvidence`，swap 后为旧 generation 生成 drain plan，明确 `accepts_new_work=false`、保留 `inflight_tasks`，无 inflight 时直接 Completed，并写入 `generation:<old>:draining_after_swap_to:<new>` audit evidence。 | `cargo fmt --check`; `cargo test -p eva-lifecycle drain`; `cargo clippy -p eva-lifecycle --all-targets -- -D warnings` | Done |
| V1.7.4.4 | 2026-07-07 | Rollback audit and release gate | `RollbackCoordinator` 新增 `plan_generation_lifecycle_rollback()`，在候选 generation 失败时保留 previous healthy generation route、附加 drain evidence audit；`release check` 新增 `REL-LUA-HOT-RELOAD-001` 和 `lua_hot_reload_lifecycle_ready`，覆盖 shadow load、generation route gate、drain evidence 和 rollback audit。 | `cargo fmt --check`; `cargo test -p eva-lifecycle rollback`; `cargo test -p eva-release`; `cargo test -p eva-cli release_check_json_reports_recovery_gate`; `cargo clippy -p eva-lifecycle -p eva-release -p eva-cli --all-targets -- -D warnings` | Done |
| V1.7.4-alpha | 2026-07-07 | Lua hot reload lifecycle alpha release | 版本号提升到 `1.7.4-alpha`；CLI release label/runtime mode、`release check` readiness audit、migration target、README/使用手册/发布说明/i18n manifest/官网文案同步 V1.7.4-alpha；新增中英文发布说明并准备 `v1.7.4-alpha` tag。 | `cargo fmt --check`; `scripts/validate-i18n.ps1`; `scripts/validate-version-management.ps1 -Tag v1.7.4-alpha`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `cargo run -- --version`; `cargo run -- release check --output json`; `cargo run -- run --example basic --output json`; BOM/mojibake scan | Done |

## V1.8 外部 Provider、MCP 与 Skill Runner

目标：把 Adapter、MCP 和 Skill 从 probe/envelope 升级为受控真实执行，但所有外部副作用仍受 manifest、policy、timeout、audit 和 output limit 约束。

| 迭代 | 小功能 | 前置项 | 具体逻辑实现 | 主要模块 | 验收与退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.8.1 | `AdapterRuntime` 接入 stdio/http runner | V1.6 audit/task store 完成；Adapter manifest typed config 完成 | `AdapterHandle` 读取 manifest 中的 `command`、`args`、`endpoint`、`permissions.env`、`limits.timeout_ms`、`limits.output_limit_bytes`/`max_prompt_bytes` 和 HTTP `headers`；`AdapterRuntime::invoke()` 对 `stdio`/`http` 分发到真实 runner；stdio 子进程不走 shell、只允许 manifest command、注入 allowlist env 并脱敏 stdout/stderr；HTTP 通过标准库 TCP client 支持 `http://` fake/明文 provider，header 可从 `env:NAME` 注入并在输出/audit 中脱敏，`https://` 在无 TLS client 时返回稳定 unsupported。 | `eva-adapter`、`eva-config`、`eva-observability` | `cargo fmt --check`; `cargo test -p eva-adapter`; `cargo clippy -p eva-config -p eva-adapter --all-targets -- -D warnings`；覆盖 fake stdio/http provider、disabled provider 不启动、credential/header/stdout/stderr redaction。 | Done |
| V1.8.2 | MCP JSON-RPC client transport | V1.8.1 完成；MCP allowlist 和 mapping registry 固定 | 新增 `eva-mcp::McpJsonRpcClient`、`McpJsonRpcClientConfig` 和 stdio JSON-RPC transport；实现递增 request id、`initialize`、`notifications/initialized`、`tools/list`、`tools/call`、JSON-RPC error object 映射；`eva-adapter` 的 MCP invoke 改为按 manifest `mcp.command`/`mcp.args` 启动 stdio server，并在发送前执行 tool allowlist、prompt size、response timeout/output limit gate。 | `eva-mcp`、`eva-adapter` | `cargo fmt --check`; `cargo test -p eva-mcp -p eva-adapter`; `cargo clippy -p eva-mcp -p eva-adapter --all-targets -- -D warnings`；覆盖 fake MCP server tool call、blocked tool 不发 RPC、timeout 透传、协议 error object 和过大响应稳定错误。 | Done |
| V1.8.3 | MCP server lifecycle 和 session supervisor | V1.8.2 完成；runtime shutdown 语义稳定 | 新增 `McpSessionRegistry` 统一 start/health/shutdown/orphan cleanup，registry 拥有 `McpSession` 并通过 `McpSessionManager` 调用 supervisor；新增 stream start/abort 边界，aborted stream 不再出现在 health active streams；`EvaMcpServerSurface` 增加 explicit-tool gate，非法 Topic/event/state 代理请求返回稳定拒绝和 audit；`eva-observability` 增加 MCP session/stream/proxy audit action。 | `eva-mcp`、`eva-runtime`、`eva-observability` | `cargo fmt --check`; `cargo test -p eva-mcp -p eva-observability`; `cargo clippy -p eva-mcp -p eva-observability --all-targets -- -D warnings`；覆盖 session stop 后 registry 无悬挂 session、streaming 可中止、orphan cleanup、非法代理请求拒绝并审计。 | Done |
| V1.8.4 | Skill workflow runner | V1.8.1 完成；artifact store 可持久保存证据 | 按 skill manifest 校验输入 schema；创建隔离 working directory；支持 manifest 显式 process runner 和 `codex_skill` 受控内置 runner；收集 stdout、stderr、artifact；失败/超时写入 audit/artifact evidence；只收集受控 artifact 目录内的安全相对路径，敏感 env 输出脱敏 | `eva-adapter`、`eva-config`、`eva-storage`、`eva-observability`、`eva-cli` | workflow 成功/失败/超时覆盖；artifact path 受控；敏感 env 不进入日志；CLI `skill run` 保持统一 JSON envelope | Done |
| V1.8.5 | Capability provider selection 和 fallback | V1.8.1-V1.8.4 完成；policy domain 基础规则可用 | 为 capability 增加 explicit provider、default provider、fallback provider 解析；调用前执行 permission gate；provider 失败按可重试/不可重试分类；错误统一为 InvokeResponse | `eva-capability`、`eva-adapter`、`eva-mcp`、`eva-policy` | 未授权 capability 默认拒绝；explicit provider 可审计；fallback 顺序稳定且可测试 | Done |

### V1.8.4 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.8.4.1 | 2026-07-07 | Skill manifest schema surface | `eva-config` 新增 JSON-Schema-like object 子集读取；`AdapterHandle` 增加 `skill.path`、`skill.entry.type`、`skill.runner.*`、artifact root 和输入 schema 字段；`skill run` 调用前校验 object、required、string enum 和 prompt size。 | `cargo test -p eva-config adapter_manifest_exposes_skill_schema_subset`; `cargo test -p eva-adapter skill_schema` | Done |
| V1.8.4.2 | 2026-07-07 | Controlled workflow runner | `eva-adapter::transports::skill` 从 envelope 升级为 `SkillRunner`；为每次 request 创建隔离 working directory；支持 manifest allowlist process runner，不走 shell，注入受控 env/input/artifact path；无显式 command 的 `codex_skill` 走受控内置 runner，避免示例配置依赖本机 Codex 二进制。 | `cargo test -p eva-adapter skill`; `cargo test -p eva-cli skill` | Done |
| V1.8.4.3 | 2026-07-07 | Artifact/audit evidence and docs | Skill runner 使用 `FileSystemArtifactStore` 保存 stdout、stderr、run-report 和受控 artifact 目录内容；拒绝不安全相对路径和 symlink；credential env 输出脱敏；`eva-observability` 增加 skill run audit action；同步 CLI、adapter、未完整清单和本实施计划。 | `cargo clippy -p eva-adapter -p eva-config -p eva-observability --all-targets -- -D warnings`; `cargo test -p eva-adapter skill`; doc stale-text scan | Done |

### V1.8.5 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.8.5.1 | 2026-07-07 | Provider selection plan | `eva-capability` 新增 `selection.rs`，用 `CapabilityProviderSelection` 保存 manifest provider、default provider、fallback providers 和 required adapter capabilities；`CapabilityDescriptor` 从 capability manifest 保留这些字段；`CapabilityRouter::provider_plan()` 在 capability 已注册且启用后生成 deterministic provider plan，顺序为 explicit request、manifest provider、default provider、fallback providers，并按首次出现去重；builtin capability 继续不触发外部 provider。 | `cargo fmt --check`; `cargo test -p eva-capability -p eva-lua-host capability`; `cargo clippy -p eva-capability -p eva-lua-host --all-targets -- -D warnings` | Done |
| V1.8.5.2 | 2026-07-07 | Capability/provider permission gate | `eva-policy::PermissionSet` 新增 explicit capability/adapter allow 查询，供 runtime gate 默认拒绝未显式授权的身份；`eva-capability` 新增 `CapabilityPermissionGate`，在 provider plan 后检查逻辑 capability、required adapter capabilities、adapter provider allowlist 和 manifest provider allowlist；`CapabilityRouter::authorized_provider_plan()` 返回已授权的 provider plan，后续真实调用必须从该入口进入。 | `cargo fmt --check`; `cargo test -p eva-capability -p eva-policy`; `cargo clippy -p eva-capability -p eva-policy --all-targets -- -D warnings` | Done |
| V1.8.5.3 | 2026-07-07 | Adapter-backed InvokeResponse normalization | `eva-adapter` 新增 `AdapterBackedCapabilityHost` 实现 `CapabilityHostApi`，先调用 `CapabilityRouter::authorized_provider_plan()`，再按 plan provider 顺序进入 `AdapterRuntime`；`AdapterInvokeReport` 统一映射为 `InvokeResponse::completed/failed/timeout`，transport `EvaError` 也归一为 failed/timeout response；`eva-core::InvokeResponse` 新增 `timeout_with_error()`，保留 provider code 和 safe context。 | `cargo fmt --check`; `cargo test -p eva-core -p eva-adapter -p eva-capability -p eva-policy capability`; `cargo clippy -p eva-core -p eva-adapter -p eva-capability -p eva-policy --all-targets -- -D warnings` | Done |
| V1.8.5.4 | 2026-07-07 | Retryable fallback classification and docs | `AdapterBackedCapabilityHost` 将 transport `EvaError` 和非 completed `AdapterInvokeReport` 都归一为 `EvaError` 后再执行同一分类；仅当 `is_retryable_provider_failure()` 返回 true 时继续下一个 provider；不可重试配置/权限/参数错误立即停止并保留 provider context；全部 provider 可重试失败时返回最后一个安全错误；同步 adapter/capability README 和中英文未完整清单。 | `cargo fmt --check`; `cargo test -p eva-adapter capability_host`; `cargo test --workspace`; `cargo clippy --workspace --all-targets -- -D warnings`; `./scripts/validate-i18n.ps1`; `cargo run -- release check --output json` | Done |

## V1.9 Policy、Discovery、Memory、Observability 深化

目标：把配置、权限、发现、记忆和观测从基础契约升级为真实 runtime 的持续门禁和上下文能力。

| 迭代 | 小功能 | 前置项 | 具体逻辑实现 | 主要模块 | 验收与退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.9.1 | 完整 JSON Schema validator 与错误定位 | V1.8 provider manifest 字段稳定；现有 schema paths 可用 | `eva-config::schema` 新增当前 schema 使用的 JSON Schema 子集 validator，支持 `type`、`required`、`properties`、`additionalProperties`、`items`、`enum`、`pattern` 和 `minProperties`；`load_project_config` 在 typed loader 前校验 `eva.yaml`、Agent/Adapter/Capability manifest、policy 和 routes；schema 错误写入 `path`、`schema_path`、`field`、`schema_rule` 和 `suggestion`；新增 Agent permission provider/capability 引用校验；CLI `config validate --output json` 透传 schema rule context。 | `eva-config`、`eva-cli` | `cargo fmt --check`; `cargo test -p eva-config`; `cargo test -p eva-config -p eva-cli config_validate`; `cargo clippy -p eva-config -p eva-cli --all-targets -- -D warnings` | Done |
| V1.9.2 | Policy domain parser 和运行时门禁 | V1.9.1 完成；Adapter/MCP/Hardware/Backup/Lifecycle 高风险动作清单固定 | 将 YAML policy domain 解析为 PolicyLayer；runtime request 合并 EffectivePolicy；高风险动作默认拒绝；扩权必须显式声明并写 audit | `eva-policy`、`eva-runtime`、`eva-adapter`、`eva-hardware`、`eva-backup` | policy YAML round trip；高风险 request 默认 denied；扩权拒绝和允许路径都有审计 | Done |
| V1.9.3 | Discovery 多来源与增量扫描 | V1.9.2 完成；发现结果仍不授予 execution handle | 增加 PATH command、MCP source、OMX/Codex source、外部 registry source；`DiscoverySourceReport` 输出 timeout、cache key、elapsed/status、source/candidate reject reason；`DiscoveryCache::merge_source` 支持增量刷新单 source 候选 | `eva-discovery`、`eva-config`、`eva-mcp`、`eva-adapter`、`eva-cli` | 慢 source 不阻塞整体扫描；多 source 隔离；候选不含 raw handle；CLI JSON 可查询 `source_reports` 和 reject reason | Done |
| V1.9.4 | Durable Memory/Knowledge | V1.6 durable backend 完成；Lua context 注入边界完成 | `eva-memory` 新增 durable memory/knowledge 文件 store，挂载 durable backend `state/memory` 与 `state/knowledge`；memory 支持 TTL/expiration、可逆 run-length compression 和 context 前 redaction；knowledge index 可从 durable item 重建；外部 knowledge retrieval 通过 `RuntimePolicyGate` 的 provider gate 决策；CLI `memory context --durable-backend` 走 durable round trip | `eva-memory`、`eva-storage`、`eva-lua-host`、`eva-policy`、`eva-cli` | Agent 私有记忆不串租户；过期记录不进入 context；索引重建结果一致；敏感字段被移除；CLI durable smoke 可验证 | Done |
| V1.9.5 | Observability 生产后端 | V1.6 audit store、V1.8 provider audit 已可产出事件 | `eva-observability` 新增 best-effort file backend、audit/metrics JSONL sink 和 OpenTelemetry-style span export adapter；metrics 增加 runtime/provider/task label helpers；`TraceFields` 支持 child span 和 continuity key；CLI 新增 `observability smoke` 验证 audit/metrics/span export，后端不可用时降级到 in-memory audit/metrics 且命令不失败 | `eva-observability`、`eva-runtime`、`eva-cli` | audit 可持久化；trace id 连续；metrics 可导出；后端不可用时不阻塞核心任务并有降级 audit | Done |

### V1.9.2 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.9.2.1 | 2026-07-08 | Policy domain typed parser | `eva-policy` 新增 `domains.rs`，把 `adapter_policy`、`hardware_policy`、`mcp_server`、`runtime_policy` 和 `lua_sandbox` 从 `PolicyDocument` 解析为 typed domain，并生成可参与 `EffectivePolicy` 合并的 `PolicyLayer`。 | `cargo test -p eva-policy` | Done |
| V1.9.2.2 | 2026-07-08 | Runtime high-risk gate | 新增 `HighRiskAction`、`RuntimePolicyRequest`、`RuntimePolicyGate` 和 `PolicyDecision`；MCP tool、topic emit、Skill runtime gate、hardware bind/raw I/O、restore/upgrade/release/supervisor 等高风险动作默认拒绝，显式 allow 会生成 allow audit。 | `cargo test -p eva-policy` | Done |
| V1.9.2.3 | 2026-07-08 | CLI policy gate wiring | `skill run` 在 runner 启动前检查 Skill runtime gate；`hardware bind` JSON 新增 policy audit；`restore apply` 错误上下文暴露 policy decision；`upgrade apply` 锁报告记录 destructive supervisor handoff 的 policy deny audit，保持 plan-first 不执行真实 mutation。 | `cargo test -p eva-policy -p eva-cli` | Done |

### V1.9.3 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.9.3.1 | 2026-07-08 | 多来源 discovery adapters | `eva-discovery` 新增 PATH command、MCP、OMX、Codex 和 external registry source；`DiscoveryCandidateKind` 增加 `path_command`、`workflow` 和 `registry_entry`；所有来源只记录候选和 trust/reject reason，`handle_granted` 恒为 `false`。 | `cargo test -p eva-discovery` | Done |
| V1.9.3.2 | 2026-07-08 | Source timeout 和 reject report | `DiscoverySource` 增加 `cache_key()` 和 `timeout_ms()`；`DiscoverySourceReport` 记录 source id、cache key、timeout、elapsed、status、error 和 reject reason；慢 source 超时不返回候选，候选级拒绝原因会汇总到 source report。 | `cargo test -p eva-discovery` | Done |
| V1.9.3.3 | 2026-07-08 | 增量缓存和 CLI source reports | `DiscoveryCache::merge_source()` 支持按 source 替换候选；`DiscoveryService::scan_project_incremental()` 只合并成功 source；`eva discovery scan --output json` 保留 `candidate_count/candidates` 并新增 `source_report_count/source_reports`。 | `cargo test -p eva-discovery -p eva-cli`; `cargo run -- discovery scan --output json` | Done |

### V1.9.4 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.9.4.1 | 2026-07-08 | Durable memory store | `eva-memory::FileSystemMemoryStore` 写入/读取 durable backend `state/memory`；记录保留 visibility、owner、version、request id、retention、created/expires 和 compression 元数据；私有记录仍按 Agent owner 读取。 | `cargo test -p eva-memory durable_memory_round_trips_private_global_ttl_and_compression` | Done |
| V1.9.4.2 | 2026-07-08 | TTL、expiration 和 compression | `MemoryWrite`/`MemoryRecord` 增加 `created_at_ms`、`expires_at_ms`、`MemoryCompression`；`snapshot_for_agent_at` 和 `ContextRequest::with_now_ms` 过滤过期记录；durable store 对 `run_length` 做可逆落盘编码。 | `cargo test -p eva-memory expired_memory_is_omitted_from_snapshots` | Done |
| V1.9.4.3 | 2026-07-08 | Redaction 和敏感信息扫描 | 新增 `redaction.rs`，在 `ContextBuilder` 输出前扫描并替换 token/password/secret/api key 等字段；audit 记录 redaction 数量，Lua snapshot 只携带计数和 audit。 | `cargo test -p eva-memory context_filters_expired_memory_and_redacts_sensitive_values` | Done |
| V1.9.4.4 | 2026-07-08 | Knowledge rebuild 和 provider gate | `InMemoryKnowledgeService::snapshot_items/rebuild_from_items` 支持索引重建；`FileSystemKnowledgeStore` 从 `state/knowledge` 重建 index；`ExternalKnowledgeRetrievalRequest` 使用 `RuntimePolicyGate` 的 `adapter.invoke` 决策作为外部检索 gate。 | `cargo test -p eva-memory knowledge_index_can_be_rebuilt_from_items external_retrieval_requires_runtime_policy_gate` | Done |
| V1.9.4.5 | 2026-07-08 | CLI durable memory context | `eva memory context --durable-backend <path>` 打开 durable backend，写入 memory/knowledge seed，再从文件 store 重新 load 构建 context；JSON 输出 TTL/compression 元数据并验证过期和脱敏。 | `cargo test -p eva-cli memory_context_can_use_durable_backend_with_redaction_and_expiration`; `cargo run -- memory context --durable-backend .eva/ci-memory --output json` | Done |

### V1.9.5 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.9.5.1 | 2026-07-08 | File observability backend | `FileObservabilitySink` 写入 `audit.jsonl`、`metrics.jsonl` 和 `otel-spans.jsonl`，复用稳定 trace/audit/metric 字段，不引入外部 SDK 依赖。 | `cargo test -p eva-observability file_observability_sink_writes_audit_metrics_and_otel_span` | Done |
| V1.9.5.2 | 2026-07-08 | Best-effort degradation | `BestEffortObservabilityPipeline` 在后端不可用或写入失败时降级到 in-memory audit/metrics，记录 degraded reason，调用方仍返回 `Ok`。 | `cargo test -p eva-observability best_effort_pipeline_degrades_without_failing` | Done |
| V1.9.5.3 | 2026-07-08 | Metrics labels and trace continuity | `MetricLabels::runtime/provider/task` 固定生产标签；`TraceFields::child_span()` 保留 request/correlation 等字段并替换 span；`continuity_key()` 为 CLI/runtime/provider/Lua 串联提供稳定 key。 | `cargo test -p eva-observability metric_labels_cover_runtime_provider_and_task_surfaces child_span_preserves_trace_continuity` | Done |
| V1.9.5.4 | 2026-07-08 | CLI observability smoke | 新增 `eva observability smoke --backend <path>`，记录 audit、runtime/provider/task metrics 和 OTel-style spans；JSON 输出 degraded、audit/metric/span 数量和 continuity key。 | `cargo test -p eva-cli observability_smoke_writes_backend_and_reports_degraded_mode`; `cargo run -- observability smoke --backend .eva/ci-observability --output json` | Done |

## V1.10 硬件真实接入与高风险 Apply

目标：在 durable、policy 和 observability 地基上，逐步启用真实硬件、restore apply、upgrade apply 和 supervisor handoff。

| 迭代 | 小功能 | 前置项 | 具体逻辑实现 | 主要模块 | 验收与退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.10.1 | Hardware driver registry 与 simulator 回归 | V1.9 policy gate 完成；现有 simulated driver trait 固定 | `eva-config` 新增 hardware typed config，解析 bus/match/identity/protocol/hotplug/driver，并预留 USB/串口/BLE/socket/vendor SDK driver kind；`eva-hardware` 新增 `HardwareDriverRegistry` 和 `run_simulator_contract_suite`，真实 driver 与 simulator 共用 `HardwareDriver` trait；`eva-adapter` hardware transport 改为经 driver registry 调用 simulator，非 simulated driver kind 保持 reserved/unsupported | `eva-hardware`、`eva-config`、`eva-adapter` | simulator contract suite 通过；driver 不暴露 raw handle；未授权 bind 拒绝；真实 driver kind 可启动前校验但不打开设备 | Done |
| V1.10.2 | OS 权限、真实 driver lifecycle 和 hotplug event | V1.10.1 完成；EventBus durable publish 可用 | `eva-hardware` 新增 `HardwareLifecycleCoordinator` 和 `OsPermissionProvider` 抽象，driver start 先过 `RuntimePolicyGate` 和 OS permission，再 claim lease 并进入 opened；stop 必须匹配 lease request id 并释放 lease；crash path 清理 lease 并写 failed audit；`publish_hotplug_event` 将 typed hotplug payload 发布到 `EventBus`；`eva-observability` 增加 hardware driver/hotplug audit action | `eva-hardware`、`eva-eventbus`、`eva-policy`、`eva-observability` | lease mismatch 拒绝；hotplug event 可订阅；driver crash 后 lease 清理；权限缺失错误稳定 | Done |
| V1.10.3 | 签名/加密 backup archive 和 pre-restore evidence | V1.9 observability 完成；backup artifact verification 已存在 | `eva-backup` 新增 `BackupArchiveCodec`，生成包含 entry bytes 的稳定 archive payload，记录 sealed/plaintext checksum、keyed signature、可选 sealed archive metadata 和 `RemoteBackupTarget` typed contract；`ManifestVerifier` 复算 artifact bytes digest 并校验 archive signature；`RestoreApplyPlan` 增加 pre-restore backup evidence，`restore apply --dry-run` 强制同时校验目标 backup artifact 和 pre-restore backup artifact；`eva-release` 新增 signed backup archive readiness gate | `eva-backup`、`eva-storage`、`eva-release`、`eva-cli` | 签名验证失败阻断；archive corrupt 测试；pre-restore backup 缺失时不能 apply | Done |
| V1.10.4 | Destructive restore apply gate | V1.10.3 完成；policy 高风险审批可用 | `restore plan` 继续默认 dry-run；`restore apply` 必须带 confirmation、apply lock、policy approval、health check；执行中分阶段写 audit；失败触发 rollback plan；当前只输出 gated/blocked report，保持 `mutation_executed:false`；`release check` 暴露 restore apply gate readiness | `eva-backup`、`eva-lifecycle`、`eva-policy`、`eva-cli`、`eva-release` | 无 confirmation 不执行；锁冲突可恢复；失败可回滚；健康检查失败阻断 apply；不移动 release pointer 或 supervisor handoff | Done |
| V1.10.5 | Supervisor blue-green handoff 和 release pointer mutation | V1.10.4 完成；generation swap 和 durable task recovery 已完成 | `eva-lifecycle` 新增 `SupervisorHandoffCoordinator`、`RuntimeBinaryProbe` 和 filesystem supervisor state store；`upgrade apply --state-store` 在 confirmation、lock、`supervisor.handoff` policy、`release.pointer_mutation` policy、runtime binary smoke 和 health 通过后提交 candidate generation、drain old generation、写 `state/release-pointer`、持久化 `handoff.prepared/committed`；health 失败输出 rollback plan 且不写 pointer；`release check` 暴露 supervisor handoff readiness | `eva-lifecycle`、`eva-runtime`、`eva-release`、`eva-backup`、`eva-cli` | handoff failure rollback；release pointer mutation 有审计；state store 可恢复；生产 OS service-manager adapter 仍留给后续 | Done |

### V1.10.1 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.10.1.1 | 2026-07-08 | Hardware typed config | `eva-config` 新增 `HardwareAdapterConfig`、bus/protocol/claim/driver kind 枚举和 `AdapterManifest::hardware_config()`；`load_project_config` 启动前拒绝未知 hardware driver kind；`scale-main` manifest 显式声明 `hardware.driver`。 | `cargo test -p eva-config adapter_manifest_parses_hardware_typed_config hardware_config_reserves_real_driver_kinds load_project_config_rejects_unknown_hardware_driver_kind` | Done |
| V1.10.1.2 | 2026-07-08 | Driver registry | `eva-hardware` 新增 `HardwareDriverRegistry`，注册 `HardwareDriver` trait object，按 driver id 调用并拒绝重复/缺失 driver；discovery 改为读取 typed hardware config。 | `cargo test -p eva-hardware driver_registry_invokes_registered_simulator project_discovery_returns_non_authorizing_candidates` | Done |
| V1.10.1.3 | 2026-07-08 | Simulator contract suite | 新增 `run_simulator_contract_suite`，验证 simulator audit 包含 `raw_io:false`、不暴露 raw handle、capability mismatch 返回 `PermissionDenied`。 | `cargo test -p eva-hardware simulator_contract_suite_rejects_raw_io_and_capability_bypass` | Done |
| V1.10.1.4 | 2026-07-08 | Adapter registry wiring | `AdapterHandle` 读取 hardware driver id/kind；hardware transport 经 `HardwareDriverRegistry` 调用 `SimulatedDriver`，非 simulated driver kind 返回 reserved unsupported，不打开设备。 | `cargo test -p eva-adapter hardware_transport_returns_simulated_audit_only handle_reads_hardware_identity_extensions` | Done |

### V1.10.2 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.10.2.1 | 2026-07-08 | OS permission and policy gate | `HardwareLifecycleCoordinator::start_driver` 在 claim 前调用 `RuntimePolicyGate` 和 `OsPermissionProvider`；`StaticOsPermissionProvider` 可稳定模拟 granted/denied，权限缺失返回 `PermissionDenied` 并保留 device/bus/permission context。 | `cargo test -p eva-hardware lifecycle_checks_policy_os_permission_claim_and_audit lifecycle_rejects_missing_os_permission_before_claim` | Done |
| V1.10.2.2 | 2026-07-08 | Driver lifecycle lease cleanup | lifecycle start claim 后进入 opened；stop 必须匹配 lease request id；crash path 移除 active session、释放 lease 并写 failed audit。 | `cargo test -p eva-hardware lifecycle_stop_requires_matching_lease_and_releases driver_crash_releases_lease_and_records_failed_audit` | Done |
| V1.10.2.3 | 2026-07-08 | Typed hotplug publish | `publish_hotplug_event` 复用 `HotplugStateMachine`，把 device/action/previous/next/reason 写入 EventBus text payload，返回 `EventReceipt` 并写 audit。 | `cargo test -p eva-hardware hotplug_publish_emits_typed_eventbus_event` | Done |
| V1.10.2.4 | 2026-07-08 | Hardware audit actions | `eva-observability::AuditAction` 增加 `hardware.driver.started`、`hardware.driver.stopped`、`hardware.hotplug.published` 稳定拼写，并同步 README。 | `cargo test -p eva-observability audit_action_spelling_is_stable` | Done |

### V1.10.3 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.10.3.1 | 2026-07-08 | Signed backup archive | `eva-backup` 新增 `archive.rs`，`BackupService::create` 写入稳定 archive payload，记录 sealed/plaintext checksum、signature manifest，并用 `ManifestVerifier::verify_backup_archive` 校验 artifact key、checksum 和 signature。 | `cargo test -p eva-backup backup_service_creates_and_verifies_artifact backup_signature_mismatch_blocks_verification` | Done |
| V1.10.3.2 | 2026-07-08 | Optional sealed archive and remote target contract | `BackupEncryptionKey` 支持可选 sealed archive，`RemoteBackupTarget` 记录 filesystem/object-store/S3-compatible typed target；remote target 进入签名 payload 但不执行上传。 | `cargo test -p eva-backup backup_service_can_encrypt_archive_and_record_remote_target` | Done |
| V1.10.3.3 | 2026-07-08 | Archive corruption detection | `ManifestVerifier::verify_artifact` 复算 `ArtifactRecord.bytes` digest，不只信 metadata digest；bytes tamper 返回稳定 `Conflict`。 | `cargo test -p eva-backup verifier_rejects_corrupt_record_bytes` | Done |
| V1.10.3.4 | 2026-07-08 | Pre-restore evidence dry-run gate | `RestoreApplyPlan` 增加 `PreRestoreBackupEvidence`；CLI plan parser 支持 `pre_restore_backup_artifact_id` 和 `pre_restore_backup_digest`；`restore apply --dry-run` 必须从 filesystem artifact store 同时验证目标 backup 和 pre-restore backup。 | `cargo test -p eva-backup dry_run_validates_matching_backup_and_pre_restore_evidence`; `cargo test -p eva-cli restore_apply` | Done |
| V1.10.3.5 | 2026-07-08 | Release readiness gate | `eva-release` 新增 `REL-BACKUP-ARCHIVE-001` required gate，把 signed archive、optional sealing、remote target contract 和 pre-restore evidence 纳入 `release check` ready 证据。 | `cargo test -p eva-release readiness_has_no_blocking_required_gates` | Done |

### V1.10.4 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.10.4.1 | 2026-07-08 | Restore apply confirmation and evidence reuse | `create_restore_apply` 复用 `create_restore_apply_dry_run`，非 dry-run 仍要求 plan、confirmation、filesystem artifact store、signed backup digest 和 pre-restore backup evidence 都匹配。 | `cargo test -p eva-backup restore_apply`; `cargo test -p eva-cli restore_apply` | Done |
| V1.10.4.2 | 2026-07-08 | Apply lock | `RestoreApplyLockStore` 新增 in-memory/filesystem 实现；CLI `--lock-store` 写入 `{plan_id}.restore.lock`，重复 apply 返回稳定 `Conflict`。 | `cargo test -p eva-backup restore_apply_gate_reports_lock_conflict`; `cargo test -p eva-cli restore_apply_reports_lock_conflict` | Done |
| V1.10.4.3 | 2026-07-08 | Policy approval gate | 非 dry-run `restore apply` 在锁创建前执行 `RuntimePolicyGate`；默认 policy 拒绝 `restore.apply` 且不留下锁，显式 allow 后进入 gated path。 | `cargo test -p eva-cli restore_apply_default_policy_denies_without_locking restore_apply_gates_when_policy_lock_and_health_pass` | Done |
| V1.10.4.4 | 2026-07-08 | Health and rollback-required output | `--health failed` 返回 `status:"blocked"`、`apply_allowed:false`、`mutation_executed:false`，并通过 `RollbackCoordinator::plan_failed_handoff` 输出 rollback plan；健康通过返回 gated report。 | `cargo test -p eva-cli restore_apply_health_failure_emits_rollback_plan`; `cargo test -p eva-backup restore_apply_health_failure_blocks_apply_after_lock` | Done |
| V1.10.4.5 | 2026-07-08 | Staged mutation boundary | `RestoreApplyReport` 和 CLI JSON/text 输出显式包含 `mutation_executed:false`，记录 V1.10.4 只开放 destructive apply gate，不执行真实文件恢复、release pointer mutation 或 supervisor handoff。 | `cargo test -p eva-cli restore_apply`; `cargo fmt --check` | Done |
| V1.10.4.6 | 2026-07-08 | Release readiness gate | `eva-release` 新增 `REL-RESTORE-APPLY-GATE-001` required gate 和 `restore_apply_gate_baseline_ready` audit marker，把 confirmation、policy approval、filesystem lock、health gate、rollback-required plan 和 staged mutation boundary 纳入 `release check` 证据。 | `cargo test -p eva-release`; `cargo run -- release check --output json` | Done |

### V1.10.5 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.10.5.1 | 2026-07-08 | Supervisor handoff coordinator | `eva-lifecycle::handoff` 新增 `SupervisorHandoffCoordinator`、`RuntimeBinaryProbe`、`SupervisorHandoffReport`，复用 generation/supervisor/drain/rollback 边界提交 healthy candidate。 | `cargo test -p eva-lifecycle handoff` | Done |
| V1.10.5.2 | 2026-07-08 | Filesystem state and release pointer mutation | `FileSystemSupervisorStateStore` 写入 `handoff.prepared`、`handoff.committed` 和 `state/release-pointer`；health failed path 只写 prepared，不写 pointer。 | `cargo test -p eva-lifecycle filesystem_store_persists_handoff_state_and_pointer`; `cargo test -p eva-cli upgrade_apply_handoff_health_failure_emits_rollback_without_pointer_mutation` | Done |
| V1.10.5.3 | 2026-07-08 | CLI `upgrade apply --state-store` | `upgrade apply` 保留 lock-only 兼容路径；传入 `--state-store` 后要求 `supervisor.handoff` 和 `release.pointer_mutation` policy allow，支持 `--runtime-binary` 的 `--version` smoke、缺失/失败阻断和 `--health healthy|failed|unavailable`。 | `cargo test -p eva-cli upgrade_apply` | Done |
| V1.10.5.4 | 2026-07-08 | Release readiness gate | `eva-release` 新增 `REL-SUPERVISOR-HANDOFF-001` required gate 和 `supervisor_handoff_baseline_ready` audit marker，`release check` 报告 handoff/pointer mutation 证据。 | `cargo test -p eva-release`; `cargo run -- release check --output json` | Done |
| V1.10.5.5 | 2026-07-08 | Remaining production boundary | 文档明确 V1.10.5 已执行本地 state-store release pointer mutation，但未替代生产 OS service manager/daemon supervisor。 | `cargo fmt --check`; docs review | Done |

## V1.11 分发链路与 CLI 收敛

目标：把真实 runtime 能力包装成可交付、可验证、可维护的发布链路，同时降低 CLI 单文件维护成本。

| 迭代 | 小功能 | 前置项 | 具体逻辑实现 | 主要模块 | 验收与退出标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V1.11.1 | Signed artifact 与 provenance bundle | V1.10 apply/supervisor 真实路径完成；release gates 可读取 artifact evidence | `eva-release` 新增 V1.11.1 key/value artifact evidence contract，记录 artifact digest、签名、源码 commit、构建参数、SBOM 和扫描状态；`ReleaseHardeningService::readiness_with_artifact_evidence` 将 evidence 转换成 required release gate；CLI `release check --artifact-evidence` 读取并阻断未签名 artifact、签名 mismatch 或 provenance/source commit mismatch | `eva-release`、`eva-cli` | release check 阻断未签名 artifact；provenance 可追踪源码、构建和扫描证据 | Done |
| V1.11.2 | 跨平台安装器和包管理器发布 | V1.11.1 完成；安装路径和升级策略固定 | `eva-release::distribution` 新增 V1.11.2 key/value distribution evidence contract，记录 Windows/Linux/macOS install smoke、安装/升级/卸载文档和 package-manager dry-run；release workflow 为 native archive 解包后执行 `eva --version`，GHCR 发布后执行 `docker buildx imagetools inspect` dry-run；CLI `release check --distribution-evidence` 将证据转换为 required release gate | `eva-release`、CI/release scripts、docs/site | 三平台安装 smoke；卸载/升级路径文档化；包管理器 dry-run 通过 | Done |
| V1.11.3 | 外部安全扫描与生产 benchmark gate | V1.11.1 完成；release check 支持外部 evidence | `eva-release::scanner` 新增 V1.11.3 external scanner evidence contract，高危/严重 finding 或 scan skipped/failed 阻断 release；`eva-release::benchmark` 新增 measured benchmark evidence contract，并可转换为既有 `PerformanceBaselineReport`；CLI `release check --security-scan-evidence/--benchmark-evidence` 和 `release perf --benchmark-evidence` 接入真实测量输入；release workflow 生成 cargo-audit 与 release binary benchmark evidence 后执行门禁 | `eva-release`、`eva-cli`、CI/release scripts、docs | scanner 缺失或高危失败会阻断；benchmark evidence 可复现；release perf 输出稳定 | Done |
| V1.11.4 | CLI command module split | V1.10 高风险 apply UX 固定；现有 JSON contract 有回归测试 | 将 `run.rs` 中 version/doctor/config/inspect/task/adapter/mcp/skill/discovery/memory/observability/hardware/backup/lifecycle/release 拆成命令模块；保留 formatter、exit code、JSON envelope 共享层 | `eva-cli` | 公共命令 JSON diff 不变；模块级测试覆盖新增分发；`cargo test -p eva-cli` 通过 | Done |
| V1.11.5 | `emit`、`agent`、`capability` 命令补齐 | V1.11.4 完成；runtime/provider gates 已可复用 | 新增 `emit` 发布 typed event；`agent status/drain/reload` 调用 runtime/lifecycle；`capability list/call/probe` 走 provider selection 和 policy gate；高风险 call 需要 dry-run/confirm | `eva-cli`、`eva-runtime`、`eva-agent`、`eva-capability` | 新命令 text/json 测试；未授权 capability call 拒绝；高风险命令无确认不执行 | Done |

### V1.11.1 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.11.1.1 | 2026-07-08 | Artifact evidence contract | `eva-release::artifact` 新增 `ReleaseArtifactEvidence`、`ReleaseArtifactSubject`、`ReleaseProvenanceEvidence`、`ReleaseArtifactSignature` 和 key/value manifest parser，固定 `eva.release.artifact_evidence.v1` 格式。 | `cargo test -p eva-release artifact` | Done |
| V1.11.1.2 | 2026-07-08 | Signature and provenance verification | 使用 SHA-256 keyed signature contract 校验 artifact digest/source metadata；unsigned、signature mismatch、provenance commit mismatch 和 scan 非 passed 都生成 blocked report。 | `cargo test -p eva-release readiness_with_unsigned_artifact_evidence_blocks_release_artifact_gate` | Done |
| V1.11.1.3 | 2026-07-08 | Release readiness gate | `ReleaseHardeningService::readiness_with_artifact_evidence` 将验证报告转成 `REL-ARTIFACT-PROVENANCE-001` required gate；默认 source-release check 保持 ready，显式 evidence 失败时阻断。 | `cargo test -p eva-release readiness_with_signed_artifact_evidence_passes_release_artifact_gate` | Done |
| V1.11.1.4 | 2026-07-08 | CLI artifact evidence check | `release check --artifact-evidence <path>` 读取 evidence manifest；签名/provenance 通过时输出 pass gate，unsigned 时返回配置门禁 exit code `2`。 | `cargo test -p eva-cli release_check` | Done |

### V1.11.2 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.11.2.1 | 2026-07-08 | Distribution evidence contract | `eva-release::distribution` 新增 `ReleaseDistributionEvidence`、`ReleaseInstallSmokeEvidence`、`ReleasePackageDryRunEvidence` 和 key/value manifest parser，固定 `eva.release.distribution_evidence.v1` 格式。 | `cargo test -p eva-release distribution` | Done |
| V1.11.2.2 | 2026-07-08 | Install smoke and package dry-run verification | distribution verifier 要求 Windows/Linux/macOS 至少各有一个 `passed` install smoke，安装/升级/卸载文档路径非空，package-manager dry-run 至少一个且全部 `passed`。 | `cargo test -p eva-release readiness_with_failed_package_dry_run_blocks_release_distribution_gate` | Done |
| V1.11.2.3 | 2026-07-08 | CLI distribution evidence gate | `release check --distribution-evidence <path>` 读取 manifest；通过时输出 `REL-DISTRIBUTION-001` pass，缺 smoke 或 dry-run 失败时返回配置门禁 exit code `2`。 | `cargo test -p eva-cli release_check_with_distribution_evidence` | Done |
| V1.11.2.4 | 2026-07-08 | Release workflow install smoke | `.github/workflows/release.yml` 在 Windows/Linux/macOS native archive 打包后解压到临时 install 目录并运行 `eva --version`；publish job 生成 `release-distribution.evidence` 并执行 release gate。 | `git diff --check`; workflow review | Done |
| V1.11.2.5 | 2026-07-08 | Install/upgrade/uninstall docs | 新增中英文安装、升级、卸载说明，更新 GitHub Packages 发布方案、CLI/release README 和未完整实现清单。 | docs review | Done |

### V1.11.3 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.11.3.1 | 2026-07-08 | External scanner evidence contract | `eva-release::scanner` 新增 `ReleaseSecurityScanEvidence`、`ReleaseSecurityScanFinding` 和 key/value manifest parser，固定 `eva.release.security_scan_evidence.v1` 格式。 | `cargo test -p eva-release scanner` | Done |
| V1.11.3.2 | 2026-07-08 | Security scan readiness gate | scanner verifier 要求 `scan_status=passed`，且 high/critical finding 会生成 `REL-SECURITY-SCAN-001` blocked gate；low/medium finding 只进入 evidence。 | `cargo test -p eva-release readiness_with_high_security_scan_finding_blocks_gate` | Done |
| V1.11.3.3 | 2026-07-08 | Benchmark evidence contract | `eva-release::benchmark` 新增 `ReleaseBenchmarkEvidence`、`ReleaseBenchmarkMeasurement` 和 key/value manifest parser，支持真实 observed/budget/sample/command/environment 输入。 | `cargo test -p eva-release benchmark` | Done |
| V1.11.3.4 | 2026-07-08 | Benchmark release/perf gate | `ReleaseHardeningService` 新增 `REL-BENCHMARK-001` required gate；`ReleaseBenchmarkEvidence::to_performance_report()` 保持 `release perf` JSON shape 稳定并在超预算时返回性能 exit code `4`。 | `cargo test -p eva-cli release_perf` | Done |
| V1.11.3.5 | 2026-07-08 | CLI and release workflow integration | `release check` 增加 `--security-scan-evidence` / `--benchmark-evidence`，`release perf` 增加 `--benchmark-evidence`；release workflow 安装 `cargo-audit`、生成 `release-security-scan.evidence`，并对 release binary 做三次真实计时生成 `release-benchmark.evidence` 后执行门禁。 | `cargo test -p eva-cli release_check`; workflow YAML parse | Done |
| V1.11.3.6 | 2026-07-08 | Docs and inventory sync | 更新 `eva-release`/`eva-cli` README、V1.x 计划和未完整实现清单，明确剩余发布缺口收窄为生产签名/attestation credential、OS 包仓库发布和上传链路。 | docs review | Done |

### V1.11.4 细分进度表

| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.11.4.1 | 2026-07-08 | Release command module split | `run.rs` 新增 `release_cmd` 子模块分发；`run/release_cmd.rs` 承载 `release check/security/perf/migration` 的 parser、evidence reader、writer 和 JSON formatter，继续复用父级 JSON envelope、trace 和 exit code helper。 | `cargo fmt --check`; `cargo test -p eva-cli release_check`; `cargo test -p eva-cli release_perf`; `cargo test -p eva-cli v15_release_hardening_commands_report_json` | Done |
| V1.11.4.2 | 2026-07-08 | Version and config command module split | `run/version_cmd.rs` 承载 `version`/`--version` 的 text/json writer；`run/config_cmd.rs` 承载 `config validate` parser、`ValidationReport` 和 writer；两者继续复用父级 JSON envelope、trace 和 exit code helper。 | `cargo fmt --check`; `cargo test -p eva-cli version_text_and_json_report_v174_lua_hot_reload_lifecycle_alpha`; `cargo test -p eva-cli config_validate_json` | Done |
| V1.11.4.3 | 2026-07-08 | Adapter/MCP/discovery diagnostic module split | `run/adapter_cmd.rs` 承载 `adapter list/probe`；`run/mcp_cmd.rs` 承载 `mcp list/probe`；`run/discovery_cmd.rs` 承载 `discovery scan` 和 source report formatter；三者继续复用父级 JSON envelope、trace、exit code 和 shared JSON helper。 | `cargo fmt --check`; `cargo test -p eva-cli v11_external_capability_commands_report_json`; `cargo test -p eva-cli discovery_scan_json_reports_source_statuses` | Done |
| V1.11.4.4 | 2026-07-08 | Memory/observability command module split | `run/memory_cmd.rs` 承载 `memory context` parser、in-memory/durable context seed 和 memory/knowledge/Lua formatter；`run/observability_cmd.rs` 承载 `observability smoke` parser、file JSONL backend smoke 和 degraded report formatter；两者继续复用父级 JSON envelope、trace、exit code 和 shared JSON helper。 | `cargo fmt --check`; `cargo test -p eva-cli memory_context`; `cargo test -p eva-cli observability_smoke` | Done |
| V1.11.4.5 | 2026-07-08 | Task command module split | `run/task_cmd.rs` 承载 `task status/logs/cancel` parser、local/durable task store reader/writer、cancel state update 和 task JSON formatter；`run --example basic` 继续通过 task module 写入 task snapshot，保持 run/task JSON contract 不变。 | `cargo fmt --check`; `cargo test -p eva-cli task_`; `cargo test -p eva-cli run_basic_example_json_succeeds`; `cargo test -p eva-cli run_cancelled_basic_example_reports_cancelled_task` | Done |
| V1.11.4.6 | 2026-07-08 | Skill command module split | `run/skill_cmd.rs` 承载 `skill list/run` parser、Skill runtime policy gate、adapter-backed runner invocation、skill list writer 和 invoke trace formatter；继续复用父级 JSON envelope、trace、exit code 和 shared JSON helper。 | `cargo fmt --check`; `cargo test -p eva-cli skill_run_json_links_adapter_audit_to_invocation_trace`; `cargo test -p eva-cli v11_external_capability_commands_report_json` | Done |
| V1.11.4.7 | 2026-07-08 | Hardware command module split | `run/hardware_cmd.rs` 承载 `hardware list/probe/bind` parser、project device discovery、bind plan、runtime policy gate audit 和 hardware candidate/plan formatter；继续复用父级 JSON envelope、trace、exit code 和 shared JSON helper。 | `cargo fmt --check`; `cargo test -p eva-cli hardware_commands_report_candidates_and_bind_plan` | Done |
| V1.11.4.8 | 2026-07-08 | Backup command module split | `run/backup_cmd.rs` 承载 `backup create` parser、BackupService 调用、signed/sealed archive writer 和 backup result formatter；snapshot/restore 继续复用该模块导出的 backup result helper，保持 lifecycle JSON contract 不变。 | `cargo fmt --check`; `cargo test -p eva-cli v14_backup_lifecycle_commands_report_json` | Done |
| V1.11.4.9 | 2026-07-08 | Snapshot command module split | `run/snapshot_cmd.rs` 承载 `snapshot create/promote` parser、ReleaseSnapshotService 调用、release pointer plan formatter 和 snapshot JSON writer；restore plan 继续复用该模块导出的 snapshot helper，保持 backup/lifecycle JSON contract 不变。 | `cargo fmt --check`; `cargo test -p eva-cli v14_backup_lifecycle_commands_report_json`; `cargo test -p eva-cli snapshot_promote` | Done |
| V1.11.4.10 | 2026-07-08 | Restore command module split | `run/restore_cmd.rs` 承载 `restore plan/apply` parser、restore apply dry-run、filesystem artifact/lock gate、policy/health/rollback writer 和 restore JSON formatter；继续复用 backup/snapshot helper 与父级 JSON envelope、trace、exit code helper，保持 staged `mutation_executed:false` contract 不变。 | `cargo fmt --check`; `cargo test -p eva-cli restore_apply`; `cargo test -p eva-cli v14_backup_lifecycle_commands_report_json` | Done |
| V1.11.4.11 | 2026-07-08 | Upgrade command module split | `run/upgrade_cmd.rs` 承载 `upgrade check/apply` parser、apply lock、state-store handoff、runtime binary smoke、release pointer/rollback formatter；继续复用父级 JSON envelope、trace、exit code helper，保持 lock-only 和 handoff JSON contract 不变。 | `cargo fmt --check`; `cargo test -p eva-cli upgrade_apply`; `cargo test -p eva-cli v14_backup_lifecycle_commands_report_json` | Done |
| V1.11.4.12 | 2026-07-08 | Inspect command module split | `run/inspect_cmd.rs` 承载 `inspect` / `inspect durable` parser、project inspect writer、durable backend diagnostics reader/writer 和 JSON formatter；继续复用父级 JSON envelope、trace、exit code helper，保持 `inspect` / `inspect.durable` contract 不变。 | `cargo fmt --check`; `cargo test -p eva-cli inspect` | Done |
| V1.11.4.13 | 2026-07-08 | Doctor command module split | `run/doctor_cmd.rs` 承载 `doctor` parser、workspace/config/schema/runtime boundary check executor 和 text/JSON writer；继续复用 `doctor.rs` 检查实现与父级 JSON envelope、trace、exit code helper。 | `cargo fmt --check`; `cargo test -p eva-cli doctor` | Done |
| V1.11.4-alpha | 2026-07-09 | CLI command module split alpha release | 版本号提升到 `1.11.4-alpha`；CLI release label/runtime mode、`release check` readiness audit、migration target、README/文档入口/发布说明/i18n manifest 同步 V1.11.4-alpha；新增中英文发布说明并准备 `v1.11.4-alpha` tag。 | `cargo fmt --check`; `scripts/validate-version-management.ps1 -Tag v1.11.4-alpha`; `cargo test -p eva-cli`; `cargo run -- --version`; `cargo run -- release check --output json`; docs review | Done |

### V1.11.5 细分进度表
| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.11.5.1 | 2026-07-09 | Typed event emit command | 新增 `run/emit_cmd.rs`，接入 `eva emit <topic>`；构造 `eva-core::Event`，支持 text/empty/bytes payload、agent/capability/adapter target、request/generation/correlation/causation metadata，并可发布到 in-memory 或 `--durable-backend` EventBus；text/JSON 输出返回 publish receipt 和 backend evidence。 | `cargo fmt --check`; `cargo test -p eva-cli emit` | Done |
| V1.11.5.2 | 2026-07-09 | Agent lifecycle commands | 新增 `run/agent_cmd.rs`，接入 `eva agent status/drain/reload`；`status` 从 ProjectConfig manifest 和 `AgentRuntime` 输出 lifecycle/queue/subscription evidence，`drain` 复用 `AgentLifecycle` 与 `DrainCoordinator` 输出 `accepts_new_work:false` drain plan，`reload` 复用 `GenerationController` 与 generation drain evidence 输出可审计 reload plan；所有路径保持 `mutation_executed:false`，不伪装为常驻 daemon 热更新。 | `cargo test -p eva-cli agent` | Done |
| V1.11.5.3 | 2026-07-09 | Capability routing commands | 新增 `run/capability_cmd.rs`，接入 `eva capability list/probe/call`；`list/probe` 复用 `CapabilityRegistry`、manifest provider selection 和 Adapter probe 输出 provider plan/permission gate evidence；`call` 默认 dry-run，显式 `--confirm <request-id>` 后才执行，复用 `CapabilityPermissionGate`、`RuntimePolicyGate`、`CapabilityRouter` 与 `AdapterBackedCapabilityHost`；显式 provider 超出 manifest allowlist 时返回 policy error，确认执行仍保持 `mutation_executed:false`。 | `cargo test -p eva-cli capability` | Done |
| V1.11.5-alpha | 2026-07-09 | CLI runtime command completion alpha release | 版本号提升到 `1.11.5-alpha`；CLI release label/runtime mode、`release check` readiness audit、migration target、README/文档入口/使用手册/发布说明/i18n manifest/官网文案同步 V1.11.5-alpha；新增中英文发布说明并准备 `v1.11.5-alpha` tag。 | `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `scripts/validate-i18n.ps1`; `scripts/validate-version-management.ps1 -Tag v1.11.5-alpha`; `cargo run -- --version`; `cargo run -- release check --output json`; smoke: emit/agent/capability | Done |

### V1.12 细分进度表
| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.12.1 | 2026-07-09 | Daemon process boundary | 新增 `eva daemon start/status/stop` 与 `eva-runtime::daemon`；固定 pid/lock/state、foreground/dev smoke、durable backend、policy、observability 和 shutdown contract；明确 `provider_processes_started:false`。 | `cargo fmt --check`; `cargo test -p eva-runtime daemon -- --nocapture`; `cargo test -p eva-cli daemon_ -- --nocapture`; daemon foreground smoke | Done |
| V1.12.2 | 2026-07-09 | Runtime control API/IPC | 新增 daemon filesystem mailbox request/response 协议和前台 control loop；CLI `daemon status/shutdown/submit/cancel/drain/reload` 通过 mailbox 调用；无 running daemon 时返回 `unavailable`，request/response 带 `trace_id`。 | `cargo fmt --check`; `cargo test -p eva-runtime daemon -- --nocapture`; `cargo test -p eva-cli daemon_ -- --nocapture` | Done |
| V1.12.3 | 2026-07-09 | Durable task lifecycle | `TaskStateSnapshot` 增加 heartbeat/deadline/cancel token/interrupted reason；新增 queued/running/cancelling/timed_out/completed/failed/interrupted/recovering 状态迁移 helper 和 `update_snapshot()`；daemon submit/cancel 使用 queued/cancelling lifecycle；recovery 覆盖 `cancelling`；AgentRunControl 输出 cancel token/deadline context。 | `cargo test -p eva-storage task_state`; `cargo test -p eva-agent cancel`; `cargo test -p eva-runtime recovery`; `cargo test -p eva-cli daemon_control` | Done |
| V1.12.4 | 2026-07-09 | Scheduler retry dispatch | `DurableEventBus` 暴露 due/replay 状态查询；`eva-scheduler::retry` 投递 replay event 到 mailbox；`eva-runtime::scheduler_retry` 执行 due scan、redrive、mailbox dispatch 和 durable ack/fail；daemon foreground loop 每轮 control polling 前运行 retry tick；recovery/diagnostics 跳过已存在 replay 子事件。 | `cargo test -p eva-eventbus due_dead_letters`; `cargo test -p eva-scheduler retry_dispatch`; `cargo test -p eva-runtime scheduler_retry`; `cargo test -p eva-runtime daemon_control_loop_ticks_scheduler_retry_once` | Done |
| V1.12.5 | 2026-07-09 | Agent daemon drain/reload mutation | `agent drain/reload` 连接 running daemon 时通过 filesystem mailbox 发送 `Drain`/`ReloadPlan`；daemon 写入 `agent-control.state`，记录 drain `accepts_new_work:false`、reload 后 `selected_generation_for_new_work`、旧 generation `draining` 状态和 audit；无 daemon 时 CLI 回退到原 `mutation_executed:false` evidence contract。 | `cargo test -p eva-runtime daemon_drain_mutates_agent_control_state`; `cargo test -p eva-runtime daemon_reload_mutates_generation_route_state`; `cargo test -p eva-cli agent_drain_and_reload_use_daemon_mutation_when_available`; `cargo test -p eva-cli agent_drain_outputs_drain_plan_without_runtime_mutation`; `cargo test -p eva-cli agent_reload_outputs_generation_swap_evidence` | Done |
| V1.12.6 | 2026-07-09 | Daemon release readiness gate | `ReleaseHardeningService::readiness` 新增 `REL-DAEMON-RUNTIME-001` required gate，汇总 V1.12.1-V1.12.5 的 daemon process boundary、filesystem mailbox、durable task lifecycle、scheduler retry tick 和 daemon-backed agent drain/reload mutation evidence；CI 增加 daemon control/retry/drain/reload smoke，版本 runtime mode 增加 `daemon_release_gate_v1.12.6`。 | `cargo test -p eva-release readiness_has_no_blocking_required_gates`; `cargo test -p eva-cli release_check_json_reports_recovery_gate`; `cargo run -- release check --output json` | Done |

### V1.13 细分进度表
| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.13.1 | 2026-07-09 | Provider supervisor slots and process table | 新增 `eva-storage::ProviderProcessSnapshot`、`ProviderProcessTable` 和 `InMemoryProviderProcessTable`；新增 `eva-adapter::ProviderSupervisor` / `InMemoryProviderSupervisor`，`AdapterRuntime::invoke` 对 stdio/http/MCP/Skill provider 统一 acquire/release execution slot，记录 session/process id、manifest digest、start command、health、last error、restart policy 和 audit；路由/disabled provider 失败发生在 acquire 前，transport 启动失败会释放 slot 并在 process table 中留下 failed evidence；版本 runtime mode 增加 `provider_supervisor_v1.13.1`。 | `cargo test -p eva-storage provider_process`; `cargo test -p eva-adapter supervisor`; `cargo test -p eva-adapter runtime::tests`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha` | Done |
| V1.13.2 | 2026-07-09 | Provider credential/session isolation | 新增 `ProviderCredentialScope`，由 supervisor slot 生成 provider session id、request/capability/provider 绑定和 redacted session token；`AdapterRuntime` 在 dispatch 前执行 `provider.credential_session` policy decision，拒绝 caller-supplied scope 和跨 provider/session 复用；stdio/HTTP/Skill 注入 session env/header/token 并把 token 纳入 stdout/stderr/body/artifact redaction，MCP 记录同一 session scope audit；`eva-observability` 增加稳定 `provider.credential_session` audit action；版本 runtime mode 增加 `provider_credential_session_v1.13.2`。 | `cargo test -p eva-policy provider_credential_session`; `cargo test -p eva-adapter supervisor`; `cargo test -p eva-adapter runtime::tests`; `cargo test -p eva-adapter transports::skill::tests`; `cargo test -p eva-observability audit_action_spelling_is_stable`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha` | Done |
| V1.13.3 | 2026-07-09 | Provider concurrency/rate/circuit admission | `AdapterHandle` 读取 `limits.max_concurrency`、`limits.rate_limit` 和 `limits.circuit_breaker`；`InMemoryProviderSupervisor` 在 acquire 前执行并发、限流和熔断 admission，超限返回 retryable `Unavailable` 且不写入新 process；连续失败达到阈值后 provider health 进入 `circuit_open`，恢复窗口后允许 half-open probe；`RuntimePolicyGate` 暴露 capability retry backoff，admission error 可写入 `retry_after_ms`；`eva-scheduler` 增加通用 `RetryBackoffPolicy`/`decide_retry_backoff`，capability host 保持 non-retryable 不 fallback、retryable gate 可 fallback。版本 runtime mode 增加 `provider_limits_circuit_breaker_v1.13.3`。 | `cargo test -p eva-adapter supervisor`; `cargo test -p eva-adapter runtime::tests::runtime_blocks_new_provider_process_while_circuit_is_open`; `cargo test -p eva-adapter manifest::tests::handle_reads_stdio_and_http_runtime_fields`; `cargo test -p eva-adapter capability_host::tests::adapter_backed_host_falls_back_after_retryable_rate_limit_gate`; `cargo test -p eva-policy runtime_gate_exposes_adapter_retry_backoff_by_capability`; `cargo test -p eva-scheduler retry_backoff` | Done |
| V1.13.4 | 2026-07-09 | Provider stream artifact data plane | 新增 `eva-adapter::stream` bounded collector，stdio/Skill stdout/stderr 使用固定 chunk、preview、truncation 和受控 artifact key；HTTP TCP client 不再 `read_to_end`，先读 header 再按 body limit 分块截断；HTTP/MCP/stdio/Skill invoke JSON 输出 stream summary，完整受限输出写入 filesystem artifact store 并保留 redaction；版本 runtime mode 增加 `provider_stream_artifact_v1.13.4`。 | `cargo test -p eva-adapter`；提交前继续运行 workspace 验证门禁 | Done |
| V1.13.5 | 2026-07-09 | Provider execution-state recovery | `eva-storage` 新增 `FileSystemProviderProcessTable`，将 provider process snapshot 写入 durable `state/provider-processes/`，并支持 `mark_interrupted_after_restart()` 保留 last error 与 audit chain；`InMemoryProviderSupervisor` 可选镜像 acquire/complete 到 filesystem process table，`AdapterRuntime` 暴露 durable table 构造入口；`RuntimeRecoveryCoordinator` 新增 task+provider process recovery，daemon start 会扫描 active provider session、标记 interrupted、关联 task interrupted/recovering，并只在显式 retryable restart policy 下生成 scheduler backoff evidence；CLI `daemon start` JSON 新增 `recovery` 对象；版本 runtime mode 增加 `provider_execution_recovery_v1.13.5`。 | `cargo test -p eva-storage provider_process`; `cargo test -p eva-adapter runtime_can_mirror_provider_processes_to_durable_table`; `cargo test -p eva-runtime recovery_interrupts_active_provider_process_and_preserves_task`; `cargo test -p eva-runtime recovery_schedules_provider_backoff_only_when_restart_policy_allows_it`; `cargo test -p eva-runtime daemon_start_recovers_interrupted_provider_process_state`; `cargo test -p eva-cli daemon_start_foreground_smoke_reports_verified_boundaries`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha` | Done |
| V1.13.6 | 2026-07-09 | MCP HTTP transport/auth boundary | `eva-mcp` 新增 `McpServerTransport::Http`、`McpHttpJsonRpcTransport` 和 `McpJsonRpcClient::call_http()`，以无新依赖的 `http://` POST JSON-RPC 边界执行 `initialize`、`notifications/initialized`、`tools/list`、`tools/call`，并保持 allowlist 先于 endpoint/RPC、timeout、output limit 和 JSON-RPC error object 映射；`eva-adapter` 让 manifest `mcp.endpoint`/`mcp.headers` 进入 `AdapterHandle`，MCP HTTP invoke 要求 provider credential scope，注入 session auth headers，读取 env auth header 并对输出/artifact 预览脱敏；CLI runtime marker 增加 `mcp_http_auth_v1.13.6`。`https://` TLS client、生产 streaming 数据面和真实外部 MCP server compatibility 仍待后续补齐；repo-local compatibility matrix 已由 V1.13.7 接续完成。 | `cargo test -p eva-mcp json_rpc -- --nocapture`; `cargo test -p eva-adapter mcp -- --nocapture`; `cargo test -p eva-adapter runtime_invokes_mcp_http_adapter_with_auth_headers -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.13.7 | 2026-07-09 | MCP streaming compatibility matrix gate | `eva-mcp` 新增 `McpCompatibilityMatrix` fixture/report，覆盖 stdio/HTTP transport、HTTP auth header、timeout/output limit、tool schema、stream lifecycle start/abort/cleanup、dangling session count 和 explicit-tool server surface，并把缺失 cleanup、transport stream lifecycle 或无限代理暴露映射为 blocked failure；`eva-release` 将 fixture verification 接入 required gate `REL-MCP-COMPAT-001`，缺失 matrix 时阻断 release check；CLI runtime marker 增加 `mcp_compat_matrix_v1.13.7`。该基线仍不是真实外部 MCP server compatibility certification、HTTPS/TLS client 或生产 streaming 数据面。 | `cargo test -p eva-mcp compatibility -- --nocapture`; `cargo test -p eva-release mcp_compatibility -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.13.8 | 2026-07-09 | Provider supervision release gate | `eva-release` 新增 required gate `REL-PROVIDER-SUPERVISION-001`，汇总 provider supervisor slot、in-memory/durable process table、credential session scope、concurrency/rate/circuit admission、bounded stream artifact、daemon restart recovery 和 V1.13.7 `REL-MCP-COMPAT-001` evidence；release audit 新增 `provider_supervision_readiness_gate_ready`；CLI runtime marker 增加 `provider_supervision_release_gate_v1.13.8`；README、使用手册、中英文未完整清单同步该 gate 不等于 OS provider process supervisor、OS credential vault 或用户隔离。 | `cargo test -p eva-release provider_supervision -- --nocapture`; `cargo test -p eva-release readiness_has_no_blocking_required_gates -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture`; `cargo run -- release check --output json` | Done |

### V1.14 细分进度表
| 节点 | 日期 | 小功能 | 修改内容 | 验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V1.14.1 | 2026-07-09 | Restore staged mutation planner | `eva-backup` 新增 `RestoreMutationStep`、copy/delete/replace operation、`RestoreStagedMutationPlanner`、deterministic `preflight_hash`、`affected_paths` 和 `rollback_manifest`；相对路径拒绝 absolute/root/parent traversal、Windows prefix/backslash 和 symlink target kind；`restore apply` plan 文件新增可选 `restore_target_root` 与重复 `mutation_step=operation\|relative_path\|source_artifact_key\|expected_digest\|pre_restore_digest\|target_kind` 字段，dry-run/gated JSON 追加 `mutation_plan`，并继续保持 `mutation_executed:false`；CLI runtime marker 增加 `restore_staged_mutation_planner_v1.14.1`。 | `cargo test -p eva-backup restore_apply -- --nocapture`; `cargo test -p eva-cli restore_apply -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.14.2 | 2026-07-09 | Destructive file mutation engine | `eva-backup` 新增 `RestoreMutationEngine`、`RestoreMutationApplyReport` 和 transaction log entry；在 staged plan、source artifact digest、target pre-restore digest、safe target path 和 no-symlink traversal 都通过后执行 copy/delete/replace；每步写 `{plan_id}.restore.txn`，失败时停止并输出 `rollback_required`；`eva-cli restore apply` 在 confirmation、artifact evidence、policy、lock 和 health gate 都通过且 plan 声明 mutation steps 时调用 engine，成功时输出 `status:"applied"`、`mutation_executed:true` 和 `mutation_apply`，旧 no-step plan 仍只返回 gated；CLI runtime marker 增加 `restore_file_mutation_engine_v1.14.2`。 | `cargo test -p eva-backup restore_apply -- --nocapture`; `cargo test -p eva-cli restore_apply -- --nocapture`; `cargo clippy -p eva-backup -p eva-cli --all-targets -- -D warnings` | Done |
| V1.14.3 | 2026-07-10 | Restore rollback apply | `eva-backup` 新增 `RestorePreRestoreArchive`、transaction log parser、`RestoreRollbackEngine` 和 `RestoreRollbackApplyReport`；rollback 只接受 `rollback_required` transaction log、匹配 staged plan `preflight_hash`，按 committed steps 倒序恢复，copy 回滚删除当前 restored file，delete/replace 回滚从 pre-restore archive entry 恢复旧 bytes，并拒绝当前 target digest 漂移；`eva-cli restore rollback` 复用 plan confirmation、artifact evidence、policy allow、rollback lock 和 health gate，默认读取 `{plan_id}.restore.txn`，成功输出 `status:"rolled_back"`、`rollback_executed:true` 和 rollback log；CLI runtime marker 增加 `restore_rollback_apply_v1.14.3`。 | `cargo test -p eva-backup restore_apply -- --nocapture`; `cargo test -p eva-cli restore_rollback -- --nocapture`; `cargo test -p eva-cli restore_apply -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.14.4 | 2026-07-10 | Restore operator UX 强确认 | `eva-cli restore apply --dry-run`、`restore apply` 和 `restore rollback` 的 text/JSON 输出新增 `operator_confirmation`，包含 `confirm_token`、`target_root`、`affected_count`、`apply_allowed`、`mutation_planned`、`mutation_executed`、`rollback_required`、`rollback_executed`、`irreversible_warning` 和 `next_action`；版本 runtime marker 增加 `restore_operator_confirmation_v1.14.4`；README 和中英文未完整清单同步该输出只强化操作者判断，不放宽 confirmation/policy/lock/health gate。 | `cargo test -p eva-cli restore_apply_dry_run_reports_staged_mutation_plan -- --nocapture`; `cargo test -p eva-cli restore_apply_executes_staged_mutation_when_plan_declares_steps -- --nocapture`; `cargo test -p eva-cli restore_rollback_restores_failed_staged_mutation_from_transaction_log -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.14.5 | 2026-07-10 | OS service-manager abstraction | `eva-lifecycle` 新增 `ServiceManagerAdapter` trait、`ServiceManagerKind`、`ServiceManagerDefinition`、status/handoff/rollback report 和 `FakeServiceManagerAdapter`；fake adapter 可提交 healthy candidate、阻断 failed candidate、执行 rollback 并输出 audit；`eva-config` 新增 top-level `service_manager` typed config，`enabled:true` 必须显式指定 kind 和 service name；`eva-release` 新增 `REL-SERVICE-MANAGER-ABSTRACTION-001` readiness gate；CLI runtime marker 增加 `service_manager_abstraction_v1.14.5`。 | `cargo test -p eva-lifecycle service_manager -- --nocapture`; `cargo test -p eva-config service_manager -- --nocapture`; `cargo test -p eva-release readiness_has_no_blocking_required_gates -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.15.1 | 2026-07-10 | Hardware OS permission provider | `eva-hardware` 扩展 `OsPermissionCheck`，新增 `PlatformOsPermissionProvider`，按平台报告 OS、user、permission、source、安全逻辑 `device_path`、remediation 和 `raw_device_path_exposed:false`；`HardwareLifecycleCoordinator` 在权限缺失时先于 lease claim/driver start 返回 `PermissionDenied` 并保留 remediation；`eva-cli hardware bind` JSON 新增 `permission` evidence，`--apply` 仍只验证逻辑计划和权限诊断；`eva-release` 的 `SEC-HW-001` 记录该证据；CLI runtime marker 增加 `hardware_os_permission_provider_v1.15.1`。 | `cargo test -p eva-hardware permission -- --nocapture`; `cargo test -p eva-cli hardware_commands_report_candidates_and_bind_plan -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture`; `cargo test -p eva-release security -- --nocapture` | Done |
| V1.15.4 | 2026-07-10 | Hardware hotplug subscriber | `eva-hardware` 新增 `run_hotplug_subscriber_once`、`HardwareHotplugSubscriberReport`、`HardwareHotplugDeviceState` 和 state render/parse helper；subscriber 从 manifest snapshot 生成 insert/remove/reconnect/fail typed hotplug event，保证 `raw_handles_exposed:false`；`HardwareLifecycleCoordinator` 新增 watcher crash release path；`eva-runtime daemon start` 运行 hotplug subscriber smoke，把事件写入 durable EventBus，并把 `hardware-hotplug.state` 持久化到 daemon state dir；CLI daemon start JSON 新增 `hardware_hotplug` 和 `paths.hardware_hotplug_state_file`；CLI runtime marker 增加 `hardware_hotplug_subscriber_v1.15.4`。 | `cargo test -p eva-hardware hotplug -- --nocapture`; `cargo test -p eva-runtime daemon_hotplug_subscriber_persists_state_across_restart -- --nocapture`; `cargo test -p eva-cli daemon_start_foreground_smoke_reports_verified_boundaries -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture`; `cargo test -p eva-release security -- --nocapture` | Done |
| V1.15.5 | 2026-07-10 | Hardware safety release gate | `eva-release` 新增 required gate `REL-HARDWARE-SAFETY-001`，在 `release check` 中汇总 simulator parity、permission denial、lease cleanup、daemon hotplug smoke evidence；release audit 新增 `hardware_safety_release_gate_ready`；`SEC-HW-001` 追加 V1.15.5 evidence；CLI runtime marker 增加 `hardware_safety_release_gate_v1.15.5`；README 和中英文未完整清单同步该 gate 只接受 alpha simulator-only evidence，不声明真实 USB/串口/BLE/socket/vendor SDK I/O。 | `cargo test -p eva-release hardware_safety -- --nocapture`; `cargo test -p eva-release readiness_has_no_blocking_required_gates -- --nocapture`; `cargo test -p eva-cli release_check_json_reports_recovery_gate -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture`; `cargo run -- release check --output json` | Done |
| V1.15.6 | 2026-07-10 | Memory/Knowledge maintenance locks and compaction | `eva-memory` 新增 durable `index.lock` guard、atomic record writes、TTL GC、`memory-gc.checkpoint`、`knowledge-rebuild.checkpoint`、stale started checkpoint recovery 和 `memory.maintenance` audit action；durable memory/knowledge read/write/load/rebuild 都经 filesystem lock；`eva-runtime daemon start` 执行一次维护 smoke 并在 CLI JSON 输出 `memory_maintenance`；CLI runtime marker 增加 `memory_knowledge_maintenance_v1.15.6`。该基线仍不是生产 retrieval provider、策略化 redaction audit 或长驻后台 scheduler。 | `cargo test -p eva-memory durable -- --nocapture`; `cargo test -p eva-runtime daemon_start_smoke_verifies_boundaries_and_stops -- --nocapture`; `cargo test -p eva-cli daemon_start_foreground_smoke_reports_verified_boundaries -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.15.7 | 2026-07-10 | Knowledge retrieval provider execution | `eva-memory` 新增 `ExternalKnowledgeRetrievalRequest::execute`、`ExternalKnowledgeRetrievalReport` 和 line-based hex retrieval schema，先经 `RuntimePolicyGate`，再通过 `CapabilityHostApi::invoke_with_provider` 调用显式 provider；provider failed/timeout/schema rejected/policy denied 均不写入 index；成功结果先脱敏并重算 source digest，再写入 knowledge index 和 source audit。`eva-capability` 新增默认拒绝的 `invoke_with_provider` trait 入口，`eva-adapter::AdapterBackedCapabilityHost` 实现 trait-level explicit provider routing；CLI runtime marker 增加 `knowledge_retrieval_provider_v1.15.7`。该基线仍不是生产检索调度或长驻后台 scheduler；策略化 redaction/audit 由 V1.15.8 补齐。 | `cargo test -p eva-memory knowledge_service -- --nocapture`; `cargo test -p eva-adapter capability_host -- --nocapture`; `cargo test -p eva-capability router -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.15.8 | 2026-07-10 | Redaction policy and memory audit wiring | `eva-policy` 新增 `memory_policy.redaction` typed domain，默认读取 `config/policies/memory.yaml` 的 enabled、replacement、sensitive key fragment、token prefix 和 audit 开关；`eva-memory` 的 redaction/context/provider retrieval 支持 policy-driven rules，并新增 memory write/read/search/context audit action、`memory.operation.count` 与 `memory.redaction.count` metrics；`eva memory context` 默认写入 `.eva/data/observability` JSONL，runtime marker 增加 `memory_redaction_audit_v1.15.8`。该基线仍不是长驻后台 scheduler 或生产检索调度。 | `cargo test -p eva-policy memory_redaction -- --nocapture`; `cargo test -p eva-memory observed -- --nocapture`; `cargo test -p eva-memory policy_driven -- --nocapture`; `cargo test -p eva-cli memory_context_writes_request_agent_observability -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.16.1 | 2026-07-10 | Runtime/provider/task audit sink wiring | `eva-observability::AuditAction` 新增 `runtime.control`、`task.lifecycle`、`scheduler.retry`、`provider.supervised`、`restore.apply` 和 `restore.rollback` 稳定动作；`eva-runtime` 把 daemon recovery/control、submit/cancel task lifecycle 和 scheduler retry tick 写入 `BestEffortObservabilityPipeline`；`eva-adapter::AdapterRuntime` 在 project 构造路径默认写 provider supervisor audit/metric/span，并在 admission/policy/transport failure 上 best-effort 降级；`eva-cli restore apply/rollback` 成功非 dry-run 路径写 `.eva/data/observability` JSONL；CLI runtime marker 增加 `runtime_audit_sink_wiring_v1.16.1`。该 V1.16.1 基线当时仍不引入后续 V1.16.2 tracing bridge、OpenTelemetry SDK/exporter、retention/rotation 或生产后台服务。 | `cargo test -p eva-observability audit_action_spelling_is_stable -- --nocapture`; `cargo test -p eva-adapter runtime_writes_provider_observability_when_backend_is_configured -- --nocapture`; `cargo test -p eva-runtime daemon_control_submit_cancel_writes_observability_pipeline -- --nocapture`; `cargo test -p eva-runtime daemon_control_observability_degrades_without_blocking_control_flow -- --nocapture`; `cargo test -p eva-runtime daemon_control_loop_ticks_scheduler_retry_once -- --nocapture`; `cargo test -p eva-cli restore_apply_gates_when_policy_lock_and_health_pass -- --nocapture`; `cargo test -p eva-cli restore_rollback_restores_failed_staged_mutation_from_transaction_log -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture`; `cargo clippy --workspace --all-targets -- -D warnings` | Done |
| V1.16.2 | 2026-07-10 | tracing subscriber bridge | `eva-observability` 新增 `TracingBridgeLayer`、`TracingBridgeSink`、`TracingBridgeReport` 和 `run_tracing_bridge_smoke`，通过 `tracing-subscriber` Layer 将 tracing span 输出到现有 OTel-style JSONL span sink，将 tracing event 的 `audit_action`/`audit_outcome` 映射为 `AuditEvent`，并从 tracing fields 提升 request/agent/adapter/capability/provider/span 等 `TraceFields`；bridge 会去重 span id，并对 dev console 与 JSONL attributes 中的 token/secret/password/credential 等敏感字段脱敏。`eva observability smoke` 新增 `--tracing-sink jsonl|dev-console`，默认 JSONL，同时在 JSON 输出追加 `tracing_bridge` report；CLI runtime marker 增加 `tracing_subscriber_bridge_v1.16.2`。该基线仍不实现 OpenTelemetry SDK exporter、生产 metrics exporter、retention/rotation 或 db sink。 | `cargo test -p eva-observability tracing_bridge -- --nocapture`; `cargo test -p eva-cli observability_smoke_writes_backend_and_reports_degraded_mode -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture` | Done |
| V1.16.3 | 2026-07-10 | OpenTelemetry SDK exporter 与 metrics exporter | `eva-observability` 新增 `OpenTelemetryExporterConfig`、`OpenTelemetryDropPolicy`、`OpenTelemetryExporterReport` 和 `run_opentelemetry_exporter_smoke`，通过 `opentelemetry-otlp` SDK 的 HTTP/protobuf trace 与 metrics exporter 向 `/v1/traces` 和 `/v1/metrics` 发送 smoke 数据；支持 endpoint、auth header、batch size、timeout、drop-new/drop-oldest 和 max metric labels。`MetricLabels::limited` 保持标签裁剪顺序稳定；collector 不可用时 exporter report 标记 degraded，但 CLI 命令仍成功返回。`eva observability smoke` 新增 `--otel-endpoint`、`--otel-auth-header`、`--otel-batch-size`、`--otel-timeout-ms`、`--otel-drop-policy` 和 `--otel-max-metric-labels`，未传 endpoint 时保持本地 JSONL smoke 且 `otel_exporter:null`；CLI runtime marker 增加 `opentelemetry_sdk_exporter_v1.16.3`，`eva-config` 和 JSON Schema 增加 `observability.otel_exporter` typed config。该基线仍不实现 retention/rotation 或 db sink。 | `cargo test -p eva-observability opentelemetry_exporter -- --nocapture`; `cargo test -p eva-config observability_exporter -- --nocapture`; `cargo test -p eva-config load_eva_config_accepts_sample_config -- --nocapture`; `cargo test -p eva-cli observability_smoke_writes_backend_and_reports_degraded_mode -- --nocapture`; `cargo test -p eva-cli version_text_and_json_report_v1115_cli_runtime_commands_alpha -- --nocapture`; `cargo clippy --workspace --all-targets -- -D warnings` | Done |

## 跨版本依赖图

| 后续能力 | 必须先完成 | 原因 |
| --- | --- | --- |
| 真实 Lua VM | V1.6 durable task/audit | Lua 超时、取消、失败和 generation 切换需要可恢复证据。 |
| 真实 Adapter/MCP/Skill | V1.6 audit、V1.9 policy 基础 | 外部进程和协议调用必须先有审计与权限门禁。 |
| Memory durable context | V1.6 durable backend、V1.7 Lua context | 记忆持久化要先有存储，注入 Lua 前要有安全上下文边界。 |
| 硬件真实 driver | V1.9 policy、V1.9 observability | 真实设备 I/O 涉及 OS 权限和审计，不能只靠 V1.3 plan boundary。 |
| Restore/upgrade apply | V1.6 durable state、V1.9 policy、V1.10 backup signature | 高风险 mutation 必须可锁定、可审计、可回滚。 |
| 签名安装器发布 | V1.10 apply/supervisor | 安装器和升级链路需要真实 runtime apply/handoff 证据。 |

## 统一验收命令

每个小版本完成时至少运行：

```powershell
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo run -- --version
cargo run -- release check --output json
./scripts/validate-i18n.ps1
```

当版本涉及对应能力时追加：

| 能力 | 追加验证 |
| --- | --- |
| Durable runtime | durable store restart smoke；corrupt store recovery test；dead-letter redrive test |
| Lua VM | sandbox escape test；timeout/memory limit test；generation swap/rollback smoke |
| Adapter/MCP/Skill | fake stdio/http provider；fake MCP server；workflow artifact isolation test |
| Policy/Discovery/Memory/Observability | schema invalid fixture；policy denial fixture；slow discovery source；memory redaction；audit export |
| Hardware/Backup/Lifecycle | hardware simulator suite；restore apply dry-run/apply pair；supervisor handoff rollback |
| Release/CLI | installer smoke；signature/provenance check；public JSON contract diff |

## 文档更新规则

| 触发条件 | 必须更新 |
| --- | --- |
| 小版本计划开始 | 本文对应迭代表状态从 `Planned` 改为 `In Progress`，补负责人和目标 PR。 |
| 小功能完成 | 本文状态改为 `Done`，在未完整实现清单中移除或降级对应缺口。 |
| 命令行为变化 | 使用手册、quickstart、release notes、兼容性策略和 CLI README。 |
| 新增配置字段 | JSON Schema、config README、项目配置方案、manifest 示例。 |
| 新增发布文档 | `docs/_i18n/manifest.json`、中英文文档入口、网站文档卡片。 |
