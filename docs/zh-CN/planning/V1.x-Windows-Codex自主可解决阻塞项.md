# V1.x Windows 下 Codex 自主可解决阻塞项

更新时间：2026-07-27

代码快照：main / c3876582960fcb60218820e6cf11d98926d7ce71

环境快照：Windows 10.0.26200、x86_64-pc-windows-gnu、Rust/Cargo 1.95.0、Windows PowerShell 5.1。当前进程用户为 DESKTOP-TCBDLI7\\admin，属于本机 Administrators 组但使用未提升的 Medium integrity token（IsAdministrator=False）。

本文是对 EXT-01 Windows 子项的自主性评估，不是生产验收记录。本文没有执行 Windows SCM 写操作、服务安装、服务删除或系统重启。

## 1. 目的与口径

EXT-01 在项目计划中的定义是“受控 Windows/Linux/macOS service host”，解除条件包括 destructive service lifecycle、crash、reboot 和 finally cleanup。Windows 本机结果只能作为 EXT-01 的平台子证据，不能单独代表三平台生产闭环。

“自主解决”按以下三类区分：

| 分类 | 含义 |
| --- | --- |
| A：可自主执行或本机闭环 | Codex 可以在仓库和已授权 Windows 主机上完成代码、测试、脚本和本机 transcript；结果必须标注 Windows/local，不能扩大为三平台 evidence |
| B：可自主推进 | Codex 可以补齐实现、harness、validator 和负向测试，但完成判定仍依赖受控 host、外部 authority 或 production gate |
| C：权限之外的外部前置 | 需要组织提供的主机、身份、凭据、信任策略或跨平台环境，单纯提高当前进程权限不能解除 |

## 2. 当前仓库和主机证据

### 2.1 已确认的代码边界

| 项目 | 当前状态 | 证据 |
| --- | --- | --- |
| Windows Service adapter | 代码已存在，覆盖 install/status/start/stop/restart/uninstall、幂等和 drift 拒绝 | crates/eva-lifecycle/src/windows_service.rs；实施计划 W2-L03 |
| Windows direct service entrypoint | 代码已存在，SCM 直接托管当前 daemon 进程，不再二次 spawn | 实施计划 W2-L07 |
| Windows process/Job cleanup | 代码和仓库回归已覆盖 Job Object、后代清理、PID/start-token fence | 实施计划 W3-L03 至 W3-L05 |
| Windows run-as boundary | 当前只允许请求账户与 daemon primary token 同 SID 的 same-service-token；不同 SID 在 spawn 前拒绝 | crates/eva-adapter/src/process_backend.rs |
| Windows credential authority | 生产默认是 FailClosedCredentialVault；仓库没有可直接宣称生产完成的 Windows OS/KMS vault | crates/eva-adapter/src/credential_vault.rs |

### 2.2 本机验证

只读 Windows SCM 回归已通过：

    cargo test -p eva-lifecycle windows_service::tests::native_read_only_query_reports_event_log_running -- --exact --nocapture

结果：1 passed、0 failed。该测试只查询 EventLog，不改变 SCM。

release check 的 Windows 输出也已读取：

    cargo run -q -- release check --target windows --output json

结果为 exit code 0、status=ready，但 closure.status=ready_with_external_blockers。外部阻塞列表仍包含 platform_service_manager_test_environment；Provider gate 仍记录 provider.os_process_supervisor:not_claimed。这里的 ready 只表示 alpha 内部门禁通过，不表示 EXT-01 已解除。

仓库当前没有用于 EXT-01 的真实 Windows service lifecycle harness。现有唯一服务名探针被标记为 ignored，因为它会修改 Windows SCM；它只能验证 create/delete 权限和清理边界，不能替代完整的 create→start→restart→stop→delete、crash/reboot 或 production transcript。

## 3. A 类：在明确授权后可自主执行

### A-01 Windows SCM 本机生命周期

在获得提升的管理员 token、唯一服务命名空间和 finally cleanup 授权后，Codex 可以：

1. 补齐 service-aware fixture，使服务入口使用当前 daemon 的 canonical executable、argv、working directory 和 identity digest。
2. 执行唯一服务的 create、status、start、restart、stop、uninstall，并捕获结构化 stdout、stderr、exit code、服务状态和清理结果。
3. 对重复 install/start/stop/uninstall、binary/account drift、权限拒绝、服务消失和 pending delete 生成负向 evidence。
4. 将 transcript 标为 local/windows，并绑定 commit、host、executor、时间戳和摘要。

这可以推进或关闭 W2-L03 的 Windows 本机子项，但只有在受控 host 上产生可回读 transcript 后，才能进入 W2-L10 的 production evidence。

### A-02 Windows Job 和 provider cleanup 回归

**状态：已完成（repository/local Windows，2026-07-27）**

W3-L03 至 W3-L05 的 Windows 进程边界已经有实现和仓库测试。Codex 可以在本机继续执行真实后代进程、owner crash、强制终止、PID reuse 防误杀和 daemon orphan scan，并确认 Job 关闭后没有残留。

这类结果可以关闭对应的 repository/local 验证债务，但不能替代 W3-L11 所需的受控身份 host 和三平台 production evidence。

完成证据：

    cargo test -p eva-adapter process_backend::tests:: -- --test-threads=1 --nocapture

结果为 12 passed、0 failed、3 ignored helper。成功场景覆盖真实 PID/start token/Job identity、幂等终止、后代回收、snapshot cleanup、PID reuse 拒绝、legacy identity fail closed、same-SID service token 运行，以及未知/不同 SID 和 Unix identity 在 spawn 前拒绝。

    cargo test -p eva-runtime recovery::tests:: -- --test-threads=1 --nocapture

结果为 23 passed、0 failed，覆盖 provider orphan cleanup、PID reuse/legacy identity 拒绝、restart budget、generation change、task preservation 和恢复审计。

    cargo test -p eva-runtime daemon::tests::daemon_start_recovers_interrupted_provider_process_state -- --exact --test-threads=1 --nocapture

结果为 1 passed、0 failed，证明 Windows 本机 daemon restart 可以回收 interrupted provider process state。以上 evidence 只关闭 A-02 的 repository/local Windows 验证，不声称服务账户 host attestation、真实 vault 或 W3-L11 production evidence 已完成。

### A-03 Windows harness、validator 和续接脚本

Codex 可以实现：

1. 使用临时 VM 或受控工作目录的 destructive harness。
2. 以唯一服务名、唯一项目根和 finally 块隔离每次运行。
3. 在重启前写入不可变的 continuation manifest，重启后由外部任务计划、CI runner 或 VM harness 恢复执行。
4. 校验服务、PID、lease、queued work、orphan process 和 artifact 是否全部归零。

当前交互式 Codex 进程本身不会在主机重启后自动存活；要把 reboot 测试变成真正的 autonomous run，必须先提供外部续接机制。

## 4. B 类：可自主推进但不能单凭权限关闭

| 工作项 | Codex 可以做什么 | 仍缺少什么 |
| --- | --- | --- |
| W2-L08 boot/restart recovery | 完善 recovery 状态机、lease fencing、queued-work/no-double-worker 检查和 Windows continuation harness | EXT-01 受控 host 的真实 crash/reboot transcript |
| W2-L09 destructive harness | 编写 install→uninstall、crash、reboot、finally cleanup 和失败即阻断的脚本 | 可安全回滚的受控 Windows host、VM snapshot 或等价隔离 |
| W2-L10 production service evidence | 实现 transcript digest、subject consistency、readback 和 gate validator | 被接受的 runner identity、evidence executor 和 production gate |
| W3-L08 run-as isolation | 验证 same-SID token、未知/不同 SID 拒绝、目录 ownership 和 ancestor race | 受控服务身份、host attestation、跨身份 policy；当前代码不支持任意跨账户 token 创建 |
| W3-L09 credential vault | 接入抽象、负向测试、脱敏和 leak-scan harness | 真实 Windows OS/KMS vault authority、密钥范围、轮换/撤销 policy |
| W3-L11 multi-process evidence | 生成 PID、token、Job、crash-loop、orphan scan 和 secret-zero 的证据格式 | EXT-01 host、真实 vault、三平台 runner 和 production readback |

## 5. C 类：权限之外的外部前置

### 5.1 EXT-01 的三平台范围

EXT-01 本身不是“当前 Windows 机器可用”这一单项条件。解除标准同时要求 Windows、Linux 和 macOS 的真实 service lifecycle、权限拒绝、crash/reboot recovery 和 finally cleanup。即使 Windows 全部通过，也只能解除 Windows 分支，不能把 EXT-01 整体标记为 Complete。

### 5.2 身份、凭据和信任根

管理员权限只授予本机操作能力，不等于：

- 获得可接受的 service identity 或域账户；
- 获得 Windows Credential Manager、DPAPI、KMS/HSM 的生产 authority；
- 获得 CI OIDC、runner attestation 或 evidence signing key；
- 获得生产 gate 对 transcript 的批准；
- 获得产品/组织对跨账户 run-as 和 destructive reboot 的 policy 决策。

当前生产 vault 默认 fail closed，故不能用本机环境变量或 MemoryCredentialVault 伪造生产凭据证据。

### 5.3 破坏性操作的持续性

服务删除、进程强杀和重启会改变主机状态。即使用户明确授权这些动作，也需要：

- 可回滚的独立 VM 或专用测试主机；
- 唯一 service/project/artifact namespace；
- 重启前后都可访问的证据目录；
- 外部 continuation driver；
- finally cleanup 失败时保留现场并使 job 失败的策略。

## 6. 最小授权和环境清单

要开始 Windows 本机 EXT-01 子项，至少需要以下前置条件：

1. 提升后的管理员 token，或明确授予目标测试服务所需的 SCM 权限。
2. 专用 Windows VM/主机，允许安装、启动、停止、删除唯一服务并执行 crash/reboot。
3. 已批准的 daemon service account、service logon right 和测试用 provider account；若只验证 same-SID，必须明确记录这一范围。
4. 重启后续接机制，以及不会随交互式 Codex 进程退出而丢失的 evidence store。
5. 已确定的 transcript schema、runner identity、commit/run binding、digest 和 readback verifier。
6. 若验证 W3-L09，提供获批的 Windows OS/KMS vault authority 和不泄露 secret 的 scan 规则。
7. finally cleanup 失败即阻断，不允许用手工删除或 fake adapter 覆盖失败。

## 7. 推荐执行顺序

1. 在专用 Windows VM 上先运行只读 EventLog 和 host capability probe。
2. 以唯一服务名运行 W2-L03 的 install/status/start/stop/restart/uninstall，并在 finally 中确认 NotInstalled。
3. 加入 daemon direct-entry、queued work 和 no-double-worker 检查。
4. 由外部 continuation driver 执行 crash/reboot，并回读同一 generation 的 evidence。
5. 运行 W3-L08 same-SID/不同 SID 拒绝和 ownership/TOCTOU scan。
6. 接入获批 vault 后再运行 W3-L09 secret redaction/leak scan。
7. 最后由独立 verifier 执行 W2-L10/W3-L11 readback；在此之前不得把 Windows local transcript 写成 production evidence。

## 8. 当前结论

在当前机器和当前 token 下，Codex 只能安全完成仓库内实现、只读 SCM 查询和普通 Windows 测试；不能直接运行服务变更或重启。

如果提供提升权限、受控 Windows host、唯一 fixture、重启续接和 evidence sink，Codex 可以自主完成 Windows 本机的 service lifecycle harness、Job cleanup 和大部分 transcript 生成。

但“EXT-01 在 Windows 下的阻塞已解决”只有在受控 host 的真实 transcript、身份/ownership 证据、必要的 vault evidence 和 production readback 全部通过后才成立；“EXT-01 整体已解决”还必须补齐 Linux/macOS 分支。权限是必要条件，不是充分条件。
