# V1.x 生产能力阻塞项

> 更新：2026-07-20
>
> 代码审计基线：`f75a070`（2026-07-18）
>
> 依据：[V1.x 未完整实现功能清单](V1.x未完整实现功能清单.md) 与 [V1.x 真实运行时能力补齐实施计划](V1.x真实运行时能力补齐实施计划.md)

## 1. 文档目的

本文只记录会阻塞 V1.x 生产能力交付或生产闭环判定的事项，并给出解除路径。`release check` 返回 `ready` 仅表示默认 alpha 输入没有 required gate 被标为 blocked，不代表生产就绪。当前公开版本仍为 `1.11.5-alpha`；代码中的 V1.12-V1.17 标识是兼容性 evidence ID，不是已发布版本。

阻塞项分为：

- **实现阻塞**：仓库中仍缺少生产运行时、平台集成或闭环代码。
- **环境阻塞**：需要平台、硬件、数据库、凭据、外部服务或仓库权限才能实施和验收。
- **证据阻塞**：代码存在，但缺少可信真实执行 evidence，不能进入生产 gate。
- **决策阻塞**：生产范围、协议、信任根或运维策略尚未批准，继续实现会产生不稳定接口。

当前实施计划共有 123 个逻辑项：52 个已完成、11 个验证中、60 个已阻塞。W0 与 W1 已 Complete；其余 Work Package 的阻塞必须按逻辑 ID 和 EXT/DEC 前置项跟踪。

## 2. 总体阻塞摘要

| 范围 | 已落地状态 | 当前阻塞 | 直接前置 |
| --- | --- | --- | --- |
| W0 Evidence Integrity | 10/10 已完成；真实 capture、manifest、readback、coverage/freshness/trusted-executor policy 已接线 | 新 tag workflow 和各生产域 measurement 尚未齐全；这些属于 W10 输入，不再作为 W0 实现缺口 | 各生产域 evidence producer |
| W1 Background Runtime | 12/12 已完成；后台 daemon/worker、heartbeat、effect ledger、recovery、drain 和三平台进程 evidence 已完成 | OS service 托管和平台开机恢复归 W2；外部非幂等系统仍需查询/幂等协议 | W2、外部 provider contract |
| W2 Platform Service | 4 已完成、3 验证中、3 已阻塞；三平台 Adapter、Service CLI 与 direct daemon service entrypoint 代码已存在 | 受控 host transcript、boot/restart recovery、destructive harness 和 production gate | EXT-01 |
| W3 Provider Supervision | 7 已完成、3 验证中、1 已阻塞；真实 PID/process group/Job、restart、跨进程 admission、W2 service identity、run-as、credential session 与 daemon-owned drain lifecycle 代码边界已接线 | EXT-01 原生 identity/vault authority evidence、ownership/TOCTOU/leak evidence 和三平台 production evidence | EXT-01 |
| W4 MCP Transport | 1 已完成、2 验证中、7 已阻塞；production transport 配置、HTTPS/TLS connector 与 HTTP framing 仓库内边界已接线 | W4-L02 production timeout/path evidence、Streamable HTTP/SSE 数据面、真实 abort、外部 server compatibility | W3-L10、W4-L04、EXT-03 |
| W5 Backup/Handoff | 12/12 已阻塞 | 真实 source/catalog、KMS/AEAD、remote readback、可信 plan/target root、service handoff 和 drill | W2、W3、EXT-02、EXT-08 |
| W6 Config/Discovery | 10 已完成、1 已阻塞 | 仅剩 Registry TLS/auth/protocol/pagination client 和真实 registry evidence | DEC-02 |
| W7 Hardware | 1 已完成、11 已阻塞 | 真实 permission、USB/serial、driver lifecycle、OS hotplug、I/O/reconnect 和 fixture evidence | EXT-04 |
| W8 Memory/Observability | 2 已完成、3 验证中、7 已阻塞 | 真实 retrieval commit/retry、export queue/degrade、database sink、retention 和生产 evidence | EXT-05、EXT-09 |
| W9 Distribution | 5 已完成、7 已阻塞；三平台无凭据 metadata/validator evidence 已完成 | signing/notarization、attestation、repository upload 和 post-upload clean install | EXT-06、EXT-07 |
| W10 Production Closure | 11/11 已阻塞 | ProductionScope、closure manifest、trust/coverage verifier、CI assemble、publication decision 和 E2E | DEC-01；声明范围内 W0-W9 evidence |

## 3. 已解除的旧阻塞

以下内容已经有代码与对应测试/evidence，不能继续作为当前阻塞描述：

- W0 的真实 argv capture、readback、coverage、freshness 和 trusted-executor policy。
- W1 的后台 daemon、真实 task worker、heartbeat、claim、effect ledger、shutdown/drain 和三平台进程级 evidence。GitHub Actions run `29542323581` 的 Ubuntu、Windows、macOS 与 aggregate job 均成功。
- W3 的真实 PID/process-group/Job cleanup、durable restart 和跨进程 admission。GitHub Actions run `29597430669` 的 Linux、macOS、Windows admission evidence 均成功。
- W6 的分层配置、watcher/preflight、原子 generation swap、崩溃恢复、真实 PATH probe、持久 TTL cache 和 mutation policy inventory。
- W8 durable operator path 的 demo seed 已移除；durable schedule claim/reclaim 已完成，retrieval worker 与 runtime-owned OTel lifecycle 已进入验证。
- W9 canonical package metadata、Homebrew/Winget/Apt 生成和无凭据 validator。GitHub Actions run `29584854839` 已生成并上传三平台 W9 measurement。
- W2-L06 Service CLI 的代码与 JSON 契约：`eva service install/status/start/stop/restart/uninstall` 已接线，Fake 明确受 `--dev` 限制并以项目/服务隔离状态支持跨 CLI 进程生命周期，host-kind mismatch 与 mutation inventory 门禁已有回归；真实平台生命周期仍由 W2-L03 至 L05、W2-L09/L10 负责。
- W2-L07 daemon service entrypoint 的仓库内代码与契约：生产 Adapter 安装 canonical、无 shell 的隐藏入口，service manager 直接启动当前 daemon 进程而不二次 spawn；可执行文件、native argv、工作目录和 service identity digest 绑定并对 drift fail closed。Unix signal/Windows SCM stop 只设置原子 token，daemon 随后复用既有 generation-bound drain/shutdown 事务，PID、state 与 lease owner 保持同一进程身份；新 control request 绑定 lease generation，service-stop 会消费同代落盘 shutdown，并隔离跨代残留，避免下一代 daemon 被旧请求重放。以上是本地回归边界，不替代 W2-L03 至 L05、W2-L09/L10 的三平台真实 host transcript。
- W4-L01 的 MCP production transport 配置合同：新增 canonical `streamable_http`（保留 `http` legacy alias）、HTTP(S) endpoint/origin 规范化、显式 trust-root/client-auth 引用、bounded same-origin redirect 与 allowed-origin policy；production HTTPS 缺 trust/origin 时 fail closed，明文 HTTP 拒绝 TLS-only policy，未知 scheme、跨 origin redirect、stdio/HTTP 联合字段、未知 manifest 字段、歧义或大小写重复 header、控制字符和保留 header 均在 I/O 前拒绝。typed config、manifest schema/projection、脱敏 Debug/audit 与 v4 manifest digest 已接线；完整数据面仍归 W4-L03 及后续项。
- W4-L02 的 HTTPS/TLS connector 仓库内实现：rustls 已接入系统/自定义 PEM trust root、证书链、hostname/SAN、SNI、有效期与 mTLS client cert/key 校验；每次调用解析 `env:`/已声明 `vault://` 材料并在 release 前销毁 TLS transport/material，稳定区分 connect/handshake/read/write 错误且 Debug/audit 脱敏。`file:` 仅能从 canonical 受控项目根读取，拒绝路径穿越、静态 symlink/reparse、Windows ADS、非普通文件和超限材料；本地 HTTPS server 已实跑 initialize、initialized、tools/list、tools/call。该项仍处于验证中，不能替代下述生产 deadline、TOCTOU 与原生平台 evidence。
- W4-L03 的 HTTP framing 仓库内实现：严格解析 CRLF 状态行/header 并限制 64 KiB header/chunk metadata，支持 Content-Length、chunked（含 extensions/trailers）、HTTP/1.0/close-delimited、keep-alive、1xx interim、202/204/205/304 无体状态和 JSON content-type；拒绝 CL/TE 冲突、重复或非法 header、缺少 framing、非法 chunk、禁止 trailer、压缩编码和超限 body。通知可在 202 无 body 时立即返回，固定长度/chunk 终止后不等待 EOF；分片、真实 keep-alive socket、chunked、oversize、invalid header/body 共有 51 项 MCP 测试通过。该项已完成，后续 session/SSE/abort 仍归 W4-L04 及以后。

## 4. P0 阻塞项与解决方案

### B-P0-01 平台 Service Manager 尚未形成生产闭环（W2）

**阻塞原因**：W2-L01/L02/L06/L07 已完成，Windows Service、systemd、launchd Adapter 代码已实现并处于验证中；canonical hidden entrypoint、direct/no-respawn daemon ownership、PID/lease identity、stop-token drain/shutdown 和 drift 拒绝均有仓库内回归。当前仍缺少受控提权 Windows、PID1 systemd Linux、macOS system/GUI host 的真实生命周期 transcript；W2-L08 boot/restart recovery、W2-L09 destructive harness 和 W2-L10 production evidence gate 仍被该环境证据边界阻塞。

**解决路径**：

1. 在 EXT-01 host 上完成 W2-L03 至 L05 的 Windows create/start/restart/stop/delete、Linux root/non-root、macOS system/GUI 生命周期 transcript。
2. 以已完成的 W2-L07 direct entrypoint、service identity 和 W1 lease/recovery 为前置，完成 W2-L08 boot/restart recovery 与 queued-work/no-double-worker 验收。
3. 由 W2-L09 执行三平台 reboot/crash/destructive lifecycle，并以 finally cleanup 失败作为 job 失败条件。
4. 由 W2-L10 回读三平台 transcript，绑定 commit、平台、service identity、executor、时间戳和结果 digest；Fake 或本地 fixture 不得通过 production gate。

**解除标准**：三平台真实 service 生命周期、权限拒绝、崩溃/重启恢复和 finally cleanup 均通过；fake Adapter 不能满足 production evidence。

### B-P0-02 Provider 用户隔离、凭据和长驻所有权未完成（W3）

**阻塞原因**：W3-L01 至 L07 已完成，跨进程 admission 不再是缺口；W2-L07 service identity 前置也已就绪。W3-L08 的代码边界已接线并进入验证中，但 EXT-01 原生 identity evidence 尚未完成：Unix helper 和 Windows same-service-token 回归已覆盖，仍缺受控 Linux/macOS/Windows service host 的真实 uid/gid 与 token 隔离 transcript；macOS 显式切换当前未开放，等待受控 no-suid 或 sandbox 方案，`Current` 仍可用。当前 Windows 只允许与 daemon primary token 同 SID 的账户继承 token，不提供任意跨账户 `CreateProcessAsUserW`/`CreateProcessWithTokenW` 切换；代码对检测到的 non-root supplementary groups fail closed，但仍缺 host 对组状态的 attestation，root 创建目录的 UID/GID handoff 也未由 host policy 证明。W3-L09 已接入 vault/session trait、显式 Memory 测试 vault 和生产 fail-closed default：stdio/MCP stdio/Skill 使用最小环境，HTTP/MCP HTTP 从 session 解析凭据；Skill workspace/artifact 目录和最终文件已做 no-follow、owner/regular-file 检查与句柄读取，且 transport/report Debug、audit、artifact、stream、error 均有脱敏边界。该项仍处于验证中，因为仓库尚未接入真实 OS secret store/KMS authority，也缺 EXT-01 原生 identity、跨 UID ownership、目录 ancestor race/完整 TOCTOU 与生产 leak-scan evidence。W3-L10 daemon-owned supervisor/drain 已接线并处于验证中；仍需验证其 renew/restart/shutdown/generation recovery 与 native evidence，W3-L11 负责三平台 production evidence；Linux/macOS 原生 crash-loop/daemon-kill evidence 尚未齐全。

**解决路径**：

1. 在 EXT-01 受控 host 回读 W3-L08：完成 Unix uid/gid、supplementary-group、目录 ownership 与 Windows same-SID service-token 的原生 transcript；macOS 显式切换当前未开放，等待受控 no-suid 或 sandbox 方案，`Current` 仍可用；禁止身份提升并验证跨账户访问隔离，未知或不同 SID 在 spawn 前拒绝。
2. 接入 OS secret store/KMS-backed vault session，secret 只通过引用进入 provider，禁止进入日志、JSON 和 artifact；当前仓库已接入 session trait、Memory 测试实现和生产 fail-closed default，但真实 authority 与 production leak-scan 仍待外部证据。
3. 校验并收口已接入的 daemon-owned provider lifecycle、renew、restart、shutdown 和 generation recovery（含 bounded drain 与剩余 deadline 传递），复用已完成的 process table、group/Job cleanup 和 admission，并补齐 native evidence。
4. 在受控 identity host 上执行 daemon kill、provider crash-loop、PID reuse、vault redaction 和 orphan scan，生成可回读 evidence。

**解除标准**：故障后无 orphan process，run-as 不越权，真实 vault secret 零泄露，daemon restart 保持 budget/admission/ownership，三平台 production evidence 可复验；在真实 OS/KMS authority、EXT-01 identity host 和完整 Skill ownership/TOCTOU evidence 到位前不得标记完成。

### B-P0-03 MCP 生产传输未完成（W4）

**阻塞原因**：现有 stdio 和 legacy 明文 `http://` JSON-RPC 具备 allowlist、timeout/output limit 和 session cleanup；W4-L01 production transport 配置合同、W4-L02 HTTPS/TLS connector 仓库内实现与 W4-L03 HTTP framing 均已接线，其中 W4-L01/W4-L02 仍在验证中。W4-L02 已覆盖系统/自定义 trust root、证书链、hostname/SNI、有效期、mTLS、受控 `file:`/`env:`/`vault://` 材料与稳定错误分类，但 DNS 解析发生在 connect deadline 建立前，handshake/read/write 仍是可被 slow-drip 重置的单次 socket I/O timeout，而不是全请求单调 deadline；写超时仅有合成错误映射。静态 component pre/post check 与 final no-follow 不能完全关闭祖先目录替换 TOCTOU，完整修复仍需 Unix `openat`/`openat2` 逐级 dirfd 和 Windows handle-based traversal/final-path 验证；Windows 无 symlink 权限时测试会跳过，Apple cross-target 缺本机 `cc`，Linux 原生目标亦未在当前 Windows host 实跑。W4-L03 已完成 Content-Length/chunked/keep-alive/202/204/invalid-header framing，但 W4-L04 之后仍缺 Streamable HTTP session、SSE、真实 streaming abort 和外部 server matrix，compatibility 也仍是 fixture。

**解决路径**：

1. 收口 W4-L02 production evidence：让同一单调总 deadline 覆盖 DNS、connect、handshake、write 与 read，补真实阻塞 write/slow-drip 回归；使用 Unix `openat`/`openat2` 与 Windows handle-based traversal/final-path 验证关闭 ancestor replacement TOCTOU，并在原生 Linux/macOS/Windows runner 回读 reparse/TLS evidence。`file:` trust root 不得回退到当前工作目录或内联 PEM。
2. W4-L03 已完成完整 HTTP framing；继续实现 Streamable HTTP session、SSE 增量解析、背压和断流清理。
3. 让 W3 supervisor 成为 MCP session owner，abort 必须终止真实 I/O 并释放资源。
4. 固定 EXT-03 server 名称、版本和 transport，实跑 stdio/HTTPS/auth/stream-abort matrix，并上传 measurement evidence。

**解除标准**：TLS/auth 错误稳定，stream abort 无资源泄漏，选定外部 server 实跑通过，静态 all-true fixture 不能满足 production gate。

### B-P0-04 备份恢复与升级交接缺少生产信任边界（W5）

**阻塞原因**：Alpha 已有本地 archive、plan-gated file mutation、rollback 和 operator confirmation，但 W5-L01 至 L12 均未完成。真实数据选择、持久 snapshot catalog、KMS 签名/AEAD、remote readback、可信 plan、target-root policy、crash-atomic restore 和 service-level handoff 尚未形成闭环。

**解决路径**：

1. 完成 EXT-02 的 KMS/HSM、key custody、rotation/revocation 和 plan-trust 决策，以及 EXT-08 的 remote DR store 选择。
2. 实现真实 source selector、atomic backup commit 和持久 snapshot catalog。
3. 使用 KMS signature 与 AEAD envelope，绑定 key version、artifact digest、issuer、expiry、target policy 和 nonce。
4. 实现受限 target root、crash-atomic restore、remote readback 和 W2-backed blue-green handoff。
5. 执行真实 dataset、KMS rotation/revoke、remote readback、candidate failure 和 pointer 不漂移 drill。

**解除标准**：未签名、过期、重放或越权 plan 在 mutation 前拒绝；失败 restore/upgrade 可恢复一致数据和 pointer，真实 artifact/service/remote evidence 可复验。

### B-P0-05 Production Closure 尚未实现（W10）

**阻塞原因**：W0 已完成 evidence 协议与 production policy 基础，但 DEC-01 尚未批准 production scope，W2-W9 的声明范围也没有全部真实 measurement。W10-L01 至 L11 尚未实现，当前 alpha `ready_with_external_blockers` 不能作为 publication decision。

**解决路径**：

1. 由 DEC-01 明确支持的 OS、service、provider、protocol、restore、hardware、telemetry、security 和 distribution 范围。
2. 实现 versioned ProductionScope、closure manifest、trust/freshness/kind verifier、跨 evidence subject 一致性和 scope coverage。
3. 让 CI 从不可变 artifact 回读并组装 evidence；production profile 拒绝 declaration、fixture、stale、untrusted、缺域和 digest drift。
4. 让 publication 只消费机器可验证 decision，增加 tamper、partial upload、rollback failure 和完整成功 E2E。

**解除标准**：每个 in-scope 能力都有可重放 measurement，gate 可机器判定发布或阻断；blocked decision 不产生 publication side effect。

## 5. P1 阻塞项与解决方案

| 编号 | 当前阻塞 | 已完成基础 | 解决路径与解除标准 |
| --- | --- | --- | --- |
| B-P1-01 / W6 | W6-L09 Registry TLS/auth/protocol/pagination 与 DEC-02 | W6 其余 10 项均已完成 | 先批准 endpoint schema、TLS trust、auth ref、pagination 和 compatibility policy；实现可取消 client、secret redaction、durable cache 接线。401/TLS/malformed/timeout/pagination 和真实 registry evidence 通过后解除 |
| B-P1-02 / W7 | 真实 OS permission、USB/serial、driver lifecycle、hotplug、reconnect 和 fixture evidence | Typed hardware config、simulator safety、permission denial、lease cleanup 和 alpha hotplug smoke | 固定 EXT-04 fixture/runner/权限，先完成 USB/serial baseline，再扩展 socket/BLE/vendor；覆盖 permission deny、I/O、disconnect/reconnect、watcher crash 和无 raw-handle escape |
| B-P1-03 / W8 | 真实 retrieval commit/retry、export queue/degrade/flush、database sink、retention 和生产 evidence | Demo seed 已移除；schedule store 已完成；retrieval worker 和 runtime-owned OTel lifecycle 在验证中 | 完成 EXT-05/09 决策与环境，接真实 source/index、idempotent retry/dead-letter、collector degradation、database migration/sink 和 retention worker；重启、故障、负载/cardinality evidence 通过后解除 |
| B-P1-04 / W9 | Signing/notarization、final artifact attestation、repository upload 和 post-upload install | Canonical metadata、Homebrew/Winget/Apt 生成、CLI 原子输出和三平台无凭据 validator 已完成 | 接入 EXT-06 signer/OIDC/KMS 和 EXT-07 repository/clean runner；完成 Authenticode、codesign/notarize、Apt signing、SBOM/attestation、幂等上传、公开下载 digest 与 clean install |

## 6. 外部与决策前置条件

以下编号与实施计划保持一致，禁止重新合并或复用：

| ID | 当前需要 | 主要阻塞 |
| --- | --- | --- |
| EXT-01 | 受控 Windows/Linux/macOS service host，可执行提权、登录、crash、reboot 和 finally cleanup | W2-L03 至 L05、W2-L08 至 L10、W3-L08/L11 |
| EXT-02 | KMS/HSM、key custody、rotation、revocation 和 plan-trust policy | W5-L04 至 L12 |
| EXT-03 | 命名的外部 MCP server、固定版本、TLS/auth 与可重复环境 | W4-L10 及 compatibility evidence |
| EXT-04 | 真实或虚拟 hardware fixture、vendor SDK、平台权限和破坏性测试边界 | W7-L02 至 L12 |
| EXT-05 | Production database、schema、凭据、retention 和运维策略 | W8-L09 至 L12 |
| EXT-06 | CI OIDC、Windows/macOS/Linux signing 与 notarization identity | W9-L06 至 L10 |
| EXT-07 | Homebrew/Winget/Apt 仓库权限和 clean-machine runner | W9-L11/L12 |
| EXT-08 | 命名的 remote disaster-recovery store 与访问身份 | W5-L06/L12 |
| EXT-09 | 真实 OTel collector 与命名的 production retrieval source | W8-L05、L08、L12 |
| DEC-01 | 获批的 production scope | W10-L01 至 L11 |
| DEC-02 | Registry protocol、认证方式与信任根 | W6-L09 |

外部条件未满足时，可以继续完成不依赖凭据的 parser、validator、fake/fixture 单测和本地 dry-run，但这些不能替代 production evidence。代码已存在但缺环境 transcript 的项目应标为“验证中”，不能重新描述为“未实现”。

## 7. 推荐解除顺序

1. 以已完成的 W0/W1、W2-L06/L07 为基座，在 EXT-01 上完成 W2-L03 至 L05 的受控 host evidence，再完成 W2-L08 boot/restart、W2-L09 destructive harness 与 W2-L10 production gate。
2. 在 EXT-01 上完成 W3-L08/L09 的 identity、vault、ownership/TOCTOU 与 leak evidence，并复核已接入的 W3-L10 daemon-owned lifecycle；随后由 W3-L11 收口三平台 native production evidence。
3. W3-L10/W3-L11 验证完成后推进 W4；W2/W3、EXT-02/08 到位后推进 W5。
4. DEC-02 确定后直接关闭 W6-L09；W7、W8、W9 分别按 EXT-04、EXT-05/09、EXT-06/07 并行推进。
5. DEC-01 和全部 in-scope producer/evidence 就绪后实现 W10，并让 publication 只服从 production decision。

## 8. 通用验收与验证

每个阻塞项解除时必须同时提供目标行为、兼容契约、负向测试和环境 evidence，并保持以下基线通过：

```powershell
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
./scripts/validate-cli-json-contracts.ps1
./scripts/build-site-i18n.ps1
./scripts/validate-i18n.ps1
./scripts/validate-version-management.ps1
cargo run -q -- version --output json
cargo run -q -- release check --output json
```

当前默认 `release check` 的本地事实是 `status:"ready"`、`warning_gates:7`、`closure.status:"ready_with_external_blockers"`。生产闭环还必须证明 commit/artifact digest、执行环境、executor、时间戳、scope coverage、gate 结果和 remediation 可追溯；未满足前版本保持 alpha，不得将内部 evidence ID 宣称为已发布版本。
