# Eva-CLI V1.5 发布验收记录

日期：2026-07-06
发布标签：`v1.5.0`
Release 地址：<https://github.com/Yetmos/Eva-CLI/releases/tag/v1.5.0>

## 结论

Eva-CLI V1.5.0 已经作为 GitHub 托管的源码发布公开存在。公开 annotated tag
`v1.5.0` 已在 GitHub 上存在，并解析到 commit
`74d85e7da58ac40ef5d30b38e2844dee503a44c0`。

本次验收不重新创建或改写该 tag。项目发布方案要求 GitHub Release 已经可见后保持
公开 tag 不可变。tag 之后进入 `main` 的文档和 Pages 修复继续由主分支跟踪，不改变
V1.5.0 不可变源码发布记录。

## 发布证据

| 证据 | 结果 |
| --- | --- |
| 根 package version | `1.5.0` |
| workspace package version | `1.5.0` |
| 本地 tag | `v1.5.0` annotated tag 已存在 |
| 远端 tag | `origin/v1.5.0` 已存在 |
| tag 解析 commit | `74d85e7da58ac40ef5d30b38e2844dee503a44c0` |
| GitHub Release 页面 | 已发布到上方 Release 地址 |
| 源码压缩包 | GitHub Release 页面提供自动生成的源码压缩包 |
| 网站部署 | GitHub Pages 已在 <https://www.eva-cli.com/> 可访问 |

## 本地门禁结果

以下检查已于 2026-07-06 通过：

```powershell
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo build --release
./scripts/build-site-i18n.ps1
./scripts/validate-i18n.ps1
cargo run -- release check --output json
cargo run -- release security --output json
cargo run -- release perf --output json
cargo run -- release migration --output json
```

Release 命令摘要：

- `release check`：`ready`，0 个 blocking gate。
- `release security`：`reviewed`，0 个 blocking finding。
- `release perf`：`within_budget`，0 个 over-budget check。
- `release migration`：`compatible`，V1.4 到 V1.5 无破坏性变更。

## 说明

V1.5 仍是源码发布。签名安装包、包管理器发布、破坏性 restore apply 和真实
supervisor handoff 仍属于后续版本范围。
