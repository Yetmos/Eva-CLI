# Eva-CLI V1.5.0 发布验收（历史决策记录）

> 语言：简体中文
>
> 默认发布文档：`docs/en/release/v1.5-release-acceptance.md`
>
> 英文原文：[English](../../en/release/v1.5-release-acceptance.md)

更新日期：2026-07-14

验收日期：2026-07-06

状态：`v1.5.0` 源码发布的不可变证据记录；不是当前发布健康报告。

## 决策

Eva-CLI V1.5.0 被验收为 GitHub 托管的源码发布。公开 annotated tag `v1.5.0` 解析到提交 `74d85e7da58ac40ef5d30b38e2844dee503a44c0`，不得移动该 tag 以包含后续文档、workflow、打包或 runtime 变更。

Release 地址：<https://github.com/Yetmos/Eva-CLI/releases/tag/v1.5.0>

![Eva-CLI 发布历史边界](../../../assets/release-history-boundary.zh-CN.svg)

## 版本上下文

| 信号 | 含义 |
| --- | --- |
| V1.0 | 内部里程碑提交 `437087c`；不存在 `v1.0` tag |
| V1.4 | 内部 lifecycle 里程碑提交 `909ab07`；不存在 `v1.4` tag |
| V1.5.0 | annotated tag `v1.5.0`，解析到提交 `74d85e7da58ac40ef5d30b38e2844dee503a44c0` |
| 当前 `main` | Cargo 和 CLI 报告 `1.11.5-alpha`；后续 V1.12-V1.17 字符串是 legacy evidence ID，不是发布 tag |

## 已捕获的发布证据

下表记录 2026-07-06 验收时观测到的证据。外部 URL 和服务是历史观测，不是持续可用性检查。

| 证据 | 已捕获结果 |
| --- | --- |
| 根 package 版本 | 验收源码状态中为 `1.5.0` |
| Workspace package 版本 | 验收源码状态中为 `1.5.0` |
| 本地 tag | annotated `v1.5.0` tag 存在 |
| Tag 目标 | `74d85e7da58ac40ef5d30b38e2844dee503a44c0` |
| GitHub Release | 在上方 Release 地址可见 |
| 源码压缩包 | GitHub 生成的源码压缩包可从 Release 页面获取 |
| 文档站点 | 在 <https://www.eva-cli.com/> 观测到 GitHub Pages |

本次验收没有重建、强制更新或重新指向 tag。

## 已捕获的本地门禁

以下命令被记录为已于 2026-07-06 针对当时 V1.5 源码状态通过：

```powershell
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo build --release
./scripts/build-site-i18n.ps1
./scripts/validate-i18n.ps1
cargo run -- release check --output json
cargo run -- release security --output json
cargo run -- release perf --output json
cargo run -- release migration --output json
```

已记录的 release 命令摘要：

| 命令 | 已捕获结果 | 解读 |
| --- | --- | --- |
| `release check` | `ready`，0 个 blocking gate | 没有 required V1.5 gate 对象被标记为 blocked |
| `release security` | `reviewed`，0 个 blocking finding | 编译进 V1.5 的 finding 中没有 blocking 项 |
| `release perf` | `within_budget`，0 个超预算检查 | 固定源码发布 smoke 观测值在预算内 |
| `release migration` | `compatible`，无声明的破坏性变更 | V1.5 兼容性声明报告无破坏性变更 |

这些摘要不能追溯证明当前 `main`、外部服务、生产凭据或平台集成。证据语义见 [发布加固证据模型](V1.5发布加固.md#证据模型)。

## 不可变边界

- `74d85e7` 之后的变更属于后续提交或 tag，不改变本记录。
- `v1.5.0` tag 包含源码发布 checkpoint，不包含后续 GHCR、原生压缩包、evidence bundle、restore-apply 或 upgrade-apply 工作。
- 后续受控 alpha 实现必须从其自身代码和当前 operations 文档说明，不能反向投射到 V1.5.0。
- 在 `main` 上重新运行命令会生成 `1.11.5-alpha` 证据，不是对 V1.5.0 的重新验收。

## 相关参考

- [V1.5 发布加固记录](V1.5发布加固.md)
- [V1.5.0 发布说明](V1.5发布说明.md)
- [V1.5.1 发布说明](V1.5.1发布说明.md)
- [当前 V1.x 未完整实现功能清单](../planning/V1.x未完整实现功能清单.md)
- [当前备份与恢复边界](../operations/备份迁移包与ReleaseSnapshot架构方案.md)
- [当前进程级升级边界](../operations/进程级停机升级架构方案.md)
- [项目发布方案](项目发布方案.md)
