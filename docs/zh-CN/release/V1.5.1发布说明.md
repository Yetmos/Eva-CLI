# Eva-CLI V1.5.1 发布说明

语言：简体中文 | [English](../../en/release/release-notes-v1.5.1.md)

> 历史快照。本页固定描述不可变的 `v1.5.1` tag。

| 字段 | 记录值 |
| --- | --- |
| 日期 | 2026-07-06 |
| 状态 | 正式 patch 版本 |
| Tag | [`v1.5.1`](https://github.com/Yetmos/Eva-CLI/tree/v1.5.1) |
| 提交 | [`968c343`](https://github.com/Yetmos/Eva-CLI/commit/968c3430a9ff) |
| GitHub Release | [已发布](https://github.com/Yetmos/Eva-CLI/releases/tag/v1.5.1) |
| Release workflow | [运行 28772939702](https://github.com/Yetmos/Eva-CLI/actions/runs/28772939702)，成功 |

## 此 tag 包含

- 在不移动既有 `v1.5.0` tag 的前提下，为 release workflow 启用 GHCR 发布。
- package evidence 记录 package digest、tags、source tag、source SHA 和 URL。
- 正式版本可以更新 `latest`，预发布 tag 不更新 `latest`。
- 保持 V1.5 CLI、配置、JSON 和文档路径不变。

## 未包含

此 patch 不新增 runtime 命令、原生二进制归档、签名安装器、系统包管理器 package
或 provenance bundle。

## 复现发布

```powershell
git switch --detach v1.5.1
./scripts/validate-version-management.ps1 -Tag v1.5.1
./scripts/validate-i18n.ps1
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo run -- release check --output json
docker build -t eva-cli:1.5.1-smoke .
docker run --rm eva-cli:1.5.1-smoke --version
```

## 制品

- GitHub Release 提供自动生成的源码归档，上传的 Release asset 为 **0**。
- Actions 保留 `release-evidence-v1.5.1` 和
  `package-evidence-v1.5.1`；它们是有保留期限的 workflow artifact。
- GHCR 中 `ghcr.io/yetmos/eva-cli` 具有 `1.5.1`、`1.5` 和 `latest` tag。
- 此 workflow 未生成原生二进制归档 artifact。

## 当前文档

当前行为以[使用手册](../guide/Eva-CLI使用手册.md)、
[当前能力缺口](../planning/V1.x未完整实现功能清单.md)和
[实施计划](../planning/V1.x真实运行时能力补齐实施计划.md)为准。
