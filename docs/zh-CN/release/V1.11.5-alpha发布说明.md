# Eva-CLI V1.11.5 Alpha 发布说明与 V1.17 同步

状态：alpha 预发布
Tag：`v1.11.5-alpha`

Eva-CLI V1.11.5-alpha 完成 CLI runtime command completion 切片。它补齐 typed
event 发布、Agent lifecycle evidence 和 capability provider routing 的 operator
命令面，同时保持稳定 JSON envelope、trace 字段、exit code 映射和 plan-first
安全边界。

## 重点

- 新增 `eva emit`，可构造 typed `eva-core::Event`，并发布到 in-memory EventBus
  边界或 V1.6 durable backend EventBus log。
- 新增 `eva agent status`、`eva agent drain` 和 `eva agent reload`，复用
  ProjectConfig manifest、`AgentRuntime`、`AgentLifecycle`、`DrainCoordinator`
  和 `GenerationController` 输出 lifecycle、drain 和 generation reload evidence。
- 新增 `eva capability list`、`eva capability probe` 和 `eva capability call`，
  复用 `CapabilityRegistry`、provider selection plan、permission gate、runtime
  policy decision、`CapabilityRouter` 和 `AdapterBackedCapabilityHost`。
- `capability call` 默认 dry-run。只有 `--confirm <request-id>` 与 request id
  匹配时才执行；provider 超出 capability manifest allowlist 会在调用前拒绝。
- 将 release metadata、release readiness docs、i18n manifest 和静态官网内容同步到
  `1.11.5-alpha` 检查点。

## 兼容性

V1.11.5-alpha 对公开 CLI 命令、JSON envelope、exit code 和既有 release evidence
gate 保持增量兼容。既有命令组输出形状不变。新增 Agent 命令只是 lifecycle evidence
边界，不会重启 daemon、修改 live scheduler 或执行真实 hot reload apply。确认后的
capability call 仍是受控 CLI invoke，并继续输出 `mutation_executed:false`。

它仍是 alpha 检查点。真实 database sink、OS provider
supervision、真实硬件接入、生产 daemon 驱动的 hot-reload orchestration、签名安装器、OS
包仓库和生产 signing/attestation credential 仍属于后续 release 范围。V1.15.8 补齐
policy-driven memory redaction/audit JSONL evidence；V1.16.1 补现有 JSONL best-effort
runtime audit wiring；V1.16.2 补 tracing subscriber bridge 到现有 JSONL/dev-console sink；
V1.16.3 补显式 OpenTelemetry SDK OTLP trace/metrics exporter smoke 和 collector degraded report。
V1.16.4 补 JSONL/durable-audit retention、rotation、max-size、corrupt-record policy
和 database policy kind 边界。V1.17.1 将 `run --example basic` parser、runtime glue、
task snapshot 写入和 text/JSON writer 拆入 `run/run_cmd.rs`，但 public text/JSON
contract 不变。V1.17.2 固定 operator execution-state 字段：`capability call`
同时输出 `invocation_executed` 和 `mutation_executed:false`，restore、upgrade 和
hardware 输出顶层 `mutation_executed`。V1.17.3 为 restore、upgrade 和 hardware
高风险 text 输出补 `final_state`、`rollback_path` 和风险摘要。V1.17.4 新增
`contracts/cli-json/` golden subset fixtures、`scripts/validate-cli-json-contracts.ps1`
和 required gate `REL-JSON-CONTRACT-001`，用于阻断 public JSON 字段删除或重命名。
V1.17.5 不新增 runtime marker，而是同步 README、文档入口、使用手册、release notes、
官网 i18n 卡片、生成的本地化 HTML 和 `docs/_i18n/manifest.json`。V1.17.6 新增
`v1x_closure_gate_v1.17.6` runtime marker、required gate
`REL-OBSERVABILITY-POLICY-001` / `REL-V1X-CLOSURE-001` 和 additive
`release check` `closure` JSON，用于汇总 daemon、MCP、provider、restore、
service-manager abstraction、hardware safety、observability policy 和 public JSON
contract gates；生产签名/attestation 凭据、包仓库权限、平台 service-manager 测试环境、
真实/虚拟硬件 fixture 与生产 database sink/retention scheduler 会作为 external blockers
记录。这些仍不等于完整生产观测后端、生产检索调度、真实 OS provider supervisor、
真实硬件驱动或生产发布上传链路。

## 验证

- `cargo fmt --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo test -p eva-cli emit`
- `cargo test -p eva-cli agent`
- `cargo test -p eva-cli capability`
- `cargo test -p eva-cli run_basic_example_json_succeeds`
- `cargo test -p eva-cli task_`
- `cargo test -p eva-cli restore_apply`
- `cargo test -p eva-cli upgrade_apply`
- `cargo test -p eva-cli hardware_commands_report_candidates_and_bind_plan`
- `cargo test -p eva-release`
- `scripts/validate-cli-json-contracts.ps1`
- `scripts/build-site-i18n.ps1`
- `scripts/validate-i18n.ps1`
- `scripts/validate-version-management.ps1 -Tag v1.11.5-alpha`
- `cargo run -- --version`
- `cargo run -- release check --output json`
- `cargo run -- emit /input/user --payload hello --output json`
- `cargo run -- agent status --agent root-agent --output json`
- `cargo run -- capability list --output json`

## 制品

- GitHub Release 源码压缩包。
- release workflow 完成后生成 `release-evidence-v1.11.5-alpha` artifact。
- package 发布成功时，GHCR 容器 package 为 `ghcr.io/yetmos/eva-cli:1.11.5-alpha`。

签名安装器、系统包管理器 package、生产 provider supervision、destructive apply 路径和
provenance bundle 仍属于后续 release 范围。
