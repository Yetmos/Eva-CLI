# V1.x Incomplete Feature Inventory

> Language: English
>
> Published default: `docs/en/planning/v1.x-incomplete-feature-inventory.md`
>
> Translation: [Simplified Chinese](../../zh-CN/planning/V1.x未完整实现功能清单.md)

Updated: 2026-07-14

## Scope

This inventory is a code-backed snapshot of the current `main` branch. It separates callable behavior, library/test-only boundaries, alpha smoke evidence, missing production integration, and external prerequisites. It is not a changelog and does not turn an internal capability marker into a released version.

Completed implementation history belongs in release notes, tags, and Git history. The companion [implementation plan](v1.x-real-runtime-implementation-plan.md) contains only remaining work and dependency order.

## Version and Evidence Semantics

| Signal | Current fact | What it does not prove |
| --- | --- | --- |
| Cargo and CLI version | `1.11.5-alpha` / `V1.11.5-alpha` | A production-ready release |
| Git tags | Tags exist through `v1.11.5-alpha`; there are no `v1.12` through `v1.17` release tags | That later internal markers are published versions |
| `version.data.runtime_mode` | Contains capability/evidence IDs through `v1x_closure_gate_v1.17.6` | A semantic-version sequence or release history |
| `release check` | Reports `status:"ready"`, one warning gate, and `closure.status:"ready_with_external_blockers"` with the default inputs | That Cargo/CI, external services, platform integration, or production credentials were checked during the command |
| Production status | The repository is an alpha implementation with controlled local boundaries | Production daemon, platform, hardware, telemetry, or distribution readiness |

The V1.12-V1.17 labels embedded in `runtime_mode`, release gates, and source identifiers are legacy internal evidence IDs added after the `v1.11.5-alpha` tag. They remain stable for compatibility, but must not be described as released versions.

![V1.x remaining capability map](../../../assets/v1x-remaining-capability-map.svg)

## Status Vocabulary

| Status | Meaning |
| --- | --- |
| Callable | A current CLI/runtime path executes the behavior within its documented boundary |
| Library/test boundary | Code and tests exist, but the production runtime or CLI does not wire the capability end to end |
| Controlled alpha | The path is local, synthetic, simulated, fake, fixture-based, or smoke-only |
| Missing | The required implementation is absent |
| External prerequisite | Hardware, credentials, repository access, platform test infrastructure, or an operational backend is still required |

Security denial is not a missing feature. For example, rejecting an unauthorized provider, MCP tool, Lua host handle, or hardware raw handle is an implemented invariant, not backlog.

## Current Gap Matrix

| Area | Current code boundary | Remaining production gap | Priority |
| --- | --- | --- | --- |
| Release evidence | `release check` aggregates compiled-in alpha checklist objects, one static MCP compatibility fixture, fixed performance observations, and optional operator-supplied evidence files | Bind gates to executed, timestamped CI/platform evidence with provenance; distinguish asserted, fixture, and observed results | P0 |
| Runtime, task, and scheduler | Foreground daemon, filesystem mailbox, durable task/event/provider snapshots, startup recovery classification, retry routing/ACK evidence, and daemon-side Agent control state | Background service, real task/Agent worker consumption, heartbeat/liveness, stale-lock repair, boot recovery, and side-effect recovery | P0 |
| Provider and capability execution | Per-invocation stdio/MCP subprocess runners, process-capable or synthetic Skill runners, plaintext HTTP, allowlists, admission limits, redaction, synthetic supervisor slots, and durable process-table evidence | TLS-capable provider HTTP client, long-lived OS process supervision with real PIDs, kill/restart, cross-process admission, user isolation, credential vault, and restart policy | P0 |
| MCP | Stdio and `http://` JSON-RPC, tool allowlists, timeout/output limits, session cleanup, and a static compatibility fixture | HTTPS/TLS, production Streamable HTTP/SSE transport, streaming data plane, external-server compatibility runs, and daemon supervision | P0 |
| Backup, snapshot, and restore | Three-entry synthetic backup CLI, in-memory snapshot reports, and real plan-gated local copy/replace/delete plus rollback | Real project/data selection, persisted snapshot catalog, production signing/encryption/KMS, remote disaster recovery, trusted plan generation, and target-root policy | P0 |
| Upgrade and service management | Local lock/state/pointer files, binary `--version` probe, in-memory generation model, trait, and fake service-manager Adapter | Windows Service/systemd/launchd Adapters, real candidate Runtime, traffic switch, drain, reboot recovery, and OS rollback | P0 |
| Durable storage | Filesystem EventLog, task/audit/artifact/provider records, redrive, and recovery diagnostics | Transactional cross-process store, production WAL/compaction, ownership/locking across all writers, and recovery of non-idempotent side effects | P0 |
| Configuration and discovery | Fixed `config/eva.yaml`, split YAML roots, limited schema evaluator, manifest/marker projection, source reports, and per-command cache | Profiles/user/env merge, file watcher, atomic live route/config replacement, PATH probing, real registry protocol/auth, and persistent TTL cache | P1 |
| Memory and knowledge | Durable file APIs, locks, TTL GC, rebuild checkpoints, one-shot daemon maintenance, and provider-retrieval library tests | Long-lived maintenance/retrieval scheduling, production data management/query APIs, and removal of CLI demo seeding from real data paths | P1 |
| Hardware | Simulated driver, logical leases, default-deny permission evidence, typed events, and one-shot manifest snapshot hotplug state | Real OS permission checks, USB/serial/BLE/socket/vendor drivers, OS hotplug watcher, raw I/O, and release fixtures | P1 |
| Observability | Selected runtime paths write JSONL; tracing bridge, OTLP trace/metrics export, and retention behavior have smoke/tests | Runtime-wide OTel lifecycle/flush, background retention, real database sink, operational dashboards, and production load evidence | P1 |
| Distribution | Unsigned native archives, install smoke, checksums/provenance, GHCR Buildx provenance/SBOM, and optional evidence parsers | Signing/notarization implementation, attestation identity, Homebrew/Winget/Apt metadata generation, repository publication, and post-upload verification | P1 |

## External Prerequisites

`release check` currently emits these five closure blockers. They are useful tracking labels, but several also require code that does not exist yet.

| Reported blocker | External input | Code still required |
| --- | --- | --- |
| `production_signing_attestation_credentials` | CI OIDC, KMS/HSM, signing and notarization identities | Credential provider, signing/notarization, verification, and provenance wiring |
| `homebrew_winget_apt_repository_credentials` | Repository ownership and publish tokens | Package metadata generation, dry-run validation, upload, retry, and download verification |
| `platform_service_manager_test_environment` | Controlled Windows/Linux/macOS service hosts | Real platform Adapters and destructive lifecycle integration tests |
| `real_or_virtual_hardware_fixture` | Stable hardware or virtual-device fixtures | Real driver, permission, I/O, and OS hotplug implementations |
| `production_database_sink_and_retention_scheduler` | Database selection, credentials, schema, and operational policy | Database sink plus long-lived retention/maintenance scheduling |

## Interpreting `release check`

> `eva release check` aggregates compiled-in alpha checklist declarations and optional operator-supplied evidence files. It does not run Cargo or CI, connect to external services, verify OS integration, or confirm production credentials. `status:"ready"` means only that no required gate object is marked blocked for the supplied inputs; it is not production readiness.

- Static declarations cover most daemon, provider, service-manager abstraction, hardware, observability, and JSON-contract gates.
- MCP compatibility uses a repository fixture whose fields are already set to supported values; it is not an external server run.
- Artifact, distribution, scanner, and benchmark results become evidence-driven only when their files are explicitly supplied.
- Commands listed inside gate evidence are recommendation strings, not an execution log from the current `release check` invocation.

## Known Risks

| Risk | Current behavior | Required correction |
| --- | --- | --- |
| Version ambiguity | Public version is `1.11.5-alpha` while internal IDs use V1.12-V1.17 labels | Keep the distinction explicit in CLI, release, website, and planning text |
| Stale diagnostics | `doctor` still describes external Adapters as V0.3 placeholders | Align the warning with current controlled execution and unsupported transports |
| Retry evidence | Scheduler retry routes into temporary in-memory mailboxes and ACKs the durable replay without Agent handler consumption | Do not treat retry routing/ACK evidence as task execution; add owned workers before claiming recovery |
| Demo data mutation | `memory context` seeds sample records on every invocation, including durable mode | Separate examples from operator data paths |
| Restore trust boundary | Plans are unsigned and may name an absolute target root | Add trusted plan provenance and explicit target-root policy before wider use |
| Gate overconfidence | Static pass objects and fixed performance values can look like observed evidence | Record source, timestamp, commit, environment, and artifact digest for production gates |

## Verification Entry Points

Run from the repository root:

```powershell
cargo run -q -- version --output json
cargo run -q -- release check --output json
cargo run -q -- doctor --output json
cargo test --workspace
./scripts/validate-cli-json-contracts.ps1
./scripts/validate-version-management.ps1
```

These commands prove local code and contract behavior only. Platform services, hardware, external MCP servers, production telemetry, signing, and repository publication require separate environments and evidence.

## Maintenance Rules

- Update a row only after reading the owning code path and its current tests.
- Do not append per-commit or per-marker completion logs; use release notes and Git history.
- Split a controlled baseline from its production gap instead of marking a mixed row `Done`.
- Keep release evidence, runtime execution, library/test boundaries, and external prerequisites distinct.
- Update both locales, localized diagrams, website cards, and `docs/_i18n/manifest.json` in the same change.

## Related References

- [Real runtime implementation plan](v1.x-real-runtime-implementation-plan.md)
- [Process upgrade and recovery boundary](../operations/process-level-upgrade.md)
- [Backup, snapshot, and restore boundary](../operations/backup-migration-release-snapshot.md)
- [Project configuration boundary](../operations/project-configuration.md)
- [Project release plan](../release/project-release-plan.md)
- [Version management](../release/version-management-plan.md)
