> Language: English
> Detail authority: ../../zh-CN/planning/V1.x未完整实现功能清单.md
> Translation status: summary

# V1.x Incomplete Feature Inventory

Updated: 2026-07-09

This page is the English summary for the current V1.x implementation gap,
blocked-feature, and known-risk inventory. The detailed source of truth is the
Simplified Chinese document:
[V1.x 未完整实现功能清单](../../zh-CN/planning/V1.x未完整实现功能清单.md).

Eva-CLI `V1.11.5-alpha` has a buildable workspace, stable CLI envelopes,
in-memory runtime evidence, durable storage/recovery baselines, restricted Lua
execution, controlled Adapter/MCP/Skill paths, hardware and lifecycle gates,
release evidence gates, and the `emit`, `agent`, and `capability` runtime CLI
commands. It is still an alpha checkpoint, not a production daemon distribution.

## Not Implemented

| Area | Current Boundary | Still Missing | Priority |
| --- | --- | --- | --- |
| Runtime daemon and long tasks | Basic runtime, task snapshots, recovery scanner, Agent lifecycle evidence | Background daemon, long-running scheduling, provider/runtime execution-state recovery, scheduler drain mutation | P0 |
| Provider supervision | Adapter stdio/http runner, MCP stdio JSON-RPC, Skill workflow runner, provider plans and gates | OS process supervision, auth/session isolation, concurrency, rate limits, streaming, recovery | P0 |
| Full MCP integration | stdio JSON-RPC initialize/tools/list/tools/call, sessions, allowlists | HTTP transport, real streaming, auth, compatibility matrix, process supervision | P0 |
| Destructive restore | Signed backup archive, pre-restore evidence, restore apply confirmation/policy/lock/health gates | Real file mutation and rollback apply | P0 |
| Production service-manager handoff | Local state-store handoff and release pointer evidence | Windows/Linux/macOS service-manager adapter, real daemon blue-green handoff, production rollback | P0 |
| Real hardware I/O | Driver registry, simulator contract, lifecycle policy, hotplug event evidence | USB/serial/BLE/socket/vendor SDK drivers, OS permission provider, runtime hotplug subscriber | P1 |
| Memory/knowledge service | Durable file store, TTL, compression, redaction, index rebuild | Retrieval provider execution, compaction/GC, cross-process locks, policy-driven redaction, audit wiring | P1 |
| Production observability | JSONL audit/metrics/spans, best-effort degradation, trace continuity keys | tracing subscriber, OpenTelemetry SDK exporter, metrics exporter, retention/rotation, full runtime wiring | P1 |
| Release distribution | Artifact/distribution/scanner/benchmark evidence gates | Production signing/attestation credentials, Homebrew/Winget/Apt repository publication, upload pipeline | P1 |

## Blocked Features

| Feature | Why Blocked | Current Evidence | Unblock Condition |
| --- | --- | --- | --- |
| Destructive restore apply | High-risk filesystem mutation cannot be enabled by CLI flags alone | `restore plan` reports `apply_allowed:false`; gated restore still reports `mutation_executed:false` | Preserve signed archive, pre-restore backup, policy approval, lock, health, and rollback tests before real mutation |
| Daemonized supervisor handoff | Current handoff is local evidence, not a production service manager | `release check` keeps `SEC-LIFE-001` as a warning gate | Implement OS service-manager adapter and production rollback |
| Agent drain/reload mutation | CLI only emits evidence and does not mutate a daemon or scheduler | `agent drain/reload` reports `mutation_executed:false` | Wire daemon apply, scheduler generation gates, and drain state |
| Hardware raw I/O | Sample hardware manifest is disabled and CLI never grants raw handles | `hardware bind --apply` reports `status:"blocked"` and `handle_granted:false` | Add real drivers, OS permissions, simulator regression, and audit gates |
| Unauthorized providers and MCP tools | Discovery and manifest visibility are not execution authority | provider allowlists and MCP allowlists block unauthorized calls before invoke | Add supervision and policy wiring while preserving allowlist/audit gates |
| Production release upload | Evidence gates validate readiness but do not hold production credentials | release checks verify evidence and do not upload packages | Add signing/attestation credentials and package repository publication |

## Known Defects And Risks

No verified compile failure or command failure was found during this document
refresh. `cargo run -- --version` and `cargo run -- release check --output json`
passed. The current known issues are capability or product risks rather than
reproduced build failures.

| Type | Item | Risk | Recommendation |
| --- | --- | --- | --- |
| UX risk | `capability call` | Confirmed invokes can execute while `mutation_executed:false` remains correct for destructive mutation semantics | Show both `invocation_executed` and `mutation_executed` in operator-facing output |
| UX risk | `restore apply` | A gated report can allow apply while still not performing real file restore | Keep `mutation_executed:false` explicit until real mutation lands |
| Product gap | `upgrade apply --state-store` | Local release pointer mutation may be mistaken for daemonized production handoff | Keep service-manager warning gates until production adapters exist |
| Architecture gap | runtime recovery | task/event/audit evidence can recover, but provider process execution state cannot | Implement provider supervision and execution-state recovery |
| Observability gap | production telemetry | JSONL evidence exists, but full runtime/provider/task events are not wired to OpenTelemetry/metrics exporters | Wire runtime audit first, then exporter/retention |
| Documentation risk | language sync | Chinese is the detail authority; English is a summary | Keep this page as summary and update the Chinese inventory first |
