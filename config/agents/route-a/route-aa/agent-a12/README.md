# Agent A12 / 智能体 A12

## 中文

`agent-a12` 是 `/sys/route-a/route-aa` 的第二个子 Agent 示例，用于验证 fanout 路由和同 Topic 多 Agent 投递。

## English

`agent-a12` is the second sample child Agent for `/sys/route-a/route-aa`, used to validate fanout routing and multiple Agent delivery on one Topic.
