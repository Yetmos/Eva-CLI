# Agent A11 / 智能体 A11

## 中文

`agent-a11` 是 `/sys/route-a/route-aa` 的子 Agent 示例，用于验证父子关系、订阅和发布权限配置。

## English

`agent-a11` is a sample child Agent for `/sys/route-a/route-aa`, used to validate parent references, subscriptions, and emit permissions.
