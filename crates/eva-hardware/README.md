# eva-hardware / 硬件接入

## 中文

`eva-hardware` 负责设备发现、driver binding、热插拔状态和 HardwareAdapter。它不允许 Lua 直接进行 raw I/O。

## English

`eva-hardware` owns device discovery, driver binding, hotplug state, and HardwareAdapter boundaries. It does not allow Lua to perform raw I/O directly.
