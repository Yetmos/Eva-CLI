# eva-config / 配置加载

## 中文

`eva-config` 负责 `eva.yaml`、manifest、schema 加载和配置归一化。它不做权限决策，也不直接修改运行时状态。

## English

`eva-config` owns `eva.yaml`, manifests, schema loading, and configuration normalization. It does not make permission decisions or directly mutate runtime state.
