# Manifest Modules / Manifest 模块

## 中文

本目录保存 Agent、Adapter 和 capability manifest 的配置边界。manifest 只描述声明和归一化，不执行授权。

## English

This directory contains configuration boundaries for Agent, Adapter, and capability manifests. Manifests describe declarations and normalization only; they do not authorize execution.
