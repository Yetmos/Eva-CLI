[package]
name = "eva-observability"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
eva-core = { path = "../eva-core" }
tracing = "0.1"
tracing-subscriber = "0.3"
