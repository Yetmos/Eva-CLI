# eva-backup / 备份迁移

## 中文

`eva-backup` 负责备份、迁移包、release snapshot 和 artifact 校验。Agent 不能直接拥有 restore 或 rollback mutation。

## English

`eva-backup` owns backups, migration packages, release snapshots, and artifact verification. Agents must not directly own restore or rollback mutation.
