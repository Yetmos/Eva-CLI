# eva-lifecycle / 生命周期管理

## 中文

`eva-lifecycle` 负责 Supervisor、runtime generation、drain 和 rollback。它不承载 Lua 业务决策。

## English

`eva-lifecycle` owns Supervisor, runtime generations, drain, and rollback. It does not own Lua business decisions.
