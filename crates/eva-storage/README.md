# eva-storage / 持久化存储

## 中文

`eva-storage` 负责 StateStore、Durable Event Log、ArtifactStore 接口和本地实现。它不承载 Agent 业务逻辑。

## English

`eva-storage` owns StateStore, Durable Event Log, ArtifactStore interfaces, and local implementations. It does not own Agent business logic.
