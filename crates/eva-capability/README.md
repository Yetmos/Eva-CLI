# eva-capability / 能力注册

## 中文

`eva-capability` 负责 Capability registry、generation swap 和 host API trait。它不实现具体 provider 或硬件 driver。

## English

`eva-capability` owns the Capability registry, generation swap, and host API traits. It does not implement concrete providers or hardware drivers.
