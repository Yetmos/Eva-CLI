# eva-scheduler / 调度器

## 中文

`eva-scheduler` 负责 Topic matcher、订阅表和 Agent mailbox 投递。它不执行 Lua，也不调用 Adapter。

## English

`eva-scheduler` owns Topic matching, subscription tables, and Agent mailbox delivery. It does not execute Lua or invoke Adapters.
