# eva-cli / 命令行入口

## 中文

`eva-cli` 负责 CLI 命令解析和用户入口。它可以调用 `eva-runtime` 启动或检查系统，但不拥有核心运行时状态。

## English

`eva-cli` owns CLI command parsing and the user-facing entry point. It may call `eva-runtime` to start or inspect the system, but it does not own core runtime state.
