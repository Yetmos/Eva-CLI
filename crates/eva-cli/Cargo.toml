[package]
name = "eva-cli"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
eva-config = { path = "../eva-config" }
eva-core = { path = "../eva-core" }
eva-observability = { path = "../eva-observability" }
eva-runtime = { path = "../eva-runtime" }
