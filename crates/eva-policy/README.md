# eva-policy / 权限策略

## 中文

`eva-policy` 负责 effective permissions、沙箱规则和 request 级权限收紧。它不做 Discovery 扫描，也不执行具体 I/O。

## English

`eva-policy` owns effective permissions, sandbox rules, and request-level permission narrowing. It does not perform Discovery scanning or concrete I/O.
