# eva-eventbus / 事件总线

## 中文

`eva-eventbus` 负责事件发布、可恢复日志集成和死信路径。它不负责 Agent Topic 订阅匹配。

## English

`eva-eventbus` owns event publication, recoverable log integration, and dead-letter paths. It does not own Agent Topic subscription matching.
