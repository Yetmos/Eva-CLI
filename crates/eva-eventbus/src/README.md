# eva-eventbus/src / 事件总线源码

## 中文

这里保存事件发布、恢复日志接入和死信路径。Topic 订阅匹配和 Agent 投递应留在 `eva-scheduler`。

## English

This directory contains event publication, recoverable log integration, and dead-letter paths. Topic subscription matching and Agent delivery belong in `eva-scheduler`.
