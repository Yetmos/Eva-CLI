# Schemas

The V1.0 basic example reuses the repository-level schemas through `config.schema_dir: ../../config/schemas` in `config/eva.yaml`. Schema files are intentionally omitted from this compact example workspace so validation stays aligned with the canonical configuration model.
